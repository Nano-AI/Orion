# Orion — Decision Log

One line per decision. Newest last.

⚠ **This ledger is checked by `tools/check-decisions.py`**, which fails on a
duplicate number, on a number cited by the tree that has no row, and on a gap
that is not declared below. Run it with the other gates. It exists because the
queue in `STATUS.md` offered **already-shipped work as the next story three
times**, and the third time the reason was that #112 — the decision that closed
the queue item — had never been written down at all.

⚠ **Declared gaps: 22, 23, 24.** No row was ever written for any of them —
checked with `git log -S` over this file's whole history — and **nothing in the
tree cites them**, in code or in prose. They are numbers that were skipped on
2026-07-27, not decisions that were lost, and there is nothing to recover. Left
as gaps rather than backfilled with something invented, and listed here so the
check above can tell a skipped number from a missing one.

⚠ **`71b`** is a second decision written on 2026-07-30 into a number already
taken by the wordmark row. Nothing cited it, and the wordmark's `#71` is cited
once, so the uncited one took the letter — the number is an identifier, not a
sort key, and its position in the table still carries the date.

⚠ **The header used to warn that two rows were numbered 96** — the
highlight-reconstruction method and the creative vignette — and said twelve
files cite "decision #96" and every one means the vignette. **All three claims
are false as of 2026-08-02.** There is **one** row 96 and it is the highlight
reconstruction; there is **one** citation of #96 in the tree,
`engine/shaders/hl_mask.slang:110`, and it means region scoping, which is that
row. The twelve were `.claude/worktrees/` copies of one file counted as twelve.
The duplicate that really existed was **71**, which the warning never mentioned,
and it sat under a warning about the wrong number for two days.

| # | Date | Decision | Why |
|---|---|---|---|
| 1 | 2026-07-27 | Planning docs live in `planning/`, kept concise | Single source of truth for Opus build sessions |
| 2 | 2026-07-27 | macOS first; Windows/Linux later | Smaller test matrix, native Metal + Core ML, ship sooner |
| 3 | 2026-07-27 | **No Rust.** Engine in C++20 | User can't debug Rust. C++ also has the stronger imaging ecosystem (LibRaw, OCIO, lensfun, lcms2) |
| 4 | 2026-07-27 | GPU via Metal; shaders authored in Slang | Best Mac tooling + f16 + ANE path; Slang keeps SPIR-V/HLSL portability open |
| 5 | 2026-07-27 | Engine = compute DAG, one shader per node, `rgba16f` linear Rec.2020 | vkdt's proven design; avoids darktable's dual CPU/OpenCL maintenance tax |
| 6 | 2026-07-27 | Scene-referred pipeline, exactly one display transform, no Lab | Avoids artifacts/hue shifts of display-referred; industry direction |
| 7 | 2026-07-27 | Default tone mapper: AgX-family sigmoid, 2–3 sliders | Great defaults without filmic's fiddly multi-tab UI |
| 8 | 2026-07-27 | Demosaic: RCD default (MIT impl), bilinear for thumbs | Near-AMaZE quality at PPG speed, excellent GPU fit |
| 9 | 2026-07-27 | XMP sidecars = source of truth; SQLite = disposable rebuildable index | Folder-first workflow, no catalog lock-in, but instant filtering on large shoots |
| 10 | 2026-07-27 | Avoid exiv2 (GPL-2.0); use Apple ImageIO for metadata | Prevents forced open-sourcing / commercial license fee |
| 11 | 2026-07-27 | Sony ARW is the only priority format for v1 | User's camera; dogfooding beats breadth |
| 12 | 2026-07-27 | Engine is a library with a narrow C API; UI is a client | Makes UI choice reversible and Windows port tractable |
| 13 | 2026-07-27 | **No Vulkan.** Metal only on macOS | Vulkan needs ~1000 lines of boilerplate to draw a triangle; unmaintainable for a solo dev |
| 14 | 2026-07-27 | Maintainability is a hard constraint: one node = one 50–150 line shader; adding a feature = 3-file change | Solo developer must be able to hand-edit any file months later |
| 15 | 2026-07-27 | Build bilateral grid + Bilateral Guided Upsampling as first-class DAG node types in M1 | General escape hatch for the 16 ms budget — decouples algorithm cost from slider responsiveness (MIT/Google, open-source impl) |
| 16 | 2026-07-27 | `CLAUDE.md` at root + `planning/STATUS.md` updated every session | Survives context clears and session wipes |
| 17 | 2026-07-27 | **No bespoke/Skia UI layer** | 1.5–2.5 engineer-years for macOS alone; Zed took 5 yrs + 1M LOC funded. Impossible solo |
| 18 | 2026-07-27 | No Dear ImGui for product UI — but **use it for internal debug tooling** | Its own FAQ says skinning is limited, built for debug tools. Perfect for a pipeline inspector |
| 19 | ~~2026-07-27~~ | ~~UI shell = SwiftUI/AppKit~~ **SUPERSEDED by #21** | Was based on two errors: HDR is not Qt-exclusive-missing, and the platform-integration argument killed bespoke, not Qt |
| 20 | ~~2026-07-27~~ | ~~`@Observable` view models~~ **VOID** — SwiftUI not chosen | |
| 21 | ~~2026-07-27~~ | ~~UI shell = Qt 6 / QML~~ **SUPERSEDED by #25** | Was correct when the criterion was "stay in one language". Priorities shifted to stunning look + minimal bloat, where native wins |
| 22–24 | ~~2026-07-27~~ | ~~Qt LGPL / styling rules~~ **VOID** — Qt not chosen | Note for the record: Qt is **not** paywalled. LGPLv3 is genuinely free for closed-source commercial use with dynamic linking |
| 25 | 2026-07-27 | **UI shell = SwiftUI/AppKit + C++ engine** (final) | Stunning look and minimal bloat are the top two priorities; native wins hardest on both. ~5–15 MB bundle vs Qt's 40–60 MB; Apple materials/vibrancy/typography free vs building a visual language from scratch. Zero licensing obligations |
| 26 | 2026-07-27 | View models = plain `@Observable` objects, zero SwiftUI types inside | Any panel re-hostable in AppKit when SwiftUI hits a density cliff. Cheap on day one, expensive to retrofit. Sets macOS 14+ floor |
| 27 | 2026-07-27 | Narrow hand-authored POD facade at the C++↔Swift boundary | No templates, no move-only types, **no exceptions escaping (they terminate the process)**. Only boundary stable across Swift releases |
| 28 | 2026-07-27 | Rejected: C++ webview shells (`webview/webview`, saucer), WebGPU, WebAssembly, Go | Webview canvas layering is fragile and the canvas *is* the app. WebGPU is an abstraction over Metal (no speedup). Wasm is slower. Go has no imaging libraries, cgo overhead, GC in the pixel path |
| 29 | 2026-07-28 | **Clip the mosaic to a single white level after white balance**, in `linearize`, before demosaic | A blown pixel arrives as (S, S, S) and white balance turns it into the gains — magenta on every clipped light. dcraw's default highlight mode; before demosaic because RCD interpolates across an unclipped neighbour and leaves a fringe rather than a clean edge. Costs the headroom highlight reconstruction could have used, which is the right trade: the cast was on every frame, the reconstruction is optional |
| 30 | 2026-07-28 | **A control's readout is its own reset**, colored by whether it has moved | One affordance says which controls are modified *and* what is clickable. A separate change dot in the margin would say it twice, smaller and harder to hit. The base is per photo, not per type: white balance is the camera's reading |
| 31 | 2026-07-28 | **Export converts through ColorSync, tagged sRGB at source** | A chromatic adaptation typed in here is a cast waiting to happen, and ColorSync's definitions are what every other application reads the file against. Tagging the pixels as the destination instead of converting them is how a file opens oversaturated |
| 32 | 2026-07-28 | **Export metadata comes from ImageIO reading the RAW**, not from LibRaw or exiv2 | exiv2 is GPL (#10). ImageIO reads the container's own EXIF/TIFF/GPS blocks, so the export carries what the camera recorded. Orientation is dropped on purpose — the geometry node already applied it, and copying the tag turns the picture twice |
| 33 | 2026-07-28 | **American spelling throughout**, code and docs | The developer's own English. `denoiseColour` is still read from old sidecars |
| 34 | 2026-07-28 | **Edits autosave: coalesced writes plus a flush on `willTerminate` and on resign-active** | `saveDevelop` ran only when switching photos, so open-one-file-edit-quit lost everything. Two triggers because they fail differently: coalescing covers the crash and the logout, `willTerminate` covers the ⌘Q that lands inside the coalescing window. The queued state travels *with* its URL, so a write cannot be misfiled during the ~50 ms a photo switch spends between `current` moving and the decode finishing |
| 35 | 2026-07-28 | **Lens corrections autoscale to keep the frame full** | poly3 pins the corners, not the edges: at k₁ = −0.35 the edge midpoints fetch 325 px past a 6024 px frame and the clamp smears the border column into a band. lensfun, darktable and Lightroom all zoom for this reason. Computed on the host in `pipe/LensGeometry.h` — it is a question about every pixel at once, which a shader cannot answer |
| 36 | 2026-07-28 | **A disabled node's consumers are told it is disabled** | `Pipeline::resolve` walks a disabled node back to the last live producer, which is correct for pixels and wrong for anything read as something other than pixels. `develop_linear` was reading the colour matrix output as log2 luminance and as guided-filter coefficients. Any node whose input carries a *meaning* rather than an image needs a flag, not just a binding |
| 37 | 2026-07-28 | **Every bench probe asserts a magnitude, against its own baseline, and gates the exit code** | "Did this change something" passed for a blacks slider delivering 39% of its effect. Floors are ratios against a reference control on the same frame, so they survive a change of photo. A known defect may be `WAIVED` with a reason string that prints every run — absent from the output is not an option |
| 38 | 2026-07-28 | **The graph's tail is eight bits for the screen and sixteen only around an export** | The drawable is `bgra8Unorm`, so `rgba16f` through the two largest nodes bought precision nothing could display, at 3.0 ms of a 16 ms budget and 383 MiB. Export widens it, reads, and narrows again. The display node dithers (ordered, Bayer 4x4) because geometry resamples its output and quantises a second time, and two roundings of a gradient is where contouring comes from. The bench asserts the two paths agree to better than one 8-bit step |
| 39 | 2026-07-28 | **Intermediates are not aliased, and that is deliberate** | Render-graph resource aliasing would cut 4.2 GiB to roughly 600 MB, but a cached node's output has to stay resident — aliasing and per-node caching are mutually exclusive. The footprint *is* the cache, and it is what makes an exposure drag 3 nodes instead of 28. The answer for smaller machines is the preview-ROI path, which shrinks every intermediate, not aliasing |
| 40 | 2026-07-28 | **Geometry stays a separate node from the display transform** | Fusing them would save ~2 ms, but `geometry.slang` resamples *display-encoded* pixels on purpose: averaging unbounded scene-linear values blooms a specular edge, which is why film and VFX resample in log rather than linear. Fusion forces scene-linear resampling and merges two small shaders into one large one, against the maintainability constraint. Revisit only if the budget gets tight — there is 6.4 ms of headroom |
| 41 | 2026-07-28 | **Grading zones are Gaussian bands on log2(Y/0.18), and the offset scales with luminance** | Linear-luma zones put middle gray at 0.70 shadows, so the shadow wheel graded the whole picture and the highlight wheel was inert. Same partition-of-unity construction as the tone bands, so a photograph has one idea of where its shadows are. Scaling the offset by Y makes a wheel a constant chromaticity shift at every exposure (0.1077 relative chroma at -3 EV against 0.1079 at +3), and makes the deep-shadow clamp that broke zero-sum unrepresentable. `k = 0.25` is derived: saturation = 1.5k/(1+k), so full travel is 30% from neutral |
| 42 | 2026-07-28 | **Bench probes gate on mean absolute per-pixel movement, not on a summary metric** | A grading wheel rotates hue at roughly constant saturation, so mean luma, mean chroma *and* mean saturation each reported a working wheel as doing nothing — three instruments, same blind spot, because a frame mean cancels a rotation. The summary is still printed for insight into *what* changed; movement decides *whether* it did. Floors are half the smaller ratio measured across two very different sample frames, because one frame was not enough — four probes calibrated on a night sky tripped on a lit forecourt |
| 43 | 2026-07-28 | **Orion is open source. Paid distribution is out of scope.** Supersedes the "undecided by choice" position in STATUS's blocked list | Settles the question that gated several others. The existing rule — avoid GPL, dynamically link LGPL — is kept anyway, because it costs nothing today and it is what keeps the decision reversible. LibRaw is dual LGPL-2.1/CDDL-1.0: taking it under CDDL permits static linking, which is the escape hatch if dynamic linking ever becomes awkward for packaging |
| 44 | 2026-07-28 | **Do not redistribute Adobe's `.dcp` profiles, and do not ship RawTherapee's either** | Adobe publishes no redistribution grant for the profiles bundled with DNG Converter, and projects that derive from them tell users to generate their own copies rather than share outputs. RawTherapee's bundled profiles are user-submitted with no stated licence of their own, inside a GPL-3 project. The DNG *specification* is separate and carries a royalty-free patent grant, so writing a DCP **reader** is fine — but any implementation distributed under that grant must carry a notice crediting DNG technology licensed from Adobe. Put it in the about box before any DNG code ships |
| 45 | 2026-07-28 | **OPEN: LibRaw's built-in colour matrices may themselves be Adobe-derived** | LibRaw's `cam_xyz` table descends from dcraw's `adobe_coeff`, whose matrices were widely understood to have been extracted from Adobe DNG Converter. If so, Orion already ships Adobe-derived colour data — the same position as darktable, RawTherapee and every other dcraw descendant, and unremarkable for an open-source project. **Evidence, checked 2026-07-28:** two independent documenters describe the table that way — Doug Pardee's ProjectDcraw notes dcraw was updated "10–14 days after Adobe releases a new version of Adobe DNG Converter", and Elle Stone's annotated dcraw source calls it "the Adobe-dng camera coefficients". Consistent, but still second-hand: no primary statement from dcraw's author was found. Stays OPEN — it would need answering before any change of distribution model, not before the next commit |
| 46 | 2026-07-28 | **A silent +1.2 EV baseline exposure, and base contrast 1.45** | Orion opened a daylight frame 1.3x darker than the camera's own JPEG, which read as flat and washed *and* pushed most of the picture into the grading shadow zone — one root, two complaints. The DNG spec's `BaselineExposure` (tag 50730) is the mechanism, applied silently on open, which is why the Exposure slider still reads 0.00. Value fitted against two independent references over six patches per frame: +1.20 EV / 1.45 is the joint best on both frames that carry signal, and mean error on the daylight frame fell 0.1543 -> 0.0194. LibRaw does not carry the tag for native ARW, so it is measured rather than read. See research/camera-profiles.md |
| 47 | 2026-07-28 | **Bench probe floors are calibrated across three frames, not one or two** | A night sky, a lit forecourt and a bright daylight cityscape. One frame tripped four probes on the second; two frames tripped six on the third — a bright frame genuinely has no deep blacks, little noise and few shadows, so those controls move less in it and that is not a regression. Half the minimum ratio over three very different pictures |
| 48 | 2026-07-28 | **The camera profile gets the DNG spec's HueSatMap stage, applied in linear ProPhoto HSV** | A 3x3 matrix is linear, so it cannot correct an error that only appears where a stimulus is saturated and narrow-band — no consumer sensor satisfies Luther-Ives. Blue sky is the worst case and Orion rendered it violet. The spec's answer is a hue/saturation table, and Orion builds one for real (90 x 25, `ValueDivisions = 1`, spec entry and loop order) rather than special-casing blue, so a `.dcp` reader later is a reader and nothing else. **`ProfileHueSatMapEncoding = 0` requires linear ProPhoto HSV**, not the working space's HSV, or the cells sit at the wrong hue angles the day a real profile loads — the node converts in and out. Table populated by a correction fitted on one hue region against two independent renderings: -8 degrees, saturation x1.05, centred at 250 degrees over a 60-degree half-width. Sky R/B 0.622 -> 0.451 against a target of 0.450. Costs one full-res pass, upstream of exposure, so the gate is unmoved at 9.37 ms p95 |
| 49 | 2026-07-28 | **Lens corrections read lensfun's database, and do not link lensfun** | The maths was never the missing part — the models were already implemented, tested and running on the GPU; what was missing was the measured numbers, and those sit in XML. Linking the library would add an LGPL-3 dependency, a build step and a second implementation of the same polynomials to obtain a value that can be read directly. The database (1,558 lenses) is vendored under `data/lensfun/` unmodified with its CC BY-SA 3.0 licence; attribution and share-alike apply to the data, not to Orion's code. The shader now evaluates ptlens, of which poly3 is the a = c = 0 case, so both of the database's distortion models land in one kernel |
| 50 | 2026-07-28 | **A confident wrong lens profile is worse than no profile**, so matching is conservative | It distorts the frame and reports that it measured it. Names normalize to letters and digits only (which collapses `f/`, case and punctuation), match by containment either way with the longest winning, and are refused below eight characters — `50mm` is inside a hundred entries and identifies none of them. A match on a differently-spelled name is flagged `approximate` and the panel says so. The developer's own Sigma 24mm F1.4 DG DN is not in the database and correctly reports nothing rather than borrowing the DSLR DG HSM entry, which is a different optical design |
| 51 | 2026-07-28 | **A profile replaces the manual sliders rather than adding to them** | Two sources for one coefficient is a control that fights the data: the profile knows what this lens does at this focal length, and a slider on top would be undoing a measurement by feel. The sliders grey out while a profile is on. Chromatic aberration stays manual in both cases — the database's TCA figures are per-copy, sample variation is the same order as the aberration, and a wrong one adds coloured edges |
| 52 | 2026-07-28 | **Exports strip GPS unless asked otherwise** | `ImageWriter` copied the whole GPS block onto every export, and the panel had no metadata control at all — a photo taken at home published the home coordinates to anyone who downloaded it, silently, with nothing in any viewer to say so. Three-way control (keep all · strip location · strip everything), defaulting to strip location, and the default is asserted in the suite because the failure mode is invisible |
| 53 | 2026-07-28 | **The generated design tokens are the palette; the hand-written mirror is deleted** | `DesignTokens.swift` is generated from `design/tokens.json` and nothing imported it — the app re-declared `Palette` by hand, with two constants renamed, one dropped and two invented. The generator also emitted Display P3 while the mirror built sRGB, so the app rendered colours the token file did not specify. Tokens are authored as sRGB hex, which is what the HTML mockup renders and where the palette was judged, so the generator emits sRGB and gains a numeric `Components` enum for the consumers that cannot take a SwiftUI `Color` (the Metal clear colour, the canvas surround) |
| 54 | 2026-07-29 | **Brush masks are reinstated into M4**, reversing the v1 cut recorded in ROADMAP and FEATURES | Asked for directly by the developer. The original cut named three costs — stroke capture, edge-aware snapping, and mask storage — and two of them turned out not to be costs. **Storage is a list of dab centres, not a raster**: a few kilobytes, resolution-independent, and exact under crop or rotation because each centre goes through the same transform the image does, where a raster mask would be 24–120 MB at 24–60 MP and compound blur every time it was resampled. **Edge-aware snapping is the guided filter Orion already has**, built and tested for dehaze — masking.md §4 is a second input binding on it, not a new algorithm. What remains is stroke capture, which is one kernel and the canvas gesture work the gradient overlay just paid for. The kernel and its tests landed the same day the decision was reversed; the interface has not. Recorded rather than quietly built, because ROADMAP and FEATURES both stated the opposite and two copies of "what we are building" disagreeing is finding #14 of the outside review |
| 55 | 2026-07-29 | **One brush node serves all four mask kinds, chained after the gradient rather than replacing it** | The graph cannot swap a node's kernel per render, and writing into a node's output from outside the graph is worse than one extra pass. Chaining works because a pass with no dabs is the identity — a property the suite already pinned, and which is now asserted over *every* pixel rather than one, because that is what the pass-through actually rests on and an off-by-one would surface at an edge. So: kind 0 gradient writes 1.0 and the brush passes it through; kinds 1 and 2 pass the gradient through; kind 3 starts from empty and lays the stroke. ⚠ The risk was never the brush — it is that every existing gradient mask now reaches `develop:linear` through a new node, so the three pre-existing measurements were re-taken and are exact: 0.4703, 0.1981, 0.4110 |
| 56 | 2026-07-29 | **Brush dab centres live outside `Adjustments`; the struct carries only a revision counter** | `Adjustments` is compared field by field on every slider tick to decide what to redirty. Carrying a stroke through that comparison would make every tick walk thousands of points. `DevelopPipeline` owns the list, set through its own facade call, and `brushRevision` — a single int — is what tells `apply` the mask is stale. The trap this creates is real and is documented at both the C and Swift boundaries: change the points without bumping the revision and the picture stops following the hand, with nothing in the code looking wrong. `Engine.setBrushStroke` therefore does both, so no caller has to remember |
| 57 | 2026-07-29 | **The mask overlay is view state, not edit state** | It is how the photographer is *looking* at the picture, not a change to it. So it is deliberately absent from `DevelopState`: it never reaches the XMP sidecar, never enters undo history, and never follows the photo to another machine. `export()` forces it off around the write and restores it afterwards, including when the export throws — an export with the overlay on would write a red-tinted photograph and nothing in the file would say why. ⚠ That guard is correct by construction but has no test |
| 58 | 2026-07-29 | **The landing site is dependency-free static files in `web/`, deployed by GitHub Actions to Pages** | React and Framer Motion were scaffolded and reverted. The page has no interactivity by explicit requirement, so React's state-to-UI core is unused, and no gestures, so Framer Motion reduces to one `IntersectionObserver`. What they would have cost is a build step, a lockfile in a C++ repository, and Vite's `base` trap — the default emits absolute asset paths that 404 on a project page **while the deploy still reports success**. That failure mode already occurred once here via `.gitignore`'s `orion-*.jpg` rule silently swallowing `web/img/*.jpg`: green push, green deploy, every image missing. Rule that came out of it: a green deploy is not proof — verify the page *and every asset* returns 200 |
| 59 | 2026-07-29 | **The landing page's static state is the finished page; JavaScript rewinds it** | The redesign adds two scroll-scrubbed scenes — the hero develops the raw as you scroll, a gradient mask sweeps a sky — and both are gated on an `html.js` class the script adds as its first act. Without JavaScript, or under `prefers-reduced-motion`, nothing is hidden and nothing is flat: the developed photograph, the placed mask and the final readout values are what the HTML itself contains. The old page had this backwards — reveal-hiding was unconditional and only JavaScript could undo it, so a no-JS visitor got a blank page |
| 60 | 2026-07-29 | **Landing page display face is Bricolage Grotesque, self-hosted in `web/fonts/`** | The page needed real display weight and the system stack cannot carry a headline at 9 rem. Self-hosted (77 KB variable woff2, latin) rather than loaded from a font service, for the same reason the app has no accounts: no third-party request, works when its owner is the only server left. Chrome discipline unchanged and now written down: teal for live numbers only, plus one new ink — film-rebate amber, from the app's own `star` token, for the frame counters that number the sections, and nothing else |
| 61 | 2026-07-29 | **The landing hero is a viewfinder, and the title is the name** | The sentence headline read as a generic hero. Now the page opens inside a camera: frame brackets, a thirds grid, an AF point that turns teal the moment the develop lands, and ORION set in Michroma — the wide engraved-on-the-body lettering cameras actually use — where the eyepiece display sits. Scrolling still develops the raw; keep going and you push through the finder, the overlay scaling past the eye while the photograph stays. The lede and the numbers moved to a strip below the hero so the finder stays clean. Michroma is self-hosted like the other two faces (11 KB) |
| 62 | 2026-07-29 | **Mask groups are one component kernel instanced per node, folding left from a written zero** | Decision #55's two-node chain (gradient + brush, one always idle) existed only because the graph cannot swap a kernel per render; a `kind` branch inside one kernel is what was actually wanted, and the merge deleted the pass-through contortion — and fixed a fourth dead control it had created (invert never reached a brush, because the gradient node held the invert and the brush node discarded its output). The group folds left in listed order per research/masking.md §6 — add is `max` (no buildup in the overlap, which is the section's whole point), subtract `α₁(1−α₂)`, intersect `α₁·α₂` — starting from a `mask:base` node that writes 0, the additive identity, so the first component needs no special case and a group of one runs the same code as a group of four. `kMaxMaskComponents` is 4: each live component is a full-res R16F pass (~46 MiB at 24 MP), so the cap is a memory number, not a concept. Compose ops and the falloff live in `ops/mask_ops.slang` — the falloff had been two identical copies each claiming to be shared. Both compose mutations (add→screen, invert-skips-brush) die |
| 63 | 2026-07-29 | **Film-rebate amber enters the app as structure only, never near the photograph** | The landing site numbers frames in amber (`star`, #F0C674) and the app had no tie to it. Amber now marks section nameplates that hold a moved control, the selected tool plate's top edge, and the histogram's clipping flags — all at the far end of the window from the canvas. It is kept off the canvas surround, off swatches and out of the colour panels, because the neutral surround exists so colour can be judged and a warm ink beside the picture biases that judgment. One ink, two roles: the rebate amber and the star rating are the same mark, the frame's own index |
| 64 | 2026-07-29 | **Version tracks the milestone; `v1.0.0` is reserved for FEATURES' v1 column** | Asked for a "v1 build"; shipped `v0.4.0-alpha.1`. Minor is the milestone in flight, so M4 closing gives 0.5.0. `FEATURES.md` still lists luminance/colour range masks, spot removal, presets, copy/paste sync, batch export and AI subject selection as v1, and a build labelled v1 promises them. The number cannot be reused; the rule can be followed by anyone |
| 65 | 2026-07-29 | **Runtime resources resolve from the bundle when there is one, and from the build tree otherwise** | `ORION_SHADER_DIR` and `ORION_DATA_DIR` are absolute paths into the source tree — correct for the tests and the bench, and fatal for anything installed: a copied app found no metallibs and died on the first open. `src/ResourcePaths.cpp` prefers `Contents/Resources` when it exists and falls back to the compile-time path, so one rule covers both and the bench needs no special casing. Verified by hiding `build/shaders` and `data/lensfun` and rendering with the packaged app — with the control that the build-tree app fails on the same run, which is what says the test was real rather than the fallback quietly working |
| 66 | 2026-07-29 | **Auto-enhance replaces the five controls it owns, and resets them before measuring** | Auto was not idempotent: press one left the frame corrected, press two measured *that* frame, derived a different look from it and solved the tone controls differently — +2.25 EV with lift 0.16, then +2.99 EV with lift 0.00. It also falsified the code's own comment, which says the look must respond to the photograph rather than to the correction; from the second press onward it responded to the correction. Auto now zeroes exposure, whites, blacks, lift and clarity before its first measurement, so it is a function of the photograph and pressing it twice is a no-op. Only those five: white balance, contrast, the crop and the curve are measured *through* and never discarded. Second cause, fixed with it: the solver ran a flat six passes, and because every step undershoots by construction (see `kDamping`) a frame far from the anchor ran out of passes short of it. It now stops when the median is within `kSettled` of the anchor, capped at 24 — the sample frames need 6, 11 and 17 |
| 67 | 2026-07-29 | **A control that changes the picture goes through `Engine.edit`, never a bare assignment** | Auto set its five sliders by direct assignment, with a comment claiming that kept them on the path an ordinary edit takes. It did not: `edit(_:_:)` is what records history, and a bare assignment records nothing — so Auto left no entry and the next undo stepped past it to the edit *before* it, appearing to discard everything the photographer had done. The rule is now explicit because the failure is invisible: the picture updates, the control shows the new value, and only the undo stack is wrong |
| 68 | 2026-07-30 | **Every piece of graph state that is not an `Adjustments` field is fanned out to both graphs by `orion::Engine`, not by the facade** | `setAdjustments` already sent the adjustment block to the full graph and the preview, and said so in bold. Brush dabs, raster mattes and creative LUTs are not in that block — they went through `developMutable()`, which is the full graph alone — so a painted mask vanished for the length of every drag and came back when the hand stopped. Worst on exactly what the preview exists for: local exposure through a brush mask is a slider. The routing belongs in `Engine` because that is the only object that knows both graphs exist; the facade calling `developMutable()` for anything stateful is now the smell. **One deliberate exception, `setWideOutput`**: sixteen bits exist for export, export reads `develop()`, and widening the preview's tail would reallocate two textures for a picture nothing measures — the reason is at the call site so it does not get "fixed" |
| 69 | 2026-07-30 | **`DevelopPipeline::reload` clears the state a reload cannot replace** — the brush dabs and the mattes | A reload is a different photograph through the same compiled graph, which is every frame of a folder from one camera. It re-pushed every parameter block and left the two things that are not parameters where they were, so opening the next photo with a saved Subject row rendered the *previous* photograph's subject — full coverage, right coordinate space, wrong picture. ⚠ `STATUS.md` had asserted the opposite for two sessions, and it was true only because `Engine.assign` happens to re-send every stroke on open. An invariant that holds because a caller does the right thing is not an invariant, and it belongs in the object that owns the state |
| 70 | 2026-07-30 | **A point on the displayed picture reaches the frame through `displayedToFrame`, never through a hand-rolled inverse** | `Engine::sampleAt` undid the quarter turn and nothing else, so the colour-mixer's scene sample read whatever sat at that fraction of the *uncropped* frame: the yellow car picked band YELLOW at 58.8 degrees uncropped and MAGENTA at 303.3 under a crop, and the drag then moved a band the car is not in. `mask::toFrame` already composes the crop, the straighten and the turns and is tested against renders; a second transform beside it is a second thing to keep in step. Same rule as #36 — the failure was not a crash but a plausible wrong answer |
| 71 | 2026-07-30 | **The hero wordmark reads through a centre-spot ND, not a scrim, and the type carries a contact edge instead of a glow** | ORION sits on the blown showroom of the plate: measured, the background behind the wordmark peaked at L 1.000 with 15% of the box above L 0.5, so white type on it was **1.0:1** — invisible, which is what the developer kept reporting. The fix is the filter a photographer would screw on. Compositing neutral black at 0.56 alpha *is* a multiply by 0.44, so it scales light rather than adding ink: the clipped highlight comes down about a stop and a half while the asphalt, already at L 0.008, does not move. The photograph keeps its shape and the flanking cars keep their colour — a flat wash would have muddied all three. Elliptical, core held out to 52% because the tracked wordmark is wider than the viewport and a tighter core left the N standing on the white car unfiltered. Worst case 1.0:1 → **3.64:1**, mean 4.6:1 → 13.6:1. The two text-shadows it replaces were a 34px and a 70px blur, which light the area around a letter without ever defining it; display glyphs in a finder carry a thin dark keyline so they survive whatever the scene is doing, and that is what they carry now. No `mix-blend-mode` — alpha-black is the same arithmetic with one less compositing layer. Rule the measurement earned: type over a photograph is a number, not an opinion; sample the plate before arguing about the shadow |
| 71b | 2026-07-30 | **A selection of one photograph is not a selection**: a batch acts on everything in view until two are chosen | The photo on the canvas is always in the set — every route to a new photo collapses the selection onto it — so one is the resting state of the interface rather than a decision. Reading "act on the selection whenever it is non-empty" makes Export All silently export a single frame, for every user, on every folder, and it looks like the button being broken. The alternative is a flag recording whether a selection was deliberate, which is a second piece of state that can disagree with the first; counting needs none, and the filmstrip shows a count only once it means something, so what a batch will do is what the strip says it will do. Lightroom draws the same line. ⚠ Two supporting rules with the same character: a modified click opens nothing (forty frames chosen is not forty raw decodes), and the open photograph cannot be command-clicked out of the set (its settings are what a sync copies *from*) |
| 72 | 2026-07-30 | **The colour range mask measures Euclidean distance in Oklab chromaticity (a/L, b/L), and excludes lightness** | `masking.md` §4b had deferred the choice to CIE76 or CIEDE2000; both were rejected. CIEDE2000 is scoped to near-threshold differences (ΔE ≲ 5) while a photographer drags a tolerance at 10–40, and any monotone miscalibration is absorbed by the slider anyway — what a slider cannot fix is the *shape* of the iso-distance surface, and there CIELAB's documented blue-to-purple bend is the flaw that bites, since sky is the first thing anyone reaches a colour mask for. CIEDE2000 also has genuine discontinuities in its mean-hue handling (Sharma, Wu & Dalal 2005), and a discontinuous distance feeding a smootherstep prints a seam across a smooth gradient — disqualifying for a mask before any argument about line count. Oklab (Ottosson 2020; W3C CSS Color 4 `oklab()`) fixes exactly that at Euclidean cost. ⚠ **Chromaticity only, because the identity is exact**: Oklab's nonlinearity is a pure cube root, so scaling the input by k scales L, a and b by k^⅓ and a/L, b/L are *invariant under exposure* — which is what makes the control well-defined on a scene-linear input that is unbounded above 1.0 and has no honest reference white. Lightness is §4b's band, composed with intersect. The target is stored as RGB and converted **in the kernel**, so the target and the pixel cannot disagree about what Oklab is |
| 73 | 2026-07-30 | **A spot is placed and adjusted by dragging, and both of its handles are on the canvas** | It was a click that placed a disc, chose the source for you and then showed neither. Healing is a judgement about *where the replacement comes from*, so the source has to be draggable; the automatic source survives as the starting position, because a click that immediately does something sensible is worth keeping as a first guess. ⚠ This needed `mask::fromFrame`, the inverse of the transform a spot is stored through — and the risk there is the **order**, not the algebra: the inverse must undo turns, straighten, crop in that sequence, and applying the forward order with negated angles is exactly equivalent whenever at most one of the three is active, which is every case anybody checks by hand. ⚠ Two hit-test orderings are load-bearing and both are tested: the **source** wins where the two discs overlap (they overlap at any useful radius, and the source is the handle with no other route to it), and **later spots beat earlier ones**, matching the draw order so the spot being looked at is not the one that cannot be adjusted. A move is labelled "Move spot" rather than "Spot" because `EditHistory` coalesces same-labelled entries, which merged a placement with a later move and made undo delete the spot |
| 74 | 2026-07-30 | **The brush erases, and the polarity belongs to the dab** | A component accumulates every stroke ever laid on it, so per-stroke polarity is not enough — the component has to remember which of *its dabs* added coverage and which took it away. It rides in the dab texture's third channel (RG32Float → RGBA32Float, 256 KB a component against 128). ⚠ **Erase is destination-out**, `a -= cov·a`, the exact inverse of paint's source-over `a += cov·(1−a)`: painting takes the alpha a fraction of the way to one and erasing takes it the same fraction of the way to zero, so the two are reversible against each other. Subtracting the coverage outright drives the alpha negative wherever a slow hand lingers, and the `saturate` at the end hides that as a hard hole in a soft brush. ⚠ **Migration is a parallel array, never a third interleaved number**: `brushStroke` is a flat list of floats in the sidecar, and re-interleaving it would read every stroke saved before erasing existed as garbage — silently, because a scrambled stroke is still a valid stroke. There is deliberately no separate *opacity*: Flow is per-dab buildup rather than a ceiling, a ceiling would need the kernel to track a per-stroke maximum, and Erase is the honest way back down |
| 75 | 2026-07-30 | **Per-layer adjustments will cover the pointwise set only; the pyramids stay global** | Asked for "every edit, mask by mask". Exposure, contrast, highlights/shadows, whites/blacks, vibrance, saturation, colour grading, the mixer and the curve are per-pixel functions — scaling them by coverage is well defined and one fused pass over baked per-layer coverages costs one pointwise pass. Clarity, dehaze and exposure fusion are 16–32-node pyramids each; a per-layer copy multiplies both the node count and the 6.4 GiB of cached intermediates that make an exposure drag 10 ms rather than 67, and it dies on arithmetic rather than on taste. The honest version of "clarity on a mask" is to render the pyramid **once** and let each layer blend toward the input by α × amount. Cap 8 layers at 46 MiB of baked coverage each ≈ 370 MiB. ⚠ **Refused: blend modes over rendered frames** — blending two tone-curved images is not the same operation as scaling a parameter and needs N framebuffers; layer opacity is a scalar into α, and the existing add/subtract/intersect are the mask blend modes |
| 76 | 2026-07-30 | **A mask may change any adjustment that is a function of the pixel alone — and Warmth is not white balance** | §2's rule is that the coverage scales the *parameter*, not the result, which is only well defined pointwise. Exposure, contrast, saturation and a colour cast pass that test and are now local. ⚠ **White balance cannot be**, and structurally so: temperature and tint are applied in `linearize`, at the head of the graph and **before the demosaic**, because the demosaic interpolates white-balanced data and the level a channel clips at moves with its multiplier — a local one means demosaicing the frame twice and choosing per pixel, which is a second pipeline rather than an adjustment. So the local controls are named **Warmth** and **Tint**, are a per-channel multiply renormalised on luminance so they move colour without moving exposure, and are deliberately not called Temperature and Tint. The contrast shares `develop_display`'s −2.5 log2 pivot; a second pivot would make a local contrast of zero visibly different from no local contrast near the crossover. ⚠ Taken **before** #75's stage 2 rather than after, because it defines what a layer *is* before multiplying layers, and carries none of the schema or graph risk |
| 77 | 2026-07-30 | **The adjustments are a catalogue, and each scope carries its own stage** | Every control was hand-listed where it was drawn — range, unit, decimals, default — which is fine until the same adjustment appears twice, and giving a mask more than exposure had just made the Local section a second copy of the Light section's list. `AdjustmentCatalogue` is the one definition and `AdjustmentGroup` renders any subset in any scope, so the global and local panels are the same specs through the same view. The binding table is a `switch` with no `default`, so an unbound id is a compile error rather than a slider that draws nothing — the way `lutStrength` once shipped dead. ⚠ **`stage` is per scope, not per adjustment**: global contrast runs in `develop_display` *after* the mask on the combined result while local contrast runs inside the mask node, so one stage per spec would make the ordering readout state one of them falsely. Found by writing the readout rather than by reading the code. ⚠ Refusals carry a **reason** and are generated from the table, so a control that stops being local-able says so the day it changes; a test fails if any refusal loses its reason, because a refusal without one is indistinguishable from a control that has gone missing |
| 78 | 2026-07-30 | **No sky matte from Apple, and no bundled segmentation weights — build a classical detector instead** | Checked rather than assumed: Vision offers people, subjects and saliency, and saliency is the wrong tool because sky is the *least* salient region in most frames. `AVSemanticSegmentationMatte` and the ImageIO auxiliary constants do include a sky matte, but they are read accessors for a matte the capturing iPhone embedded at shutter time — `AVCapturePhotoOutput` is the only producer and needs a live capture session, so an imported ARW will never carry one. ⚠ **Bundling a network is refused on the grounds already written for camera profiles (#44)**: sky appears in ADE20K, COCO-Stuff and Cityscapes, whose terms are respectively unclear, unclear and research-only, and weights inherit their training data's ambiguity however permissive the architecture's code is — no model is known whose architecture, weights *and* data all carry a clean redistribution grant, and it would add a Python/coremltools step for a solo C++/Swift maintainer. The answer is a classical gradient-energy sky detector (Shen & Wang 2013; Hoiem's geometric context before it) feeding the existing raster-matte path, ~80-90% right on daytime landscapes. ⚠ Its failure list — sunsets, water and glass reflections, white overcast against white buildings, night skies, sky through foliage — is acceptable **because a mask is a proposal inspected as an overlay and corrected with the brush**: the purple cast was dangerous because it was invisible, and visibly wrong and correctable is a different category. It must not claim anything beyond "smooth bright region connected to the top edge" |
| 79 | 2026-07-31 | **A raster matte is saved as a sibling 16-bit linear-grey PNG, referenced by a per-component id — and it is never regenerated on its own** | Every other mask kind is a handful of numbers in the sidecar; kind 4 is ~700k alpha values and was written nowhere, so a Subject, Person or Sky row reopened present and empty. ⚠ **Storage, not cache**: Vision's models move between macOS releases, so re-running on open would silently change a finished edit on an OS update — which also rules out an Application Support cache keyed by a path that dies the moment the photograph moves. Base64 in the XMP is refused because autosave rewrites that attribute 900 ms after any slider moves, and megabytes of text per settle is not a save path. So: `PHOTO.orion-matte-<uuid>.png` beside `PHOTO.xmp`, and what travels with a photograph is the raw, its sidecar and its mattes, all caught by a `PHOTO.*` copy. ⚠ **16-bit and the colour space is stated at both ends.** CoreGraphics colour-manages greyscale: write 0.5 through a Gamma-2.2 grey space and read it back as linear and it returns 0.735 — every feathered edge on every reopened photograph shifted, nothing crashing, nothing to see. That is the purple cast's shape exactly, and it is why the round-trip fixture is a **gradient ramp**: a binary matte survives a wrong colour space, a wrong bit depth and a wrong byte order, because 0 and 1 land on 0 and 1 however the curve between them is mangled. 8 bits was rejected because the case for it is "the guided filter probably averages the staircase away", and *probably* is not a thing to ship a mask on, for ~1.5× on data PNG compresses well anyway. A hand-rolled deflate blob was rejected as a bespoke container needing its own header, dimensions and versioning — PNG is self-describing, one ImageIO call each way, and openable in Preview the day a matte looks wrong. ⚠ **Files are immutable and the write precedes the reference**: regenerating mints a new id, so a half-written file can never clobber the only good copy, and the sidecar can never name a file that is not already on disk — the only reachable crash window leaves an orphan. Orphans are swept on open and **only after the sidecar has parsed**, because an unreadable sidecar yields no components and sweeping against that would delete every matte the photograph has. Deletion is never eager: a deleted component must be undoable without re-running a three-second inference. ⚠ A **missing file is reported, not swallowed** — leaving the row with no coverage changes the picture with nothing on screen saying why |
| 80 | 2026-07-31 | **Brush dabs are rejected a run of 64 at a time, by a host-built bounding box — and the rejection is provably bit-identical, not merely close** | The kernel looped every dab at every pixel: one texture fetch per dab per pixel, which is ~58 billion fetches for a 2400-dab stroke on a 24 Mpx frame and measured **2148 ms a render** against a 16 ms budget. ⚠ The cost is the **fetch**, not the arithmetic, so a faster inner test was never the answer — the fix has to remove fetches. Hierarchical bounding volumes, one level (Clark, CACM 1976; `research/brush-acceleration.md`): a box per run of 64 consecutive dabs in a 4 KB aux texture, skipping 64 fetches with one test. Measured 152→14.5 ms at 300 dabs and **2148→65.6 ms at 2400**. ⚠ **Runs of consecutive dabs, never a spatial partition**: paint is source-over and erase is destination-out and the two do not commute, so an index range preserves application order for free where a per-tile bin has to rebuild it with a stable scatter or a sort — three kernels and a prefix sum inside a static graph. ⚠ **The boxes are unexpanded and tested in the per-dab test's own expression shape**, because `fl(q·W) − fl(c·W)` is not `fl((q−c)·W)` — floating point does not distribute, so a box pre-grown by the nib radius would be a *different computation* and would disagree with the full loop on some pixel near some rim. Monotone rounding then gives the guarantee: when the block test fires, every dab in it takes the existing `continue`, which performs no floating-point operation on the accumulator, so the skip executes exactly the instruction stream the full loop would have. ⚠ The boxes are built from the **float32 texels actually uploaded**, never from the higher-precision values before the geometry transform, or the box can round tighter than what is stored and skip a real dab. ⚠ **A frame-filling scribble defeats it entirely** and returns to the old cost plus ~1.6%; that is not fixable with a tighter volume, only by not re-evaluating the whole stroke. Deliberately **not** done here: the incremental accumulator that would make painting O(1) in stroke length needs a persistent ~97 MB R32F texture per brush component and a host predicate deciding when the stroke's prefix is unchanged, whose failure mode is a stale accumulator rendering a completely plausible brushstroke — costed in `ROADMAP.md` rather than bolted on |
| 81 | 2026-07-31 | **Film grain is a precomputed correlated plate applied after the display transform, not noise added in scene-linear** | `research/film-grain.md`. Newson, Delon & Galerne (CGF 36(8), 2017) model the emulsion as a Boolean process of Poisson-distributed discs and that is the right physics — but their exact renderer is per-pixel Monte Carlo over the disc process, orders of magnitude outside 16 ms at 24 Mpx and several hundred lines against this project's 50–150 line node ceiling. Both hard constraints, broken at once. So: AV1's architecture (Norkin & Birkbeck, DCC 2018) — synthesise a correlated plate **once**, apply it per pixel with an intensity-dependent scale — carrying Newson's variance law `σ(Y) = amount·√(Y(1−Y))`, which is the Boolean model's own Bernoulli statistics and is zero at both ends, so `saturate` cannot truncate it into a mean shift. ⚠ **Monochrome**: luminance grain is what film looks like, per-channel grain is what a noisy sensor looks like. ⚠ **After the display transform, and the reason is not taste**: `develop_display` opens with `max(c, 0)`, which half-clamps zero-mean noise near black and lifts the mean, and AgX takes `log2` of its input, which turns small symmetric linear noise into large asymmetric shadow noise — and an amplitude defined against unbounded scene-linear EV would mean something different on every photograph and would move every time the exposure slider did. ⚠ **Keyed to the frame, not the output**, which comes free from running before geometry: a crop enlarges the grain exactly as enlarging more of a negative does. Two exports at different sizes therefore differ, and that is correct — the promise kept is the narrower true one, that the same edit at the same size renders the same bytes. ⚠ **Determinism is the dangerous part.** A hash of the pixel coordinate fails twice: the preview graph is a different grid, so preview and export would be different *realisations* rather than two resolutions of one field, and the preview would show full per-pixel variance where the settled render averages 16×16 — an order of magnitude grainier than the thing it previews. The plate plus an explicit `log2(gridStep/size)` level of detail makes both graphs sample one field. Also refused, each for a specific reason: hardware samplers (filtering precision unspecified across GPU families, so export could differ by device), `std::normal_distribution` and `generateMipmaps` (implementation-defined, so the plate would differ by toolchain), and grain applied after the dither. Controls are **Amount and Size only** — roughness is the plate's spectrum, which the fixed band-limit already encodes, and a chroma slider's correct value is always zero |
| 82 | 2026-07-31 | **The grain node disables to nothing at Amount 0, and the Bayer dither moves to whichever node writes the eight bits** | #81 costed grain as `develop:display` → `RGBA16Float` unconditionally plus one always-on node. Built that way it is a full-resolution pointwise pass, plus a doubled write on the node feeding it, on **every frame of every drag** — measured on `_PIC8220` as the exposure slider going from 3 nodes and 10.63 ms p95 to 4 nodes and **17.03 ms**, past the 16 ms M0 gate, and reported from the app as sliders getting slow before the bench was next run. Every other expensive feature in the graph already disables to nothing when it is off; grain is the same, and the only thing that made it different is that it is also the node that **quantises**. So the two facts move together in one place, `retargetOutputChain`: grain off → display writes `RGBA8Unorm` and dithers, grain on → display writes float and grain dithers, wide export → neither. ⚠ Deciding that in two places is how the narrow path ends up rounding twice, or not at all. The +194 MB #81 costed is now paid only while the slider is up; the resting cost is the idle node's own 93 MB. `testGrainWiring` pins that a tick costs the same as it did before grain existed, that Amount 0 is bit-identical to never having touched the slider, and that *something* still dithers |
| 83 | 2026-07-31 | **The brush cursor is round on screen, because the paint is round on the picture** | Reported live as "the circle is an OVAL". `CanvasLayout.brushCursor` built its outline as a circle in **normalized** coordinates and mapped it out, so on a 3:2 frame it drew 1.497× wider than tall — measured, and exactly the frame's aspect. ⚠ It was correct when written and its comment said why: the dab was measured in normalized coordinates too. Decision #62 folded `mask_brush.slang` into `mask_component.slang` and that fold moved the dab into **frame pixels** (`nibPx = brushRadius × min(shownW, shownH)`) so the brush would stop stretching under the Size slider. The paint went round; the outline over it did not. What kept it alive for two months is that the only thing tying the two together was a **comment naming a file that had been deleted** — nothing compiled against it and nothing tested it. `testBrushCursorIsRound` checks both roundness and that the radius equals `nibPx`, on landscape, portrait and square, at two zooms; reverting the shape fails 8 checks. ⚠ The **radial mask** is still an ellipse in normalized coordinates and is deliberately left that way for now — it has explicit per-axis semi-axes, so unlike the nib its shape is something the photographer sets, and changing the convention would change the meaning of `radius[1]` in every sidecar already written |
| 84 | 2026-08-01 | **Every gesture on the picture arms degrade-then-refine, exactly as every slider does** | Reported as "forced updates per stroke". `AdjustmentSlider` armed the preview graph through `AnalogTrack`; no canvas gesture ever did, so a slider tick rendered a quarter-linear preview while a brush stroke rendered the **full graph at full resolution** once per pointer event — on a photograph that gets more expensive with every dab already laid. Measured on `_PIC8220` with the new `paint` verb: 7.6 ms an event at 41 dabs and **27.3 ms at 246**, against **0.7 and 1.9** armed; and placing a radial mask carrying a local exposure, **13.0 ms a tick → 1.3**. The argument is the one already written on `AdjustmentSlider`: *every* slider arms it, not only the slow ones, because which controls are expensive is a property of the graph and it changes. The same holds for gestures. ⚠ **Arming happens after the mask hit test, not before**: a press that grabs no handle pans the picture, and arming first would swap the canvas to the preview texture for a gesture that is not an edit. ⚠ Four gestures — crop, spot, the curve editor, the grading wheels — are named and left for the performance audit rather than changed in the same breath: each swaps the canvas to a differently-sized texture mid-gesture with an overlay drawn over it, which is the shape of a bug this project has already shipped once, and the crop overlay's rectangle *is* the geometry being changed |
| 85 | 2026-08-01 | **`scenario paint` measures the stroke and refuses to arm the preview graph itself** | `brush` hands the engine one finished stroke in a single `setBrushStroke`; the canvas calls it again on every pointer event, appending. Nothing issued a stroke the second way, so the cost a photographer actually feels was unmeasured — this is piece 1 of ROADMAP's interaction-latency story. ⚠ The verb does **not** arm the preview graph, and that is the decision: a verb that armed it itself would report the same number whether `MaskOverlay` still called `beginInteraction` or not, which is a measurement that cannot observe the thing it exists to measure. Arming stays the `interact` verb's job so a scenario can time both sides. ⚠ What it does not cover is stated in the code and in the gap table rather than implied away: `Scenario` drives `Engine` and `CanvasLayout` and never a SwiftUI view, so **nothing asserts that a gesture arms** — the four unarmed ones were found by `grep`, not by a red test |
| 86 | 2026-08-01 | **Autorelease-pool discipline lives in `Resources.mm` and nowhere above it** | ARC is on for the engine's three `.mm` files, so strong members release correctly — but **ARC does not drain autorelease pools**, and nothing in this engine turns a run loop. Every autoreleased Metal temporary accumulated for the life of the process: measured at 393 B per texture descriptor, 1.6 KB per library load and 2.3 KB per kernel, which is **~0.64 MB per `DevelopPipeline` built** and so ~1.3 MB per photograph opened, since a photograph builds a full graph and a preview graph. ⚠ **`leaks` reports zero for this and is right to**: the blocks are still *reachable* from an undrained pool, so they are not leaks by its definition — only a footprint measurement finds it, which is why this survived a clean `leaks --atExit` on both the tests and the bench. ⚠ The shipping app was shielded **by accident**: `pushAndRender` runs on the main thread, whose run loop drains each cycle. The bench, the tests and the scenario runner are not, and moving a photo open to a background queue would have exposed it. Six `@autoreleasepool` blocks inside the Objective-C++ layer, which is where the file's own design says ObjC stays — no change to `Pipeline.cpp` or to Swift. Verified on the harness that found it: the same 15-iteration build/destroy loop went from **+8.92 MB, linear** to **+0.58 MB, flat from iteration 13** |
| 87 | 2026-08-01 | **A sidecar has three states, not two, and the sweep has to tell absent from unreadable** | `MatteStore.sweep` ran only inside the successful-parse branch of the loader, guarded by a comment that is correct about a sidecar which *exists and did not parse*: sweeping against that reads as "nothing is referenced" and would delete a photograph's selections. But a photograph that had **simply never been saved** fell into the same `else`, and a matte id lives only in a sidecar — so nothing on disk could ever reference its PNGs and they accumulated forever. Measured: **26 orphan mattes, 512 KB, beside one sample frame, the oldest three days old**, because pressing Subject mints a fresh UUID and writes a new file every time. The policy is now one function, `MatteStore.sweepAfterLoad` — parsed → keep what it references; **absent → keep nothing**; present-but-unreadable → keep everything. ⚠ It is a function rather than a rule repeated at each call site because it had already been written twice, in the app's loader and in the scenario runner's `reopen`, whose own comment claims it takes "the same steps `Editor.load` takes". Two copies of a delete policy is how one of them quietly stops matching. Tested where it lives, because `Scenario` cannot reach the no-sidecar case at all — its `reopen` needs a sidecar to reopen *with* |
| 88 | 2026-08-01 | **A stroke in flight lives outside anything observable, and is written to the document once when the hand stops** | Reported at **~155 ms a pointer event** in the app, against **0.9 ms** in the headless harness — a 170x nothing here could see, because `Scenario` drives `Engine` and never renders SwiftUI. `Engine` is `@Observable` and Observation is property-granular, so writing `maskComponents[i].brushStroke` per event invalidated every view that had read `maskComponents` — and `DevelopPanels` reads it in eleven places including a `ForEach` over the mask rows. Measured with `withObservationTracking` in the `paint` verb, which is a check that can fail: **41/80 events before, 0/80 after**. Everything the old path did per event was for the *record* of an unfinished stroke — re-flattening it with an array allocation per dab, then `onEdit?(state)` copying the whole `DevelopState` and handing it to `Autosave.note`, whose first act is a full structural compare. The dabs now go straight across the facade from `@ObservationIgnored` buffers, appended rather than rebuilt, and `maskComponents` is written exactly once in `endBrushStroke`. ⚠ The first draft also skipped `orion_engine_set_adjustments`, which looked free because dabs travel by their own call — but `brush_revision` rides in that block and is the only thing the engine compares to decide the mask node is stale. Without it the kernel never re-ran and the paint did not appear, at 0.0 ms an event and 45,000 fps, which is what "did no work" looks like when you are hoping for "fast". `repro/gesture-preview-agrees.txt` caught it |
| 89 | 2026-08-01 | **The codebase reads American; the on-disk keys stay as they were written** | Asked for directly. Renamed in two compile-checked stages — C++/Slang/C-header first (58 files, with `static_assert(offsetof(...))` as the tripwire, since the C++ struct and its Slang mirror bind by byte offset rather than by name), then Swift (28 files). ⚠ **Persisted keys are an interface, not prose, and are frozen.** `MaskComponentState`'s stored property names *are* the sidecar keys — its `Key` enum is deliberately named apart so the decoder can carry legacy spellings — and a renamed key does not fail to parse, it yields a perfectly valid mask sitting in the middle of the frame. `PresetGroup.colour`'s raw value is in presets.json, where `PresetStore.load` decodes the whole array or nothing, so one unknown value loses every saved preset at once. The three `legacy*` strings exist to read old files, and only two of the three fail loudly if swept. ⚠ The scenario grammar keeps **both** spellings permanently: renaming collapsed four existing alias pairs into duplicate cases and fourteen checked-in `repro/*.txt` failed at once. That grammar is an interface too — the session log has to stay runnable as a scenario. Americanising the keys is a schema migration with dual reads and a both-keys-present test, which is its own session with sign-off |
| 90 | 2026-08-01 | **A directory enumeration bounds its own temporaries** | Decision #86 put pools in the engine's Metal layer; this is the same defect one layer up, in Swift. `MatteStore.sweep` calls `contentsOfDirectory(at:)`, which mints one autoreleased `NSURL` per entry — each dragging an `NSPathStore2`, a CoreServices `_FileCache` and three `CFString`s, about **0.8 KB per file in the folder, per call**. Nothing released them, so they sat in whatever pool enclosed the caller. Measured on `reopen` × 300, paired against an `open` × 300 control on the same folder: **+165 KB per reopen beside 200 files, +2.5 KB beside 2** — a straight line in the file count and nothing to do with the photograph. `leaks` reports zero, correctly, exactly as it did for #86: the blocks are reachable from an undrained pool. The pool goes **inside `sweep`** rather than around the caller, because the caller that shows it is the scenario runner and the caller that matters next is a batch — the function that creates O(files) temporaries is the one that should contain them. After: **+1.5 KB per reopen at 200 files, against +1.3 for the `open` control**. `testSweepDoesNotHoardTheDirectory` runs 400 sweeps of a 300-file folder in one pool and fails over 8 MB; without the pool it grows 97.8 MB |
| 91 | 2026-08-01 | **The folder index is a two-stamp cache in Application Support, and every row is validated against the file it came from** | M1's `Epic: Cull` named a SQLite index and it was never built: every folder open re-opened every raw through LibRaw, re-extracted every embedded preview and re-read every XMP. Measured at **300 frames, page cache warm: 454–688 ms cold against 28–54 ms warm, 12.9–17.2×**, and an indexless open lands on the cold number, so the win is the index and not the disk. ⚠ **The danger is not a miss, it is a hit that is wrong** — a rating nobody set, or a thumbnail of a photograph that has since been replaced, either of which looks perfectly correct on screen. So: **two stamps per row, not one.** The raw's `(mtime, size)` validates the dimensions, the camera and the thumbnail; the *sidecar's* validates the rating, the reject flag and the label. A single stamp taken from the raw is the obvious design and it is wrong, because **rating a photograph does not touch the raw** — the row would report the first rating it ever read, forever. mtime is whole **nanoseconds** from one `fstatat`, never a `Date`: pressing 4 on a photograph rated 3 rewrites the sidecar to the same length in the same second, so second granularity is a hole you can drive a cull through. The thumbnail hangs off the raw and deliberately *not* the sidecar — it is the camera's embedded preview, which no develop setting has ever affected, and invalidating it on a star press would throw away a good thumbnail sixty times a session. ⚠ **Nothing is recorded that was not read back off disk**: `refreshMarks` stats, reads, and stats again, and files nothing when the two disagree, so the contents of an old file can never be filed under the identity of a new one. `Library.persist` calls it *after* writing rather than filing what it holds in memory, because a merge that failed would otherwise leave a rating in the index that is in no sidecar. **Thumbnails are blobs in the same database**, not a folder of files: eviction and payload then move in one transaction and the cache cannot leak files whose bookkeeping is gone — decision #87's orphaned mattes, already paid for once — and it is one file to delete when a disposable cache needs disposing (Kennedy, *35% Faster Than The Filesystem*, 2017; Sears/van Ingen/Gray, MSR-TR-2006-45). **Eviction is LRU against a 512 MB budget, down to 90% of it**, because culling is folder-shaped: you finish a shoot and leave it, so least-recently-used is the folder you are done with, where LFU keeps last month's favourite and insertion order evicts the start of the folder you are still in. `used` is only rewritten when an hour stale, so opening 500 frames is 500 reads and no writes. ⚠ **A schema change drops and rebuilds; there is no migration**, because a migration implies the data is worth keeping. ⚠ **Corruption degrades, never crashes and never deletes a sidecar**: `SQLITE_CORRUPT` and `SQLITE_NOTADB` alone — never `BUSY` or `LOCKED`, which mean another process holds the file — take the index out of service, and at open discard the database once and rebuild, refusing any path not named `*.sqlite3`. This does not relitigate #79, which refused an Application Support store keyed by a path "that dies the moment the photograph moves": it refused it for **storage**. For a cache a moved photograph is a key that misses, which costs one re-read and loses nothing. SQLite is used through `import SQLite3` from the macOS SDK rather than as a dependency — it is already on every Mac, it is public domain, and the alternative was a package for something the platform ships |
| 92 | 2026-08-01 | **A parameter block that no control reads is pushed once, not per tick** | `Pipeline::setParams` memcpys and calls `markDownstreamDirty` **without comparing the bytes**, so re-pushing an unchanged block costs exactly what changing it costs. Dehaze pushed all sixteen of its blocks whenever the slider moved, but only `dehaze:moments` reads the slider — the dark channel, the six rank passes and the candidate pooling are functions of the frame's size, the paper's constants and A. Nine nodes, six of them full resolution over 24 MP, redone per tick for a value none of them read: **0.69–0.71x** off the tick once latched, paired and interleaved. ⚠ This is the third instance of the same shape (lens vignette, the grain node at Amount 0, this), so the rule is now general and each instance gets a **named-node** invariant in `apps/bench` rather than a wall-clock threshold — this machine has measured one binary at 8.97 and 44.53 ms p95 within an hour, and node identity does not care |
| 93 | 2026-08-01 | **The bit depth a file will hold decides which graph renders it: eight is the narrow, dithered path** | Export forced `setWideOutput(true)` unconditionally, so every file Orion had ever written was sixteen bits per component — PNGs about twice the size they needed to be, with no control saying so. Offering 8-bit meant adding the narrow direction, and the narrow direction is not merely "the same pixels, rounded". `ops/dither_ops.slang` puts an ordered sub-LSB offset on whichever node writes the eight bits, and quantising a smooth sixteen-bit sky in CoreGraphics afterwards has nothing equivalent — that is the banding the dither exists to prevent. So `Engine::exportImage` picks the graph from `options.depth`, an 8-bit export is byte-for-byte what the screen shows, and `ImageWriter`'s quantisation is deliberately undithered because the graph already did it. ⚠ Two consequences that are not obvious. `readOutput16` had to learn to read `RGBA8Unorm` — it assumed half float, and reading a byte texture as half float gives noise, not a picture — and expands by **257**, not 256, so the writer's round-trip back to eight bits returns exactly the byte the graph produced. And the *panel* must send `effectiveDepth`, not `depth`: JPEG cannot hold sixteen bits, so leaving the control at 16 and switching to JPEG would render the undithered wide graph only for ImageIO to round it to eight with nothing left to break up the banding. ⚠ The **engine's** default stays sixteen while the **panel's** default is eight, and the split is deliberate: an unspecified export must keep what it was given (this path already shipped a resize that silently halved precision), while a file a photographer hands to someone else should be eight. `ORION_DEPTH_DEFAULT = 0` makes a zeroed options struct mean "unspecified" rather than "eight" |
| 94 | 2026-08-01 | **Output sharpening is a CPU pass in `ImageWriter`, after the resize, and its two amounts are in the register** | Fraser's multipass model — capture, creative, output — puts this pass at final output size, because resampling is what softened the image and sharpening before the resample is thrown away by it. The resize is CoreGraphics', in `ImageWriter`, so the develop graph is the one place this pass cannot go; it is ~60 lines beside the resize rather than a Slang node, and CLAUDE.md's "one node = one small shader" is not violated because it is not a node. Luminance only, Rec.709: the same delta goes to R, G and B so an edge gains contrast without shifting hue, which is what stops the control putting colored fringes on exactly the high-contrast edges it is aimed at. ⚠ **The placement is sourced and the numbers are not**, and that is stated rather than implied: Fraser & Schewe (2009) give the workflow and that print takes more than screen, but not the sigma and amount an implementation should use, so 0.6/0.40 and 1.0/0.80 are listed in `UNSOURCED.md` §2 as chosen. What is held instead is the invariant a photographer would notice — None does nothing at all, Screen overshoots a step edge, Print overshoots more, brightness does not move, and all three channels move together — so replacing the constants with measured ones needs no test rewritten. Default is None, because an export going back into an editor must not be sharpened and the panel cannot know which kind it is |
| 95 | 2026-08-01 | **"Strip location" removes the place *names* as well as the coordinates** | The GPS block was dropped and the IPTC dictionary was copied whole — so city, sub-location, province and country survived a policy whose entire purpose is that they should not. The sample frames have no GPS (no receiver in the body), which is why this held for as long as it did: the test's stand-in file carried coordinates and nothing else, so the hole was invisible to the one check aimed at it. The stand-in now carries a city and a sub-location too, and both directions are asserted. ⚠ A control that reads as honest and is not is worse than no control, and the landing page advertises this one by name |
| 96 | 2026-08-01 | **The last M3 item is not segmentation, and the name is why it sat unbuilt for three milestones** | `research/highlight-reconstruction.md`. ROADMAP said "segmentation-based highlight reconstruction", which reads as a connected-component pass — union-find over 24 Mpx behind a readback stall, or GPU label propagation whose **iteration count depends on the picture**, against a graph that is static. Both are the wrong shape and that is why nobody started. **Rouf, Lau & Heidrich (PROCAMS 2012) remove the requirement rather than satisfying it:** their §3.2 estimates a clipped region's color by `∇²ρ = 0 over Ω^∪` with the region's own rim as a Dirichlet condition, and **a Dirichlet solve is already region-scoped** — nothing crosses a pixel outside `Ω^∪`, so each connected blown region is solved on its own, from its own boundary, without ever being labelled. Labelling gives the same answer more slowly. Chosen over **Zhang & Brainard (JOSA A 2004)**, whose global channel-correlation prior fitted on a night frame is fitted on the dark warm background — the exact shape of the purple halo `kShoulder` exists to prevent, and Rouf et al.'s Figures 4 and 7 show it failing on a neon sign for that reason — and over **Guo et al. (CVPR 2010)**, which needs user intervention. ⚠ **The gap is real and measured, not assumed**: `highlights.slang` reaches 12 pixels and, under #29's common clip, its `count == 3` branch is a **literal identity**, so a fully blown core is untouched at any distance. On a 140 px blown disc the shipping node returns the core at R/B **1.000**, its input. ⚠ **Deliberately not wired.** Piece 1 is the solver — `hl_pull`/`hl_push` as Gortler et al.'s (SIGGRAPH 1996 §3.5.1) pull-push, with a Gauss-Seidel reference in `HighlightFill.h` so the approximation error is printed every run (**6.1% of rim span**) rather than hoped at. Pieces 2–6 are costed in ROADMAP at **+25 nodes and ~516 MB** for the pyramid alone; three or four sessions. ⚠ **When it is wired it reopens #29** — which clips every channel to one ceiling precisely so blown cores render neutral — and that argument must be written down then, not assumed: #29's magenta was the white-balance gains, evidence of nothing, while `ρ` is measured from the region's own rim |
| 97 | 2026-08-01 | **The cross-channel paper's third author is Tappen, and it had been Tang in five files** | S. Z. Masood, J. Zhu and **M. F. Tappen**, CGF 28(7):1861–1869, 2009. Verified against the reference list of Rouf et al. 2012 and the paper's own listing. The likely origin is He, Sun & **Tang**, cited correctly in five other places here for the guided filter and the dark channel prior — a plausible name, wrong, unchecked, and copied forward. ⚠ This is the failure `research/README.md` exists to catch, one level up from the purple cast: not a wrong constant but a citation that cannot be looked up, which makes every constant under it uncheckable. Found only because the new research file quoted the reference list of a paper that cites it. Worth the rule it vindicates — and worth noting that nothing in the test suite can ever catch this class, so the only defence is reading the source you claim to have read |
| 98 | 2026-08-01 | **A performance fixture that grows one dimension must grow the dimension the cost is in** | The brush probe grew a stroke by *subdividing a path of fixed extent* — the same sine wave, sixteen times as many samples. What `mask_component`'s kind 3 pays is `sum over blocks of (pixels in that block's box) x 64 fetches`, so that fixture multiplies the block count by sixteen and divides every box by sixteen: a constant, for any block size, any nib and any frame. It reported **29.00 ms at 60 dabs and 21.96 at 960** and was read as proof that the mask kernel is not what makes painting linear. It is not proof of anything — the probe held the product fixed by construction. Dab spacing is fixed by the nib, so **appending is the only way a real stroke grows**, and under an appended stroke the same node goes **2.65 -> 34.88 ms (full) and 0.17 -> 2.23 ms (preview) for 49 -> 294 dabs**: six times the dabs, thirteen times the cost, identical slope on both graphs. The bench now runs both shapes side by side, because the flat one is the evidence that decision #80 works and the rising one is the evidence that it is not enough. The host columns are printed beside them and are flat at 0.001 and 0.057 ms an event, which is how `orion_engine_set_brush_stroke` uploading the whole dab list per event was ruled out by measurement rather than by argument. ⚠ The general rule: a fixture that varies N must be checked for what it holds *constant* while doing so, and a flat result is a claim about the fixture until that check is done |
| 99 | 2026-08-01 | **A snapshot is the photograph's whole state in a sibling `PHOTO.orion-snapshots.json`, and the matte sweep's keep-set is the union of the sidecar's ids and every snapshot's** | M4's last workflow item. A snapshot is neither undo (fifty deep, coalescing, dies with the process) nor a preset (#74-era: a **patch** carried between photographs, deliberately excluding the crop, the dust and the masks) — it is this photograph's whole `DevelopState` under a name, so a photographer can save a look, keep working and come back. ⚠ **Not in the sidecar, and #79's reason does not transfer:** a state is a few kilobytes, not the megabytes a raster matte would be. Two reasons that do. Autosave rewrites the sidecar 900 ms after any slider moves through `Sidecar.merge`, a read-modify-write over a hand-rolled string matcher rather than an XML parser — every version kept would be decoded, re-encoded, re-escaped and rewritten on every settle, and the cost of a drag would grow with how many versions the photograph has. And **blast radius**: one bad merge would take the working edit *and* every version with it, when a snapshot's whole job is to be the copy that survives when the working state does not. So a sibling file, written on save, rename and delete and at no other time; what travels with a photograph is the raw, its sidecar, its mattes and now its versions, all caught by a `PHOTO.*` copy. Application Support was rejected on #79's grounds — a path-keyed cache dies when the photograph moves, and this is storage. ⚠ **The matte problem, which is the hard half.** A `DevelopState` is not the whole edit: a raster mask is a sibling PNG named by id (#79) and `MatteStore.sweep` deletes every matte the *sidecar* does not reference — so a version saved with a Subject mask, whose row is later deleted, restores a mask covering nothing the first time the photograph is reopened. Silently. The fix is a **pin, not a copy**, and it can be a pin only because matte files are already immutable (#79: regenerating mints a fresh id and a fresh file), so an id in a version always names the pixels it named when the version was taken. The keep-set is computed in `sweepAfterLoad` — the one function the delete policy lives in, per #87 — as the union of what the sidecar references and what every version does. ⚠ **And the version file has #87's three states too**: absent is `[]`, unreadable is `nil`, and unreadable means both *collect no mattes at all* and *write nothing over it*. What a pin cannot cover — a photograph copied without its siblings — is **reported before the version is pressed**: the row says which selections it can no longer find. ⚠ **Restoring keeps the working edit first**, as a single automatic version replaced each time, because undo alone is a trap: it is capped at fifty, coalesces, and dies with the process, while autosave overwrites the sidecar 900 ms after the restore lands. Renaming that automatic version promotes it. The engine also records one history entry, so ⌘Z takes a restore back in one step the way it takes back a preset. ⚠ **No eighth tool tab**: seven plates divide 364 points into 48 each, eight leaves 42, and VERSIONS needs about 51 at the bar's 9-point type. Rendered rather than argued — the bar came back reading `PRESE… VERSI…`, so the new tab cost the old one its name. Versions sit at the top of the Presets tab instead |
| 100 | 2026-08-01 | **Perspective correction is another matrix in the geometry node, not another node** | `research/perspective.md`. The correction is a plane projectivity — Hartley & Zisserman, *Multiple View Geometry in Computer Vision*, 2nd ed., CUP 2004: §2.3 for the 8 degrees of freedom, §4.1 for the Direct Linear Transformation, §4.1.2 for the inhomogeneous solve with h₃₃ = 1. Implemented from the description; **no GPL source consulted**, and in particular not darktable's `ashift` or RawTherapee's tool. Three controls — vertical, horizontal, aspect — reduce to four point correspondences on the frame's corners, and the DLT through them is an 8×8 solve run once per geometry change. ⚠ **It goes inside the pass that was already resampling.** `geometry.slang` composes the camera orientation, the quarter turns, the straighten and the crop into one coordinate transform for exactly this reason (decision #40); a perspective *node* would be the obvious build and would sample the picture twice, which is a triangle filter convolved with itself and high frequencies nothing gets back (Wolberg 1990 §7). Measured: the same transform split across two geometry passes comes out measurably softer, and a perspective tick runs **one node**, which `testPerspectiveWiring` and the bench probe both assert. ⚠ **A zeroed control is bit-identical to a build without the feature**, held by a flag in the parameter block rather than by a comparison in the shader, so the kernel takes the branch it took before and no baseline in any suite rebases silently. Three neutral short-circuits back it, and one of them is load-bearing at real frame sizes and not at the fixtures' — `T·I·T⁻¹` is the identity in exact arithmetic and not in float, and it fails at 6024×4024 while passing at 96×64, which is why the check names a real frame. ⚠ **Auto-scale is computed against the whole frame and knows nothing about the crop**, and that is what makes it compose: `constrainedCrop` already keeps the crop inside the turned frame, so if H maps the frame into the frame it maps anything already inside it into the frame too. Cheaper and more certain than `lens::autoScale`, which walks 64 points an edge — a homography takes lines to lines, so four corners bound the rectangle, and `fits` is an interval in the zoom, so bisection is exact rather than approximate. ⚠ **The same matrix bytes go to `mask::toFrame`**, so masks, brush dabs and spots follow the picture; a second derivation "in normalized coordinates" is how a mask ends up plausibly wrong. Centre and gradient direction are **exact** (H maps points, and a line's image direction is the Jacobian applied to its direction); *extent* is √\|det J\| at the centre, which is first order — bounded, measured at 0.28 of the frame under a 0.45 keystone, and costed in `ROADMAP.md` rather than implied away. ⚠ **Superseded by #107, and the numbers in this sentence did not reproduce.** Driven through the scenario, the 0.34 × 0.22 mask at vertical 0.45 recorded here as leaking 2 of 60 cells leaves 64 cells clear — on the build before #107's fix as well as after. A vertical keystone stretches *y* and that sweep varied *x*, so the reading was taken off the axis the error is not on. What the first-order extent actually cost was invisible to it: **aspect** is `diag(1/g, g)`, exactly linear and exactly area-preserving, so √|det J| was exactly 1 and a mask stayed round while the picture under it was squeezed two to one — **0.1461 luma**, an order past any keystone reading ever recorded here. ⚠ **No fourth Rotate control**: straighten is already a rotation in the same matrix, and a second angle is two controls that do one thing and disagree about the pivot |
| 101 | 2026-08-01 | **Split toning is not built. It is the grading wheels with fewer controls, and the vendor of the reference implementation retired it** | Asked for as an unbuilt M3 item beside the vignette; refused, with the argument here rather than in a commit message. Split toning is a hue and saturation for shadows, the same for highlights, and a Balance. Two of Orion's three wheels **are** those two tints, each already carries a luminance track split toning never had, and the third grades the midtones, which split toning cannot reach. Adobe reached the same conclusion and shipped it: **Camera Raw 13.0 / Lightroom Classic 10.0, October 2020, replaced the Split Toning panel with Color Grading**, and documents its Blending slider at 100 as giving "the same effect as the pre-existing Split Toning feature". So building it would mean adding the control the reference implementation retired six years ago, beside the control they retired it in favour of, which Orion already ships — two ways to do one thing, disagreeing at the edges, maintained forever. ⚠ **One thing split toning has that these wheels do not, and it is named rather than waved away: Balance.** Orion's zones are Gaussians fixed at −2.5 / 0 / +2.5 EV with σ = 1.6 and nothing moves them; Balance slides the crossover. That is ~5 lines in `color_grade.slang` (a signed EV offset on the three centres) plus one float through the twenty files an adjustment crosses, and it belongs **on the grading panel** beside the bands it moves — not in a second panel wearing an older name. Costed in `ROADMAP.md`, not built in the same session as the vignette: one story per session, and a half-threaded adjustment is the failure this file has already recorded twice. `research/color-grading.md` carries the long form |
| 102 | 2026-08-01 | **The incremental brush ships its predicate a session before its accumulator, and the predicate compares the uploaded texels bit for bit** | Painting appends dabs and every appended dab re-runs the loop over every dab laid so far, so `mask:0` costs `Σ over blocks of (pixels in that block's box) × 64` an event and the block count is what grows: **2.64 → 34.87 ms for 49 → 294 dabs** on the full graph, 0.17 → 2.23 on the preview, re-measured this session (#98, `research/brush-acceleration.md`). The fix is to keep the coverage and composite only the new dabs, and the question that has to be answered first is *how much of this stroke did I already draw*. ⚠ **That question has one wrong answer that is worse than being slow**: "the count did not shrink, so the prefix held". Undo three dabs, paint three different ones — the count returns to a value it has already had, the accumulator keeps coverage the photographer took back, and what renders is **a completely plausible brushstroke that is not theirs**. Every screenshot passes; every perceptual check passes. So `params::unchangedPrefix` walks the dabs and returns the length of the stable prefix, and `ROADMAP.md` splits the story in two so the predicate is built and attacked with nothing depending on it. ⚠ **It compares the post-transform texels — the floats actually uploaded** — because a crop, a straighten or a quarter turn moves every centre through `mask::toFrame` while the stored stroke and its revision sit untouched, and a predicate reading the displayed-coordinate dabs would call that prefix unchanged. Same reason `buildDabBounds` takes the texels (#80). ⚠ **`memcmp`, not `==`**: the claim is that identical bits give identical coverage, which needs no argument about `-0.0f == 0.0f` or `NaN != NaN`. The erase flag rides in the texel's `z` and is therefore inside the comparison for free, which it must be — paint is source-over, erase is destination-out, and **the two do not commute**. Identical centres are still not enough: one radius covers the whole stroke, so the nib, the flow, the hardness and the kind are compared as well and any of them moving returns 0. ⚠ **A predicate that answers 0 forever is correct and useless**, so the wiring test asserts the fast path was *taken* — 80 of 160 dabs already on the GPU after an append — and `BrushPrefixStat::evaluations` exists so that assertion cannot be satisfied by an answer left over from an earlier event, which is exactly what a forgotten `brushRevision` bump would leave behind. Six mutations, all red: returns `prev.count` when the count did not shrink (7 checks), compares only x and y (2), compares the pre-transform dabs (1), returns 0 always (9), ignores the nib/flow/hardness/kind (4), and asks only when the count grew (5) |
| 103 | 2026-08-01 | **The creative vignette is an exposure change in scene-linear light, shaped by the cos⁴ law, centred on the crop — and fused into the grading node** | `research/vignette.md`. M3 listed it and it was never built. ⚠ **The first thing it must not be is the lens correction**, which already exists and runs before the demosaic from lensfun's measured polynomial: that one takes a falloff *out*, this one puts one *in*, and a photograph can carry both. `testCreativeVignetteGpu` asserts in both directions that neither control switches the other's node on, because the mistake looks roughly right on screen. **The curve is published**: Reiss, *"The cos⁴ Law of Illumination"*, JOSA 35(4), 283–288 (1945), restated in Kingslake, *Optics in Photography* (SPIE 1992) p. 121, and `cos² = 1/(1+tan²)` turns it into `V(r) = 1/(1+(r·T)²)²` with no trigonometry in the kernel. It is a first-order model that real lenses depart from — which is a reason to refuse it for a *correction* and none at all for imitating the look. **Two controls, both physical**: Amount is the exposure change at the corner of the composition **in stops**, Field angle is the half-diagonal angle of view of the lens being imitated. Refused: Roundness (a lens's iso-illuminance contours are circles about the optical axis; a non-circular darkening is a radial mask, which Orion has), Feather (the curve is the feather and the field angle moves it), and Adobe's three styles (they exist to rescue highlights from a display-referred blend, and this is a multiply in scene-linear *before* AgX, so a bright corner rolls off instead of clipping). ⚠ **Scene-linear also for a mechanical reason**: the graph's tail is eight bits for the screen and the dither lives there (#38), so multiplying an already-dithered gradient by 0.25 in a corner scales the dither down with it and bands a smooth sky. ⚠ **After the grade, though a lens comes first physically** — the zones are Gaussians on luminance, so a vignette applied first would slide every corner pixel into the shadow band and change what all three wheels do, which is the interaction #41 removed. ⚠ **Post-crop, and the kernel never learns that**: `geometry` crops last, so `DevelopPipeline::compositionCircle` hands the shader three numbers — the rectangle's centre normalized in the unrotated frame and its half-diagonal in units of the frame's height. Only a circle, deliberately: a rotation cannot change a length, so the straighten and the quarter turns move the centre and leave the radius alone, and there is no second copy of `geometry.slang`'s inverse (#70). It falls out resolution-independent, so the 1/16 preview places it identically. **Fused into `color_grade.slang`** rather than given a node — pointwise, and every extra pointwise pass is ~194 MB at 24 MP, the same trade the creative LUT lost inside `develop_display.slang`. At Amount 0 the node disables to nothing and an exposure tick is 3 nodes and 8.97 ms p95, exactly what it was before; the mutation that leaves it always on takes the drag to 4 nodes and 12.81 ms and fails 4 checks |
| 104 | 2026-08-01 | **Balance is a rigid shift of all three grading zone centres, ±1.25 EV at full travel, and it is not a reason to run the grade node** | The remainder #101 named when it refused split toning: the zones are Gaussians fixed at −2.5 / 0 / +2.5 EV and nothing moved them. Reference behaviour is Adobe's Balance — Split Toning from Camera Raw 4 (2007), carried into the Color Grading panel that replaced it in Camera Raw 13.0 / Lightroom Classic 10.0, October 2020 — documented as balancing the effect between the highlight and shadow ranges, positive favouring the highlights. **Rigid shift, not a re-spacing:** a partition of unity translated along its own axis is still one, and the zones keep their 2.5 EV spacing so two centres can never collide; a re-spacing squeezes the two crossovers to 1.24 EV at full deflection, which the tests now fail on. **`kBalanceEv = 1.25` is chosen** — half the zone spacing, the largest travel where a crossover stays strictly between the two centres it separates — `UNSOURCED.md` §26. **Neutral costs nothing and rebases nothing:** the shift at 0 is `−0.0f` and `x + (−0.0f) == x` bit for bit, and with the wheels centred Balance scales weights that multiply an all-zero offset and a slope of one, so it neither enables the node (#82) nor dirties the block (#92). The bench asserts both directions — grade ran 0 times over 8 Balance ticks ungraded, 4 of 4 graded — and `repro/balance-survives-a-reopen.txt` covers the memberwise-initializer trap that lost film grain a session |
| 105 | 2026-08-01 | **The harmonic fill's pyramid is solved at a quarter resolution, and the number ROADMAP costed it at was the decision** | ROADMAP costed piece 3 at **+25 nodes and ~516 MB** — 13 levels at full resolution, level zero 194 MB at 24 MP — on a graph already at 149 nodes and 6971 MiB, and that number is what made it a three-session item. ⚠ **It was measured before it was spent, which is the only reason it is not true.** `rho` solves `∇²ρ = 0`; it is *harmonic*, so it has no detail to lose, and the only place it moves quickly is the rim, which the apply pass reads at full resolution regardless. `testHighlightFillGpu` now sweeps the factor against the Gauss-Seidel reference on every run and prints all five: **6.1% of rim span at 1/1, 6.3% at 1/2, 6.9% at 1/4, 8.7% at 1/8, 12.6% at 1/16**. A quarter costs 0.8 points on top of an approximation already worth 6.1 — less than the pyramid itself costs — for a sixteenth of the memory. Sixteen does not, and both ends are pinned by a check so the factor is a measured choice rather than a free one. ⚠ **What subsampling does *not* buy is node count**, which is logarithmic in the frame and barely moves: the chain is 24 nodes where ROADMAP said 25. The real cost is **173 nodes and 7186 MiB, +24 and +215 MiB** — and of that 215, the pyramid is **30 MiB** and the other **185 MiB is the apply node**, one full-resolution `RGBA16Float` pass that no factor can subsample away and that every pointwise node in this graph costs (decision #96 measured the same 194 MB for the vignette and fused it into the grade rather than pay it; there is no node here to fuse into, because the fill must land after `highlights` and before the denoise). Same factor as `kGuideScale` and `dehaze::kGuideScale`, and for the same reason: four is where the measured error stops being smaller than the approximation already carried |
| 106 | 2026-08-01 | **Wiring the fill reopens #29, and the difference is where the color came from** | #29 clips every channel to one common ceiling in `linearize`, before demosaic, *because* an unclipped blown light rendered magenta — and its whole effect is to make blown cores neutral. `hl_apply.slang` puts hue back into them, which is the change #29 forbade, so it is argued here rather than assumed. ⚠ **#29's magenta was the white-balance gains and was evidence of nothing.** A blown pixel arrives as (S, S, S); multiplying by (2.2, 1.0, 1.6) makes it magenta. That magenta is a property of the arithmetic, not of the light — it is the *same* magenta on every blown pixel of every frame whatever the lamp actually was, and nothing in the picture supports it. **`ρ` is measured**: it is the harmonic interpolant of the region's own boundary, and that boundary is real data — unclipped pixels, or ones `highlights.slang` recovered from cross-channel evidence. By the discrete maximum principle (`hl_push.slang` carries the proof) it cannot leave the range of the rim it read, so it cannot invent a color the region's own edge does not exhibit. **A neutral rim gives a neutral core** — #29's outcome, reached by evidence instead of by decree. ⚠ **#29 itself is untouched**: the clip still happens, still pre-demosaic, still before RCD interpolates across it. What changes is that a node five stages downstream may put a measured color back where the clip erased one, and #29's own row already said this was the trade it was making. ⚠ **The headroom stays gone and is not recoverable.** Clipping pre-demosaic means the fill works from clipped data: it recovers the hue and the shape the rim implies, never the sensor's counts. Worth having anyway because everything from the camera matrix to AgX is scene-linear and unbounded, so a restored highlight rolls off through the display transform instead of sitting flat. ⚠ **And it is off by default with a stated failure case** — Rouf et al. §4: a sunset, where hue is correlated with intensity and the correct core hue "is not observed anywhere in the image". Night frames with sodium lights are the good case. The control has to be able to be turned off, and `highlightRecovery` is that control for both halves of the feature |
| 107 | 2026-08-01 | **A mask's extent under the correction is the ellipse the Jacobian makes of it, in closed form — not one isotropic scale** | `research/perspective.md`, closing `UNSOURCED.md` §24's second item. Decision #100 shipped a radial mask's semi-axes as **√\|det J\| at the mask's centre**: the geometric mean of the two axis scales, right only where a homography happens to be conformal. The image of an ellipse under a linear map is an ellipse whose semi-axes are the map's **singular values** along its **left singular vectors** — Golub & Van Loan, *Matrix Computations*, 4th ed., JHU Press 2013, §2.4.1 — read off the symmetric 2×2 **S = B·Bᵀ** for **B = J·R(φ)·diag(aₓ, a_y)**, whose eigenproblem is closed form, their §8.5.2. Thirty lines in `mask::radiusToFrame`. ⚠ **The check that has teeth is the aspect squeeze, not the keystone.** Aspect is diag(1/g, g): exactly linear, so no curvature, and area-preserving, so **√\|det J\| is exactly 1** — the old code left a mask **round while the picture under it was squeezed two to one**, which is not an approximation going soft at the rim but the shape being dropped. Measured on `_PIC8220`: **4 of 84 clear cells leaked at 0.1461 luma**, an order more than any keystone reading ever recorded here, and every one of the 84 is bit-identical now. ⚠ **It is exact under the *derivative*, which is not exact under the map**, and the difference is measurable rather than rhetorical: the same fix buys about a fifth of a keystone's rim error (0.0214 → 0.0163 luma at vertical 0.45 on a 0.34 mask) and **all** of a squeeze's, which is what "first order removed, second order left" predicts. A mask larger than a third of the frame under a strong keystone still disagrees with the overlay at its rim, and that is now a statement about the map's curvature. ⚠ **The angle comes back as a delta on `Placement::angle`**, for two reasons that are both load-bearing: it keeps the quarter turns out of `radiusToFrame`, whose derivation is decision #83 and is pinned by `repro/mask-alignment.txt`; and it is **exactly zero** at a neutral control, so every uncorrected photograph in the library renders bit-for-bit as before. Neutral is short-circuited rather than solved, the same reason `persp::inTexels` short-circuits. ⚠ **A linear gradient's endpoints now read `placed.angle` explicitly**, because `m.angle` has stopped being the same line — the ramp's direction is the image of the mask's direction and is exact, while `m.angle` names the *ellipse's* principal axis. Its **length** is still isotropic √\|det J\| and is the one first-order term left in a mask's extent, in `UNSOURCED.md` §24 rather than implied away. ⚠ **Smith's closed form (CACM 1961) is in this repository and was not reused**: `SkyDetector.Stats.largestVariance()` is Swift, is the 3×3 case, and returns one eigenvalue and no eigenvector, while this needs both roots and an axis in C++ on the engine side of the facade. Smith reduces to the quadratic formula at n = 2, so what is written is that reduction. ⚠ **And the table `UNSOURCED.md` §24 printed was not reproducible.** It recorded 0.34 × 0.22 leaking 2 of 60 cells at 0.0105 luma; re-run through the scenario file on the build before the fix *and* the build after, that shape gives **64 clear cells and no leak**. A keystone's error grows with the mask's extent along the axis it stretches, and the old sweep varied the other one |
| 108 | 2026-08-01 | **The brush keeps its coverage in one accumulator belonging to whichever component is being painted, and the host's claim about what is in it is advanced from what the graph *ran*, never from what it was told** | Session two of #102. `mask_component.slang` kind 3 now continues from a persistent R32Float texture instead of re-laying the stroke, and the marginal cost of a pointer event stops depending on how much is already painted: `mask:0` alone, appending 49 dabs against re-laying the same stroke because one dab of its head moved — same dab count, same host work, same blocks and boxes, interleaved rep by rep in one process — is **4.66/9.19 ms against 7.65/14.53 at 49 dabs held and 5.20/5.80 against 36.46/46.87 at 294**, two runs. The left column is flat in what is already down and the right one is linear in it. ⚠ **One accumulator, for the live component, not one per component.** ROADMAP costed the second shape at ~97 MB each; measured, four is **393 MiB** against **98.25 MiB** for one (92.47 full + 5.78 preview, +1.37% on 7186 MiB). Painting is a single-component gesture, so the second shape's only purchase is the *first* event after the photographer moves to a different row — one out of a gesture's hundreds — and its price is paid by every photograph with four brush rows whether or not any of them is being painted. Registered at 1x1, grown on the first dab, and given back when no component is a brush. ⚠ **R32Float, because the invariant is bit-identity with a full evaluation** and only float32 round-trips float32 exactly; switching it to R16Float turns the ten-event comparison red and nothing else. Half-resolution accumulation is not available for the same reason — it would be a different computation, so the comparison the design rests on could not be made. ⚠ **`unchangedPrefix` returning N does not license skipping N dabs.** The texture holds `prev.count` dabs, not `prefix` of them, so continuing is only sound when the two are equal — *undo three, paint three different* leaves a prefix shorter than what is on the GPU and the three the photographer took back are still in it. ⚠ **And session one's `brushPrev_` answered a question this half does not ask.** It advanced when a stroke was *uploaded*, which is right for a predicate nobody reads; an accumulator is a texture, and a texture changes when the graph is **rendered**. `Engine::setAdjustments` applies to both graphs on every pointer event while only the preview renders, so the full graph is handed a hundred strokes and renders once — a claim recorded at push time would start the catch-up render 180 dabs into a texture holding none of them. It is now advanced from `Pipeline::lastRun()`, from what the graph reports having executed. ⚠ **A kernel that accumulates is not idempotent and nothing else in this graph is**, so running it twice on one parameter block lays the same dabs twice — a heavier stroke, in the right place, in the right shape. `apply` cannot see that coming: white balance moves and the reference behind every mask component changes, a matte is uploaded, `setEnabled` returns a hidden row. `reconcileBrushAccum` therefore runs **immediately before the render**, where nothing can intervene after it, and refuses the fast path to any node about to run on parameters this `apply` did not push — the alternative is proving that nothing in a 2,500-line file dirties that node, which the next edit breaks silently. Ten mutations. Six red on the first pass (2, 2, 2, 3, 1, 15); **three passed everything and were defects in the checks** — the accumulator changing hands *downward* (mask nodes run in component order, so 0→1 has the stale writer harmlessly overwritten and 1→0 does not), a refusal keyed on the host's record rather than the node's own `firstDab` (correct, and gives the feature back the first time an unrelated slider moves), and a clear-on-reallocation that could only fire when there was nothing to clear. A fourth survives with an argument: storing `saturate(own)` is the same number, because both composites are closed on [0,1] and correct rounding cannot carry a result past a bound the exact value respects. The raw store is kept because that argument is about the operations currently in the loop |
| 109 | 2026-08-01 | **Rouf et al.'s §3.3 ships as its model without its solve, and it costs no node — because the pixels it corrects are the ones that were setting piece 3's boundary condition** | `research/highlight-reconstruction.md` §5c, `UNSOURCED.md` §28. ⚠ **ROADMAP's cost for this piece was wrong in the word *reuse*.** It read "the pyramid can be reused ... a second solve is +23 nodes and +30 MiB", and that is what made piece 4 look like the cheaper of the two remaining pieces. Neither half survives checking. `hl_mask.slang` writes `Ω^∩` as the hole, so every pixel outside it and above the shoulder is *known* with `rgb = f` — which means **`ρ ≡ f` over `Ω^∪ \ Ω^∩`, §3.3's entire domain**, and `f*_k = (ρ_k/ρ_j)·f_j` evaluates to the identity. `hl_apply.slang`'s own comment already said so. And **pull-push solves Laplace, not Poisson**: §3.3's equation has a source term `∇·g*_k` and a pull-push interpolant has no residual and no relaxation to put it in; substituting `f* = r·f_j + u` gives `∇²u = f_j∇²r + ∇r·∇f_j`, whose neglected term is the size of the detail being transferred. So what ships is §3.3's **model** with the integration replaced by a per-pixel clamp, stated in `UNSOURCED.md` §28 rather than implied. ⚠ **The census came before the design, and it changed the answer.** `apps/bench` block 3e counts the clip sets on `highlights`' own two sides: the set §3.3 newly serves is **0.023%–0.068%** of a frame (5,608 and 16,578 px on `_PIC8220`/`_PIC8095`, zero on `_PIC8148`), which alone does not buy 23 nodes. What does is the next row — the ring that supplies `ρ` for **every** blown core is 11,901 and 20,563 px, of which **58% and 69%** comes back untouched by the window fit and 5.6%/22.7% is beyond its 12-pixel reach. Piece 2 was handing still-clipped pixels to the solver as Dirichlet data, so every core in the frame was solved from a rim that was itself wrong by a mean of 0.14–0.22 of the clip level. This is a correction to piece 3, not a new feature. ⚠ **The fix separates two sets piece 2 conflated** — which pixels the fill may *write* (unchanged: Masood et al.'s measurement wins wherever it exists) from which pixels are trustworthy *evidence*. The hole is now the part of `Ω^∪` the window fit did not recover, and "recovered" is read off the node as `rec_k > raw_k` rather than off a threshold, because demosaic ringing round a clipped edge puts pixels either side of any level constant. **Cost: 173 nodes and 7186 MiB, both unchanged** — `nRgb_` already existed and was already live, so asking for it costs a binding. ⚠ **It reopens #29 differently from #106 and owes a different argument.** #106's maximum principle does not cover this branch: `(ρ_k/ρ_j)·f_j` multiplies a rim-derived ratio by the pixel's own measured channel and *can* exceed the rim's range in level, which is the point. What is bounded is the **chromaticity** — `ρ` is a convex combination of known colors, so the ratio is one the rim exhibited and the level is evidence at that pixel. A neutral rim gives `ρ_k = ρ_j` hence `f*_k = f_j`, and under #29's own clip the domain is *empty* at a neutral white balance because equal gains put `Ω_R = Ω_G = Ω_B`. ⚠ **One mutation is green in `orion-tests` and red in the bench**: spelling the predicate as a level test passes all 750 engine checks and overwrites 13,135 pixels of a photograph, because the fixture's recovered ring is uniform and `ρ` equals the picture over it. The invariant lives in `apps/bench` block 3e for that reason |
| 110.1 | 2026-08-01 | **No stored property of `DevelopState` carries an inline default; the defaults live in an `init()` written in an extension** | A default on the property becomes a default on the *memberwise initializer*, and `Engine.state` builds one with the memberwise initializer — so a field could be left out of that call and the build said nothing. It shipped twice: film grain with Amount stuck at 0, and Grading Balance, both reaching the screen, reaching the sidecar as 0, and leaving every suite green. With the defaults moved, adding a field breaks `DevelopState.init()` **and** `Engine.state`, both naming it: `error: missing argument for parameter 'gradeBalance' in call`. ⚠ The extension matters — an initializer in the struct's own body suppresses the memberwise one, and then there is nothing left to check the field list against. ⚠ The hole that remains is a field added *with* a default anyway, which the compiler still cannot see; `testDevelopStateRoster` is the backstop, and it reflects rather than remembers |
| 110.2 | 2026-08-01 | **`Scenario` gains `wheel` and `dragwheel` beside the scalar control table, never instead of it** | The grading wheels write `[Float]` and `apply(control:value:)` takes one number, so the one gesture of the six that arms the preview graph with no measurement against it was the one nothing could drive. Both puck components move inside a single `edit`, because `ColorWheel.onChanged` does — two `set`s would be two ticks and two history entries, and would time a gesture nobody makes — and the path is clamped to the disc rather than the square that bounds it. ⚠ Additive under #89: `gradeShadowX`/`gradeShadowY` keep working. The number, 12 ticks on `_PIC8220`: **9.6 ms per tick unarmed against 1.2 armed, 8.0×**, settled picture identical at luma 0.2268 / sat 0.5136 |
| 110.3 | 2026-08-01 | **A canvas gesture's *arming* stays unverified, and the reason is that synthetic events do not drive SwiftUI** | `repro/gesture-preview-agrees.txt` compared an armed run against an unarmed one and demanded they agree — which is green when arming does nothing at all, so a gutted feature passed six checks. It now also asserts that arming *has an effect*: the preview graph renders only while `interacting`, so eight ticks of a 2.5-stop drag leave the preview surface at 0.2323/0.2918 unarmed and put it at 0.4814/0.2037 armed. ⚠ That closes the second link only. The first — a `DragGesture` closure calling `beginInteraction` — was **attempted and abandoned with a reason**: `NSHostingView` in an off-screen window lays the wheel out and hit-tests it, but `NSEvent.mouseEvent` dispatched through `NSApplication.sendEvent` never reaches the recognizer, because SwiftUI's macOS gestures want CGEvent-backed events, which want a real on-screen window and the real cursor. A suite that takes over the machine is not one that runs 39 scenarios in a loop. Deleting `ColorWheel`'s `beginInteraction` is **green across 744 engine, 3624 viewport and all 39 scenarios**, measured |
| 110 | 2026-08-01 | **Three Swift-layer gaps, and the memberwise initializer that had silently shipped two dead features** | ⚠ **This row was reconstructed on 2026-08-02 (#139) and is not the original.** No row was ever written — checked with `git log -S` over the whole history of this file, which finds none. It is rebuilt from the code that cites it, the session record in `HISTORY.md` and the tests, and it is marked so nobody mistakes a reconstruction for a contemporaneous note. Landed as **#110.1–.3** from one wave, which is why the parent number has no separate story. **.1 — `Engine.state` used the memberwise initializer**, so adding a field to `DevelopState` and forgetting the call compiled silently; it had shipped **film grain and then Balance dead**, each costing a `repro/` scenario written afterwards. No stored property carries an inline default any more, so an omitted field is `error: missing argument for parameter 'gradeBalance' in call` at `Engine.swift:1669` and `DevelopState.init()`. ⚠ A field added *with* a default still compiles — that is what `testDevelopStateRoster` is for, and its second half found `busyState()` had never moved **eight** of the fields it claimed to round-trip. **.2 — the grading wheel's arming was unmeasured**: `wheel` and `dragwheel` drive a three-component control, added beside the scalar spellings rather than replacing them (#89), measuring **9.6 ms a tick unarmed against 1.2 armed, 8.0×**, settled picture identical at luma 0.2268 / sat 0.5136. **.3 — "nothing asserts that a gesture *arms*"** narrowed to the first link only: `repro/gesture-preview-agrees.txt` compared an armed run against an unarmed one and demanded they agree, which is green when arming does nothing, and now also asserts arming has an effect (0.2323/0.2918 → 0.4814/0.2037 over eight ticks). ⚠ A `DragGesture` closure calling it stays unreachable and that was **attempted, not assumed**: `NSHostingView` off-screen lays the wheel out and hit-tests it, but `NSEvent.mouseEvent` through `NSApplication.sendEvent` never reaches the recognizer, and CGEvent-backed events need a real on-screen window and the real cursor. Saying so plainly is #110.3's shape |
| 111 | 2026-08-01 | **Core ML denoise is an on-demand pass, never a graph node and never a slider — and it is not started until the gap it closes has been measured** | `research/denoise-learned.md`, written for an M5 line that had been on the roadmap since the first milestone list and never investigated. **Nothing was built; the deliverable is the write-up and `ROADMAP.md`'s piece table.** Three findings, in the order they cost. ⚠ **The domain is wrong, and not the way the line assumed.** Orion's noise *fit* is pre-demosaic (`raw/NoiseProfile.cpp`, on the `BayerImage`) but its *filter* is post-demosaic and pre-matrix, in **linear camera RGB**, because `var = a·x + b` only holds there and the color matrix would mix the variances along with the channels. Published denoisers are trained either on sRGB — gamma-encoded, tone-mapped, 8-bit, which is what SIDD's 40.30 dB measures — or on the Bayer mosaic (Brooks, Mildenhall, Xue, Chen, Sharlet & Barron, *Unprocessing Images for Learned Raw Denoising*, arXiv:1811.11127, CVPR 2019, which is the paper that names this problem and reports 14–38% lower error for fixing it). Orion's insertion point is **neither**, and an sRGB checkpoint applied there would look plausible and be wrong for a reason invisible to inspection — the purple cast's exact shape. ⚠ **It cannot be a node, and the arithmetic is one line:** at 24 Mpx one fp16 32-channel activation is **1,480 MiB, precisely what the whole existing 8-node denoise chain costs**, because 32 channels × 2 bytes and 8 nodes × 8 bytes are both 64 B/px. A four-level width-32 U-net is ~2,868 MiB for one activation per level and a guessed 4–8 GiB in practice, against a graph already holding 7,186 MiB. It tiles or it does not run. And NAFNet's own 65 GMAC at 256×256 (Chen, Chu, Zhang & Sun, *Simple Baselines for Image Restoration*, ECCV 2022, Table 6) is **48.1 TFLOP** for one 24 Mpx frame — 1.6 s even at 40 TFLOP/s sustained, **100× M0's whole 16 ms budget**. ⚠ **The blocker was never the facade.** Core ML is Objective-C — verified in this machine's SDK headers, not remembered — so it is callable from the engine's existing `.mm` files without Swift and without crossing the POD boundary; it fails by `NSError`, so the wrapper returns a status code rather than letting an exception cross and terminate the process. **Licensing is the part that actually stops it**, and decision #78's rule transfers unchanged — weights inherit their training data's terms however permissive the architecture's code is: NAFNet and Restormer are MIT, **DnCNN and FFDNet have no licence file at all** (404 from GitHub's licence API — no grant, not a permissive one; `cszn/KAIR` is the MIT route to those two), **DND is explicitly non-commercial**, and SIDD's MIT claim is a single unverified read of a project page. ⚠ **The order is the decision.** Pieces 1 and 2 are measurements, piece 3 is a written decision that is allowed to conclude "stop here", and nothing is converted or trained before them — `research/highlight-reconstruction.md`'s estimate was 16× out because the measurement came after the cost, and this is the same trap one milestone later. Guesses are labelled as guesses in both files |
| 112 | 2026-08-02 | **The persisted keys are Americanised — a schema migration with dual reads, which is what #89 refused to do without sign-off** | ⚠ **This row was reconstructed on 2026-08-02 (#139) and is not the original.** No row was ever written — checked with `git log -S` over the whole history of this file, which finds none. It is rebuilt from the code that cites it, the session record in `HISTORY.md` and the tests, and it is marked so nobody mistakes a reconstruction for a contemporaneous note. #89 froze the on-disk keys while the codebase was Americanised, and named the terms: a schema migration with dual reads and a both-keys-present test, its own session, with sign-off. This is that session. **Seven mask-component keys** — `centreX`, `centreY`, `colourR/G/B/Tol/Soft` — plus top-level `denoiseColour` and `PresetGroup`'s `"colour"`. ⚠ **The reason it needed sign-off is that it cannot fail loudly.** A component's stored property names *are* its sidecar keys, since the encoder is synthesised from them, so renaming `centreX` does not throw, does not log and does not open the photograph unedited — it opens it with the mask at its default 0.5, 0.5: a valid radial mask in the middle of the frame, brightening the wrong thing, on every photograph anyone ever masked. ⚠ The `legacy*` case names are deliberately spelled **apart from their raw values**, so a future `Centre`→`Center` sweep over identifiers cannot quietly collapse the pair. **Read, never written**: there is no rewrite pass and no version stamp, so a sidecar adopts the American key the next time the photograph is edited and saved, and never otherwise — which is why the migration touches no file the photographer does not touch first. **The American key wins whenever both are present**, stated once for the whole sidecar rather than per field; a file carrying both cannot come from a new build then an old one, because an old build's synthesised encoder drops keys it does not know, so both together means a hand edit or a merge and the American key is the one this build wrote. ⚠ **`PresetGroup`'s legacy spelling is a `static let`, not a seventh case** — a case would join `allCases`, put a duplicate "Color" row in the save sheet, and let two groups that mean the same thing disagree inside one `Set`. ⚠ **And it exposed a defect on its own terms**: `PresetStore.load` was `try? JSONDecoder().decode([Preset].self)`, all or nothing, so one unreadable element left the photographer with the four built-ins and every saved look gone with nothing written anywhere to say why — and the group rename is exactly the change that would have triggered it, on every installation at once, on first launch. It decodes element-wise through `Failable` now. Element-wise `try? container.decode(...)` is the obvious alternative and is **wrong**: `UnkeyedDecodingContainer` does not promise to advance `currentIndex` on a throw, so the loop either spins forever or eats the element after the bad one depending on the Foundation build. ⚠ **`testAmericanKeyMigration` asks the shader's question, not the decoder's** — a legacy component and an American one must agree about `CanvasLayout.maskAlpha` at every point of a grid, and both must **disagree with a default component**, because a test that only proves the two agree would pass just as happily if both had quietly landed on the default |
| 113 | 2026-08-02 | **`DevelopPipeline.cpp` splits by graph region, and each piece carries the nodes it builds *and* the parameters it pushes** | 2,896 lines against a stated ceiling of 1,000, plus a 917-line header — the largest standing violation of a hard constraint and the only entry in the gap table anyone had costed. **The seam is not "construction vs. `apply`", which was the obvious cut and is the wrong one.** Splitting that way leaves `apply` at 1,298 lines, and it puts the two edits a new node needs in two different files: adding dehaze meant the constructor at line 400 and `apply` at line 1,628, and the cut that helps is the one that makes those adjacent. So the four regions of the graph each get a file holding both halves — `DevelopCapture.cpp` (715, sensor to profile), `DevelopLocal.cpp` (686, the multi-node operators), `DevelopMask.cpp` (757, the group and the brush), `DevelopOutput.cpp` (586, tone to the screen) — and `DevelopPipeline.cpp` drops to **452**: the two call lists, and little else. `Adjustments.h` (345) leaves the header, which is plain data the sidecar round-trips and the facade carries and which knew nothing about the graph; the header falls to 691. ⚠ **`ApplyContext` is the only new code.** Ten values one stage derives and another reads — the composed keystone, the crop and turns, `frameMoved`, `needsGuide` — because the stages are not independent and a *second* derivation of the perspective is exactly how a mask ends up a few percent off its subject. Every other line is verbatim motion, extracted by line range rather than retyped, with each stage opening on a preamble that aliases what it takes from the context so no moved line needed editing. ⚠ **Neither call list may be sorted.** `build`'s order is load-bearing — indices live in members, so a reorder compiles and changes which node feeds which, and mutation M6 swaps two `build` calls: red in all three checks. **`apply`'s order turned out to be load-bearing too, which the split had only guessed** — it was preserved on the cautious argument that "the stages write to distinct nodes so it probably does not matter" is a claim about 1,300 lines, and then M8 moved `applyTone` past `applyOutput` and **the rendered bytes changed**, on the geometry and crop-preview frames, with `orion-tests` and every bench invariant green. Why those two and not the other eight is **not diagnosed** and the guess written beside it is labelled as one. ⚠ **Not one pixel moved, and it is checked rather than asserted:** ten canvas renders, against a baseline built from `a15c7eb` — the commit before any of #113, because comparing the split against its own output proves nothing — spanning tone, white balance, the three local operators, grading and the vignette, spots, grain, four kinds of mask over two layers with feathering and a hidden row, and the whole geometry stack including the crop preview, all **byte-for-byte identical** by `cmp`. 173 nodes and 7186 MiB unchanged, every named invariant holding, 800 / 3655 / 39 green. ⚠ **The tenth render exists because of a defect in the ninth.** The mask shot added four rows and hid none, so every component was live — which is what `apply` does anyway with the enable loop deleted. Mutation M3 (`ctx.visibilityMoved` stuck false) was therefore **green across all nine renders** and red only in `orion-tests`: an oracle that could not fail on the one predicate governing mask enablement. One `maskhide` line fixes it and M3 now turns it red. ⚠ **Nine mutations; three are visible to the byte comparison alone** (M4, M8, M9), and M9 moves the intermediates figure 7186 → 7279 MiB — which the bench *prints* and does not assert. ⚠ **M4 found an existing check that cannot fail**: the bench's `endpointPair` turns the guided filter on with `shadows = 1e-6` and asserts the two runs agree, so hardwiring `needsGuide` false makes the "on" run the "off" run and `blacks -1, guide off/on` passes under exactly the mutation it looks built to catch. The whole six-node chain can be deleted from the product with 800 engine checks and every bench invariant green. Left alone rather than fixed, because a behaviour change hidden in a refactor is unreviewable. ⚠ **Four stages are unreachable from a scenario** — `highlightRecovery`, denoise, the lens correction and capture sharpening have no `set` name in `Scenario.swift`, so the byte-check is blind to them and they rest on `orion-tests`' GPU renders. Stated rather than hidden |
| 114 | 2026-08-02 | **X-Trans goes through LibRaw's own `xtrans_interpolate`, not a Slang port of Markesteijn — and it does not start until Markesteijn's wall clock and the white-balance question are answered** | `research/demosaic-xtrans.md`, written for an M5 line that has been on the roadmap since the first milestone list and had never been investigated. **Nothing was built; the deliverable is the write-up and `ROADMAP.md`'s seven-piece table.** ⚠ **The licensing premise the line was framed on is half right and half beside the point.** The right half: **there is no published description of Markesteijn's algorithm anywhere.** Checked dcraw (code with an attribution and no derivation), darktable's `xtrans.c` and RawTherapee's `xtransdemosaic.cc` (**GPL-3**), both projects' manuals (which say *which variant to pick*, not how either works), Fujifilm's patents (they cover the **array** — the 6×6 layout and its aperiodicity — not the interpolation, and Markesteijn is an independent contributor, not Fujifilm), and `xtransdemosaicking.blogspot.com` (the Markesteijn+FDC work that shipped in darktable 2.4.0, which calls Markesteijn as a black box). **The only extant description is source code and the accessible copies are GPL**, so re-typing one in Slang is copying rather than implementing from a description, and `CLAUDE.md` forbids it. That route is closed. ⚠ **The beside-the-point half: the same code also ships under LGPL-2.1 / CDDL-1.0.** `xtrans_interpolate(int)` is at `libraw.h:451` in LibRaw 0.22.2 — read out of this machine's installed headers, not remembered — dual-licensed by the distributed `COPYRIGHT`, and credited to *"Frank Markesteijn's algorithm"* in its own source header. The **GPL** interpolators (AMaZE, LMMSE, AFD) are in the separate, abandoned `LibRaw-demosaic-pack-GPL2/GPL3` repositories that Homebrew does not build; the X-Trans one is in the LGPL/CDDL core because it arrived through dcraw's non-RESTRICTED code. Orion already links `libraw_r` dynamically, so **nothing about the licence model changes**, and `dcraw_process()` is public with `user_qual > 2` selecting 3-pass. ⚠ **What actually makes this expensive is decision #29, not the licence.** #29 clips all three channels to a common ceiling *after* white balance and *before* demosaic, and `DevelopPipeline.cpp:1269` says so in a comment written for Bayer: moving temperature recomputes the demosaic, *"That is inherent"*. **A demosaic that leaves the GPU therefore takes the temperature slider with it**, and the four ways out are all costly — clipping post-demosaic reopens #29 and loses, a GPU preview demosaic is circular (that is the thing which does not exist licence-clean), and rescaling gains by a ratio is unsound because Markesteijn's direction decisions are not linear (`UNSOURCED.md` §29). ⚠ **Two findings that were not expected.** The GPU graph gets **smaller** — removing `linearize` and the four `rcd:*` nodes and uploading a demosaiced `RGBA16Float` source is **−5 nodes and −445.6 MiB** at 26 MP, so an X-Trans frame would run 168 nodes against a Bayer frame's 173, which is a load-immune number a bench probe can assert by name. And a 40 MP X-Trans body (X-H2 / X-T5, 7728×5152) puts the **existing** 7,186 MiB graph at **~11,800 MiB** before any of this, which is a constraint arriving with the first Fuji file whatever the demosaic is. **The published alternative is real and is not a substitute:** Rafinazari & Dubois, ICIP 2014, and Rafi Nazari's open-access uOttawa thesis (2017, ch. 3) — **opened and read**, per the rule that came out of the Tang/Tappen correction — give a frequency-domain luma-chroma method at 36.50 dB against Bayer LSLCD's 39.8 on **simulated, gamma-corrected, white-balanced** Kodak mosaics, which is not Orion's domain; and the thesis opens by naming the literature gap itself. ⚠ **It has never been compared to Markesteijn by anyone.** **The order is the decision:** pieces 2 and 3 are a measurement and a written argument, either may conclude "stop", nothing is wired before them, and **none of it starts before `DevelopPipeline.cpp` is split** — it is 2,549 lines against a ceiling of 1,000 and this feature branches the head of that graph. **Declining X-Trans, with the reason written down, is a first-class outcome** and is cheaper than a half-threaded one |
| 115 | 2026-08-02 | **The silent-failure sweep — four fixes landed, the agent was killed mid-run, and the sweep was finished by hand** | ⚠ **This row was reconstructed on 2026-08-02 (#139) and is not the original.** No row was ever written — checked with `git log -S` over the whole history of this file, which finds none. It is rebuilt from the code that cites it, the session record in `HISTORY.md` and the tests, and it is marked so nobody mistakes a reconstruction for a contemporaneous note. An inventory of every `app/` write path that could fail without saying so. ⚠ **The agent running it was killed by a session limit before it reached the sweep**, having landed four fixes; the inventory was completed inline afterwards, since it needs a grep and a judgement rather than an agent. **The data-loss one**: `Engine.restore` on an undecodable blob. Also the index's thumbnails hitting because they were keyed by `path` alone, matte collection beside the photograph, and the reopen-refusal path — `reopenrefused` asserts the failure is *set*. ⚠ **The finding is mostly a negative one and that is the part worth recording**: after the fixes the write paths are in good shape, and manufacturing more fixes to look busy would have been the wrong answer. What was **left, and argued in place**: `Snapshots.commit` on restore is best effort by design and the *guard* is what is best effort rather than the restore — refusing to restore over an unwritable version list would punish the photographer for a disk problem; `MatteStore.sweep`'s delete writes the failure to stderr and keeps going, because collecting is housekeeping; `MatteStore.write` and `Snapshots.save` both `throws`, so nothing is swallowed. ⚠ One check `#115` touched **could not fail until it landed** — see `ViewportTests+Preset.swift:455` |
| 116 | 2026-08-02 | **The M0 gate reads the best of three rounds, not the last one, and prints every round** | The gate fed the bench's exit code off a single 60-frame sweep's p95 against 16 ms, and on this machine that is a coin toss rather than a measurement: one unchanged binary has given p95 **8.87 and 31.45 ms within the hour**, and on 2026-08-02 it failed three times running at **31.70, 22.64 and 16.52** while agents compiled in the background, then passed at **9.01** the moment the machine went quiet. ⚠ **A gate that fails for reasons the diff cannot cause is worse than no gate** — it gets read as noise, and then it is not read at all, which is how a real regression would walk through it. **Contention can only ever add time**, so across repetitions the smallest p95 is the least contaminated estimate of what the build costs, and a real regression raises the *floor* (it is in every round) while load raises the *tail* of some. Every round's p95 is printed with the spread, so a genuinely slow build cannot hide behind one lucky round and a thrashing machine is visible rather than mysterious. ⚠ **Proven still able to fail**: the threshold mutated to 5 ms gives FAIL and exit 1, restored gives PASS and exit 0 — an anti-vacuity check on a change whose whole purpose is to make a gate fail *less*. ⚠ **It narrows the sensitivity, it does not remove it**: one run still failed while three agents were compiling and three benches ran back to back, and could not be reproduced on a quiet machine, where the three rounds came in at 9.74 / 9.75 / 9.71 with a spread of 0.04 ms. **Node counts remain the load-bearing numbers**; per decision #92 each perfor mance invariant is a named-node assertion rather than a wall-clock threshold |
| 117 | 2026-08-02 | **`Engine.swift` splits by region of the problem, and every stored property stays in the class body** | 2,331 lines against a stated ceiling of 1,000 — the largest standing violation once `DevelopPipeline.cpp` left it (#113), and the sibling of that story. The seam is #113's and for #113's reason: **by region, not by construction-vs-use**, so both halves of a change sit together and *which* file is answered by what the feature is. Seven extensions — `Engine+Geometry.swift` (136, crop/straighten/rotation), `Engine+Spots.swift` (146), `Engine+Mask.swift` (460, the group and its local adjustments), `Engine+Brush.swift` (249, mask kind 3 and its live-stroke path), `Engine+Document.swift` (244, open/restore/presets/versions/LUT/export), `Engine+Render.swift` (297, textures/push/render/readbacks), `Engine+Compare.swift` (145) — and `Engine.swift` falls to **795**. ⚠ **The Swift constraint decides the seam and has no C++ equivalent.** `Engine` is `@Observable`, and the macro only sees declarations in the class body: a `var` moved into an `extension Engine` is not a compile error in general — it is a compile error only because *stored* properties are banned from extensions outright, which is what mutation M1 confirms (`error: extensions must not contain stored properties`). So every stored property stayed, and `Engine.swift` is therefore **the values and the three lists that enumerate them** — the properties, `state`, `assign` and `cAdjustments`. That is deliberate, not residue: those three are exactly the edits a new adjustment needs, and splitting them would have repeated #113's rejected seam in a new place. Adding a slider is now one file. ⚠ **What it cost, stated rather than glossed: Swift's `private` is file-scoped, so encapsulation was traded for the ceiling.** Fifteen `private` members widened to internal and **eighteen `private(set)` properties lost their compiler-enforced "only `Engine` writes this"** — `isLoaded`, `maskComponents`, `generation` and the rest now rest on convention within the app target. `OriginalGeometry` also had to leave `private`, because the stored `originalGeometry` naming it cannot move out of the class body. There is no way to split a Swift type across files without paying this; it is recorded in the header of `Engine.swift` so the next reader does not think it was carelessness. ⚠ **Verbatim motion, asserted rather than trusted.** Extracted by line range with a script that proves every one of the 2,331 lines is claimed by exactly one destination — nothing retyped, nothing lost, nothing duplicated. The only hand edits afterwards were the access widening above and collapsing the blank runs the extraction left. ⚠ **Not one pixel moved, and it is checked:** 27 canvas renders spanning tone, the grading wheels, grain and the vignette, the whole geometry stack including the crop preview, four mask kinds over two layers with hiding and reordering, a painted and erased brush stroke through the pointer-event path, spots, colour-range picking, the Vision matte and auto-enhance — all **byte-for-byte identical** by `cmp` against a baseline built from `ab6f9b2`, the commit before any of this. The oracle was first checked against itself: two full runs of the pre-split binary agree 27/27, so a difference is a difference. 800 / 3702 / 40 green and the bench exits 0. ⚠ **#110.1's trap still bites, and both halves were checked rather than assumed**: adding a field to `DevelopState` fails the build twice over, once in `DevelopState.init()` and independently once in `Engine.state` — the second is the half a bad split would have broken, and it was confirmed by satisfying the first and watching `Engine.swift:419` still refuse. ⚠ **Two of the seven mutations were green on everything, and both were defects in the checks rather than passes.** M3 (delete `pushStrokes()` from `assign`) was invisible because no scenario painted and then reassigned the state; M5 (`constrainCrop` a no-op) was invisible because the geometry scenario cropped *after* straightening, and `setCrop` runs the same clamp itself, so the constraint had nothing left to do. Both scenarios were rewritten — paint-then-undo-then-reorder, and crop-then-turn — the baseline was re-taken from `ab6f9b2` rather than from the new build, and both mutations now turn the byte comparison red. **The 40 repro scenarios stayed green through both**, so these were gaps in the repository's coverage, not only in this session's. ⚠ **The byte comparison is blind to compare.** `shot` writes `Screenshot.developed`, which reads the output texture; the split composite lives in `CanvasBlit` and never reaches a PNG. M2 (cut `render()`'s call to `refreshOriginal()`) is therefore green on all 27 frames and red only on `repro/geometry-while-comparing.txt` and `repro/rotate-then-compare.txt`. The compare region rests on those two files. ⚠ **And one check that does not exist anywhere.** M7 deletes both `lastFailure = nil` clears from the success path — so the footer would keep showing a stale failure forever — and **every check stays green**: 27 frames, 40 scenarios, 800, 3702, bench 0. `lastFailure` landed the same day and has no scenario name, no `control` getter and no test. Left alone rather than fixed, because a behaviour change hidden in a refactor is unreviewable — it is written down here instead. ⚠ **Also blind, as in #113:** the curve, denoise, sharpening, highlight recovery, the lens corrections and the creative LUT have no `set` name in `Scenario.swift`, so no render here can see them; they rest on `orion-tests` |
| 118 | 2026-08-02 | **`apps/bench` is one file per section of the report it prints, and a check lives with the feature it pins** | 2,289 lines in one `main` against a stated ceiling of 1,000, ~700 of them added in a single day as feature after feature bolted a probe onto the end. The seam is not "helpers vs. `main`" — that leaves the probe table and the invariants in the same 1,700-line function, which is where the problem actually is. **The seam is the report**: `bench_gate.cpp` (179) the M0 gate, the wide tail and the curve drag, `bench_controls.cpp` (669) the probe table, `bench_invariants.cpp` (275) the guide chain, lens, dehaze, Balance and screen-vs-export, `bench_highlights.cpp` (424) the fill's four invariants *and* the clip-set census, `bench_brush.cpp` (386) the accumulation invariant *and* the stroke profiles, `bench_compose.cpp` (188), `bench_profile.cpp` (152), `bench_metrics.cpp` (147) the instruments, `bench.h` (126) — and `main.cpp` falls to **85**: decode, device, pipeline, thirteen calls and the exit code. **The rule is stated on `bench.h` and is what makes the next edit obvious**: a new control gets a row in `bench_controls.cpp`, a new node gets an invalidation check in `bench_invariants.cpp`, and a check belonging to a feature big enough to own a file lives *there* rather than in the general home — which is why the fill's invariants sit beside the census and the brush's beside its profiles. ⚠ The numbered blocks **3b, 3c, 3d and 3e** — numbers assigned so concurrent agents would not collide in one file — are gone; they were never names, and files are. ⚠ **`Bench::cleanNodes` is the only value that crosses a section boundary** and it is deliberately not re-derived: the exposure gate measures how many nodes a tick recomputes, and `exposure drag, lens on`, `exposure drag, fill off` and `exposure drag, fill on` are all stated against it, so a second derivation would turn three checks into comparisons of a number with itself. ⚠ **Verbatim motion, checked rather than claimed**: every body is a line-range slice of the original dedented as a block, each section opens on a preamble aliasing what it takes from the context so no moved line needed editing, and a multiset diff of every non-blank line accounts for all seven dropped lines (two `namespace` markers, two block braces, two includes that moved, and the return statement). ⚠ **The named check set is identical on all three sample frames** — 54 / 55 / 54 checks, same names, same verdicts, same node counts, and the same rendered means on the guide and screen-vs-export lines. The whole report differs only in wall-clock, one field width, the output prefix and tie-breaks in the hottest-N rankings. ⚠ **Twelve mutations, and three of them found checks that cannot fail.** Nine reddened what they were aimed at, including all four the brief named (`perspective 0.6` 1 node → NO EFFECT, `balance with no grade`, `highlights off` 0 → 24 fill nodes, `dehaze drag` 10/0 → 19/9) and the M0 threshold itself. The three that stayed green are the finding: **`needsGuide` hardwired false still prints both `guide off/on` lines `ok`** — #113's known dead check, confirmed still dead and deliberately not fixed here; **a dither 40× too large leaves `screen vs export path` green** at 0.00313 against its 1/255 bound, because an ordered dither is zero-mean and the check compares means, so it sees a *biased* dither (red at +0.00738) and barely sees a wrong-magnitude one; and **`auto_enhance::refine` can be gutted with `converged` still green on `_PIC8220`**, the frame the gate command names, because the look's fusion and clarity land the median inside `kSettled` before the solver is consulted once — it is red on `_PIC8095`. All three are written down and none is fixed, because a behaviour change hidden inside a refactor is unreviewable |
| 119 | 2026-08-02 | **A control probe may assert a node floor, because a control can move the picture correctly with a whole chain under it dead** | `highlights` and `shadows` drive a **seven-node guided filter**. Hardwire `needsGuide` false where it is derived and the chain dies, those two fall from **10 nodes to 3**, the picture still moves by roughly the right amount — the tone curve alone does that — and **the whole bench exits 0**. ⚠ The invariant written specifically to catch this is blind to it: `endpointPair` renders the chain-off frame, then renders it again with `shadows = 1e-6` to switch the chain on, and asserts the two agree. With the chain dead the second render *is* the first, and two identical frames agree perfectly — the delta even **improves to +0.00000**. Found by mutation in #113's split, confirmed by #118's, reproduced and fixed here. **The fix is a `leastNodes` floor on the probe row**, printed on every line like the movement floor beside it, because a threshold nobody can see is a threshold nobody maintains. Ten holds on all three sample frames; `whites` and `blacks` sit at three, so the gap is exactly the chain. The mutation now prints `CHAIN MISSING` and exits 1. ⚠ **A first attempt was reverted and is recorded rather than hidden.** It counted `guide:` nodes inside `endpointPair` and asserted the count rose between the two runs. That measures **recomputation, not enablement** — a cached node reports `executed = false` — so hardwiring `needsGuide` *true* left the count at `0->6` and the new branch passed. It was a check whose failure path could not be demonstrated, which is the defect it was meant to fix, so it was dropped rather than shipped |
| 120 | 2026-08-02 | **`Scenario.swift` splits by what a verb *drives*, and each family owns a switch that answers whether it took the verb** | 1,615 lines against a stated ceiling of 1,000, and **977 of them were one `switch`** — the last of the three violations in `app/`. ⚠ **`Scenario` is not ordinary code, it is an interface**: decision #89 records that renaming its verbs collapsed four alias pairs into duplicate cases and failed fourteen checked-in `repro/*.txt` at once, and the session log the app writes is itself a runnable scenario. So this is a **pure move** — nothing added, nothing renamed, nothing removed. The seam is #113's and #117's, for their reason: **by region of the problem, not helpers-vs-switch**. The obvious cut — lift `apply`, `eyedrop`, `maskCheck`, `check` out and leave the switch standing — moves 350 lines and leaves "where does a new verb go" answered by "somewhere in the middle of the largest function in the app". Four families: `Scenario+Frame.swift` (306, the photograph, geometry, history, sidecars), `Scenario+Controls.swift` (362, sliders, wheels, drags, eyedropper — **and both control tables**, so a new slider is one file), `Scenario+Mask.swift` (546, rows, brush, rasters, pickers, spots, overlay), `Scenario+Report.swift` (220, measurement, assertion, instruments, files written and read back) — and `Scenario.swift` falls to **301**: the grammar comment, the run loop, the shared state, and a dispatcher that asks each family in turn. ⚠ **Verbatim motion, proved rather than claimed.** Extracted by line range with a script that asserts every one of the 1,616 lines is claimed by exactly one destination, and the **multiset of all 158 `case` labels is identical before and after** — which is the check that matters here, because it is what says no alias pair merged and no verb was lost. **Exactly two lines of a verb body changed shape**: `point` and `number` were closures over `step`'s `args`, so `number` is now `number(args, i)` at 45 call sites and nothing else moved. ⚠ **Swift's `private` is file-scoped, so encapsulation was traded for the ceiling again** — ten members widened to internal (`Reading`, `readings`, `failures`, `checks`, `quiet`, `say`, `Bad`, `photo`, `snapshots`, `stashedDevelop`) plus `step` and `read`; everything used by one file alone (`apply`, `controlValue`, `setWheel`, `clampToDisc`, `eyedrop`, `persistMatte`, `maskCheck`, `check`, `fail`) **stayed private**, and mutation M6 confirms the widening is load-bearing rather than blanket (`error: 'step' is inaccessible due to 'private' protection level`). ⚠ **Nothing moved, and it is checked twice over.** All 40 `repro/*.txt` produce **byte-identical output** — not exit codes, the full measured text — once the timing lines are normalised, and **51 rendered artefacts** (48 canvas PNGs, two JPEG exports, a 16-bit TIFF) are byte-identical by `cmp` against a baseline built from `9d9158d`, the commit before this work. ⚠ **The oracle was self-checked first**: two runs of the *pre-split* binary agree on all 40 scenarios and all 51 frames, so a difference would be a difference. 800 / 3702 / 40 green, bench exits 0. ⚠ **The four families claim disjoint verbs and nothing at runtime enforces it** — a verb answered by two would be taken by whichever is asked first, silently, which is #89's failure shape in a new place. It rests on the case-label multiset check, and that is stated rather than implied. ⚠ **The big finding is a defect in the repository's oracle, not in this split, and it is left alone rather than fixed.** Mutation M4 makes `frameStep` claim every verb, so three quarters of the vocabulary silently does nothing — and **39 of 40 scenarios still exit 0**, because 38 of them then run **zero checks** and `Scenario` exits 0 on a run that asserted nothing. A scenario that measures nothing is indistinguishable from one that passed. The byte comparison caught it completely (0 of 51 frames identical) and the repro *output* diff caught it (1,013 lines) — the exit codes did not. **This is why the brief's "diff the full output, not the exit codes" is right, and why a `--min-checks` floor belongs on the runner**; a behaviour change hidden inside a refactor is unreviewable, so it is written down here instead |
| 121 | 2026-08-02 | **`OrionApp.swift` splits by region, and the command-line modes stay with the `@main` entry that dispatches them** | 1,557 lines against a stated ceiling of 1,000, the last of the three the sixth wave was chartered against. **The brief's proposed seam — entry-and-CLI / menus / root-view-and-chrome — was checked against the file and rejected**: `Editor` alone is 1,257 lines, so that cut leaves the third file at about 1,350 and still over the ceiling. The seam is #113's and #117's instead — **by region of the problem**, so both halves of a change sit together and *which* file is answered by what the change is about: `OrionApp.swift` **296**, `OrionApp+Commands.swift` **323** (what a key press does), `OrionApp+Chrome.swift` **373** (toolbar, tab strip, footer), `OrionApp+Canvas.swift` **244**, `OrionApp+Tools.swift` **163**, `OrionApp+Files.swift` **262** (everything that touches a file on disk). ⚠ **`cullActions` moved in with the menus rather than staying with the view**, because adding a menu item means a field on `CullActions` *and* a line in `cullActions` *and* a button in `PhotoCommands` — three edits that were in one file and two hundred lines apart, and are now adjacent. The same argument put `installKeyMonitor` and `handleKey` there: **the menus and the local key monitor are two halves of one answer**, each covering what the other cannot, and the two bug histories that explain them now sit in one header instead of four hundred lines apart. ⚠ **`OrionApp.swift` is what Swift left, and that is stated rather than glossed**: `@State` are *stored* properties and stored properties are banned from extensions outright, so `Editor`'s three dozen of them and the `init` that seeds four could not move. What remains is the entry, the four CLI modes, the values, `body`, and the lifecycle pair `body` installs and takes back. Mutation **M5** confirms the constraint is a compile error and not a style rule — `error: extensions must not contain stored properties`, exactly #117's M1. ⚠ **The cost is #117's exactly**: Swift's `private` is file-scoped, so **seventeen declarations widened to internal** — five stored properties, eleven views and methods, and `PhotoCommands`, which was a private type. Nine members stayed `private`, and every widening is marked where it happened. ⚠ **Verbatim motion, asserted by ledger rather than by eye**: every body is a line-range slice, and a multiset diff of all 1,426 non-blank lines accounts for every difference — five `// MARK:` comments dropped because the filenames now carry them, seventeen declarations re-emitted without `private`, and the file headers. **No moved line was edited.** ⚠ **The oracle was checked against itself before it was believed, and it is noisy on one scene.** Two runs of the *pre-split* binary over 38 scenes agree 37/38; `--scene versions` disagrees with itself, because `Screenshot.snapshots` stamps `Date()` into the rows and the panel prints an absolute clock time. Pre-split against post-split is **the same 37/38 byte-identical**, and the 38th differs only by `6:24 AM` → `6:34 AM`, confirmed by looking at it. Recorded as a gap, not fixed. ⚠ **All four command-line modes were run before and after and diffed** — `--scenario` over all 40 repro files (954 lines of report, 40/40 exit 0), `--batch-export` of three photographs, `--library-open` of `samples/`, `--screenshot` over 38 scenes — and after normalising wall-clock and the probe database's UUID **every one is identical**, the three exported JPEGs matching by SHA-256 with no normalisation at all. ⚠⚠ **Three of the four named gates cannot see this file, and that is structural.** `apps/tests/CMakeLists.txt` and `apps/bench/CMakeLists.txt` name no Swift at all, and `orion-viewport-tests` compiles a Swift source list containing **zero** `OrionApp*` files. Only the 40-scenario sweep runs the app binary, and it only ever passes `--scenario` — so `--screenshot`, `--batch-export` and `--library-open` are each **checked by nothing in the repository**, which M1, M3 and M4 confirm one at a time: deleting a mode's four-line dispatch from `init()` leaves every repo gate green and is caught only by a probe written for this session. ⚠ **Eight mutations, and the three that stayed green everywhere are the finding.** **M5** proves the seam's constraint is a compile error rather than a preference — a `@State` moved into `extension Editor` gives `error: extensions must not contain stored properties`, exactly #117's M1. **M1/M2/M3/M4** each disable one CLI mode and each reddens exactly one check and no other; M2 is the only one the repository itself catches, because it is the only one the 40 scenarios go through. **M6 deletes the Reset Adjustments menu command — three scenarios, three byte-compared frames, both other modes and all four gates stay green.** A menu item can be removed from the product and nothing anywhere notices: the screenshot harness renders `Editor` directly and never builds the `Scene`, so `PhotoCommands` is not in any captured hierarchy, and no scenario verb opens a menu. **M7 swaps `--screenshot` ahead of `--scenario` in `init()` and is green everywhere** — the comment calling the order load-bearing is defensive rather than load-bearing, because `Screenshot.options` returns nil unless `--screenshot` is in `argv` and no invocation passes both flags; the eight repro files that write stills use the `shot` *verb*, which calls `Screenshot.writeCanvas` directly and never the CLI mode. The order is kept anyway. **M8 deletes the footer's `engine.lastFailure` branch — the "Render failed — …" line that landed the same morning — and is green everywhere**, including on `nofailure`, the verb added hours earlier for exactly this hazard: **that verb pins the engine's `lastFailure` value, not the status line that displays it**, and no scene renders a failed frame, so the byte comparison is blind to it too. #117 recorded that `lastFailure` had no oracle; half of it now does, and the half that reaches the photographer still does not. All three are written down and none is fixed, because a behaviour change hidden inside a refactor is unreviewable.
| 122 | 2026-08-02 | **`DevelopPanels.swift` is one file per tool tab, and the furniture that two tabs share is its own file** | 1,366 lines against a stated ceiling of 1,000, and the last of the three `app/` violations that is not a harness. **The seam is the tool tab, and it is the seam because it is the unit a change arrives in**: `CLAUDE.md`'s test is that adding a slider is a repeatable 3-file change, and a slider belongs to exactly one tab, so naming the file after the tab is what makes *which file* answerable without opening any of them. `DevelopPanels+Light.swift` (61), `+Color` (105), `+Detail` (188), `+Optics` (70), `+Mask` (580, the rows, the Add menu, the placement controls and the matte helpers), `+Presets` (367, presets and the versions that share its tab) — and `DevelopPanels.swift` falls to **56**: one button. ⚠ **The residual file is furniture rather than a dispatch list, and that is because the list is not in it.** #113 and #117 both left the two enumerations behind; here the tab bar and its `switch` are in `OrionApp.swift`, as are the `section` and `slider` helpers every panel calls, so what was actually shared was one control. ⚠ **No SwiftUI state moved, and that is checkable rather than claimed**: the file declared exactly one property wrapper in 1,366 lines — `FlowGroups`' `@Binding var selection` — and it travelled inside its own type. Every `@State`, `@FocusState` and `@Environment` the panels read is declared in `struct Editor` in `OrionApp.swift`, which was not touched; the panels are extensions, so they cannot carry storage even by accident. **This is why a SwiftUI split is safer than #117's**: `Engine` is `@Observable` and had stored properties to strand, and these files have none. ⚠ **One hand edit, and it is the same price #117 paid.** Swift's `private` is file-scoped, so `PanelButton` — drawn by Light's Auto and Detail's Load LUT — widened to internal. It is the *only* widening: every other private member (`addMenu`, `versionRow`, `stamp`, `saveVersion`, `commitRename`, `restoreVersion`, `maskKindCell`) kept its callers inside its own file, which was a constraint on where the cuts went rather than a happy accident. Mutation M6 restores the `private` and the build refuses: `'PanelButton' is inaccessible due to 'private' protection level`. ⚠ **Verbatim motion, proved rather than trusted:** every body is a line-range slice and a coverage check asserts all 1,366 lines are claimed by exactly one destination or by the 24-line scaffold each file re-emits (its import, its header, its `extension Editor {` and the closing brace). Nothing was retyped. ⚠ **The interface did not move, and the oracle was checked against itself first:** 42 scenes rendered twice from the pre-split binary agree on 41, and the 42nd is `versions`, whose rows carry `Date()` stamps at minute resolution — so it is compared visually and is identical but for `6:27 AM` → `6:35 AM`. After the split those same 41 are **byte-for-byte identical** by `cmp`, the 40 repro scenarios exit 0 with **full output identical** once the wall-clock lines are removed (42 differing lines, 0 of them not a timing), and 800 / 3702 / bench 0. ⚠ **Three of the five gates cannot see these files at all, and it is structural rather than bad luck.** `orion-tests` and `orion-bench` are C++ binaries, and `orion-viewport-tests` compiles 39 Swift files of which none is a panel — so the only checks with any reach here are the app build, the 41-frame comparison and the 40 scenarios. ⚠ **And the frame comparison has a hole underneath it, found by mutation and measured rather than argued: the Detail panel scrolls, and everything below the fold is unchecked by anything.** M9 deletes **five entire sections — Grain, Vignette, Dehaze, Clarity and Sharpening, 60 lines of shipped controls — and every check in the repository stays green**: 41 frames, 40 scenarios, 800, 3702, bench 0. M1 had already shown it for Vignette alone. M10 deletes `Noise Reduction`, eleven lines higher in the same file, and six frames go red — so the file is covered exactly as far as the panel is tall. **Left unfixed and written down, because a behaviour change hidden inside a refactor is unreviewable**; the fix is a scene that scrolls the Detail panel, and it is the same shape as #117's fix for its two blind mutations. ⚠ **`maskKindCell` is dead code** — 20 lines, one occurrence, stranded when the six-cell grid became the Add menu. Moved verbatim into `+Mask.swift` and left alone for the same reason. ⚠ Eleven mutations; the seven that redden do so **only** on the frame comparison and the build, never on repro, which drives the engine and not the panel |
| 123 | 2026-08-02 | **A zero-mean error needs a spread to see it, and a solver must be started somewhere it has to work** | The last two of the three checks #118 found that cannot fail. ⚠ **`screen vs export path` compared two frame *means* and an ordered dither is zero-mean by construction**, so mutation M9a — `bayerOffset` multiplied by forty, a dither eighty times too wide — printed `ok` at -0.00313 luma against a 0.00392 bound, the gap coming only from `saturate` clipping the ends off. The fix is the **mean per-pixel deviation between the two buffers** (`meanAbsDiff`, the one instrument in the file that cannot cancel), against the same one-eight-bit-step bound stated per pixel rather than per frame. That bound is the same claim, not a new one: the narrow path is `round(v + d)` with `\|d\| <= 0.5/255`, a resample, then a second round, so the honest deviation is a fraction of a code and it scales **linearly** with the dither amplitude. Measured **0.00116 / 0.00115 / 0.00119** on `_PIC8220` / `_PIC8148` / `_PIC8095` against 0.00392 — 3.3x of headroom, and near-identical across three very different photographs because it measures the quantiser, not the picture. M9a takes it to **0.01398** (worst pixel 0.08075) and prints `DITHER MAGNITUDE`. Deterministic, not sampled-noisy: an ordered dither is a fixed table. The worst pixel is printed beside it and deliberately **not** gated — a max over 24 MP is a good thing to read and a poor thing to assert. ⚠ **`auto_enhance::refine` could be gutted with `converged` still green on `_PIC8220`** — mutation M11 — because the look sets fusion and clarity from the *first* measurement, and on that frame they land the median at 0.4570 against a 0.4610 anchor, inside `kSettled`, so the loop broke before calling `refine` once and the reported set was `+0.00 EV, +0.00 blacks, +0.00 whites`. It went red on `_PIC8095`, so the suite caught it only by luck of which file the gate command named. **The fix is to start the solve two stops away from the anchor**, in whichever direction that is for the frame — measure at rest, then displace `-2 EV` if the median is below mid-gray and `+2 EV` if above. Two stops is far outside `kSettled` under any transfer function and the response is monotonic, so the first measurement is off-anchor on *every* frame and the loop cannot exit without the solver having moved something. It is also the stronger claim, because a fixed-point iteration must not care where it started: from the displaced start `_PIC8220` converges in 8 passes to `+0.01 EV`, `_PIC8148` in 7 and `_PIC8095` in 11 from `+2.00 EV`, all landing within 0.004 of the anchor. `refines >= 1` is asserted alongside, so removing the displacement later fails loudly rather than restoring the blind spot. M11 now prints `converged: NO` and exits 1 on **all three** frames. ⚠ Both fixes are stated as node-free statistics rather than wall clocks, per #116 |
| 124 | 2026-08-02 | **A scenario run that asserted nothing fails. The floor defaults to one check and the file may declare its own with `minchecks <n>`** | #120's mutation M4 makes one verb family claim every verb, silently disabling three quarters of the vocabulary — and **39 of 40 `repro/*.txt` still exited 0**, because 38 then ran **zero checks** and `orion: 0 checks, 0 failures` is a success. ⚠ **A scenario that measured nothing is indistinguishable from one that passed**, and this is the gate named in `CLAUDE.md`, in every agent brief and in the working agreement. Only two things saw it: a byte comparison of 51 rendered frames and a diff of the runner's whole output. Neither is what the gate runs. ⚠ **Exactly 2 of the 40 legitimately assert nothing** — `eyedropper-latency.txt` and `slider-drag-cost.txt`, both instruments rather than tests, both of which already explained themselves in prose ("There is no `expect` here on purpose … a wall-clock threshold fails on a busy machine and teaches everyone to ignore it") that nothing could read. They are demos, not gaps; the other 38 range from 1 check to 24. ⚠ **Default-deny is the load-bearing half, not the guard.** Mutation M-C sets the default to 0 and leaves the guard standing — an opt-in floor — and M4 goes straight back to **39/40 exit 0**, exactly as blind as no floor at all: the declaration is a line of the file, so a dispatcher that eats every verb eats that one too, and an opt-in guard disarms itself with the defect it exists to catch. Defaulting to 1 means a broken dispatcher can only ever lower a file's checks *into* the floor, never lift the floor out of the way. ⚠ **The number lives in the file, not in the caller.** A `--min-checks N` on the command line is one number for forty files that range from 1 to 24, held in the gate loop where it drifts out of sight of what it describes; a per-file floor stays with the file and, for the two instruments, sits next to the paragraph saying why. A blanket "at least one check" needs no bookkeeping but cannot express those two at all. What shipped is the blanket rule *as the default* with a per-file override — no bookkeeping for 38 files, and the 2 exceptions stated rather than tolerated. **The exact counts are deliberately not pinned** in the other 38: an exact expectation churns on every honest edit and buys nothing here, since every M4 casualty goes to zero. ⚠ **`minchecks` is a declaration, not a step** — parsed in `run` before the first verb executes, and the one word in the grammar that does not live in a family switch, because a floor dispatched like an ordinary verb can be *taken* by a broken family and lost. #89 is honoured: added, nothing renamed, nothing removed. A `#`-commented declaration is inert and a malformed one exits 2 rather than being ignored. ⚠ **The consequence to accept:** the session log `InteractionLog` writes is a runnable scenario with no checks, so replaying one now exits 1 with a message naming the fix. Nothing automated runs it and nobody reads a replay's exit code, but a `minchecks 0` in that header is owed — left to whoever owns that file rather than reached into from here. **Result: M4 goes from 39/40 exit 0 to 3/40**, the three being the two declared instruments and `snapshot-keeps-its-matte.txt`, whose 3 surviving checks are `snapshot missing`, implemented by the very family the mutation makes claim everything. That last one is a real residual and is recorded as a gap, not papered over. 800 / 3708 / 40 green, bench exits 0, and the 40 scenarios' full output is unchanged from before this work |
| 125 | 2026-08-02 | **Three holes in the interface's coverage were one hole, and the fix is three scenes in the screenshot harness** | Two splits had each found, by mutation, that shipped UI could be deleted with every check in the repository green, and each correctly left it — a behaviour change hidden inside a refactor is unreviewable. **All three have one cause**: `Screenshot.swift` builds `Editor` directly and drives only what is on screen at rest. ⚠ **(1) The Detail panel scrolls, and coverage stopped at the fold** (#122's M9): Grain, Vignette, Dehaze, Clarity and Sharpening — 60 lines of shipped controls — deleted, and 41 frames, 40 scenarios, 800, 3702 and the bench all stayed green, while Noise Reduction eleven lines higher reddened six frames. **`--scene detail-tail`** scrolls the panel column to its end through the `NSScrollView` AppKit backs SwiftUI's `ScrollView` with — the real scroll on the real view, chosen by overflow rather than by name, since the filmstrip scrolls sideways and its document can never be taller than its clip — and **refuses to write a frame at all when nothing overflows**, because a scene that photographs what is past the fold and finds no fold has stopped covering anything. ⚠ **The first capture landed past the section it was written for, and looking at the PNG is what caught it**: the panel's content is **1,701 points** against a **681**-point window, so the scene at rest accounts for 0–681 and a frame scrolled to the end accounts for 1,020–1,701, leaving **Grain in the 339-point band between them**, seen by neither. The scene therefore asks for a **1,500-tall window** — a resizable window on a taller display, which is a state the interface is really in — giving 1,131 of window, 570 of scroll, and **111 points of overlap with nothing between**. ⚠ **(2) The Photo menu is unreachable from every check** (#121's M6): a `Commands` is not a `View`, so there is no hosting view to put `PhotoCommands` in and nothing to photograph, and deleting a menu command reddened nothing anywhere. **`--scene menu`** clears the harness's own hook, calls **`OrionApp.main()`**, and reads `NSApp.mainMenu` once AppKit has installed it — the shipping `Scene` building the shipping `PhotoCommands`, not a second list of what it ought to contain — asserting **26 commands by title**, printing the whole 75-item bar when one is missing. ⚠ **Deliberately not driven through `CullActions`**, which is reachable and would have been easy and **green on the mutation**: the mutation deletes the *button* and leaves the action it called sitting there, and what was unchecked was whether the command is in a menu at all. ⚠ It asserts **presence, not firing** — the items are disabled at launch, and firing one needs a photograph, a key window and focus; that is #110.3's answer, reach for the real thing and say plainly which link is still unpinned. The price is a window that really does open, for about a second. ⚠ **A defect nobody was looking for, on its first run**: the bar ships **`Compare Original  ()`** — the source writes `"Compare Original  (\\)"` and a `Button`'s string is a `LocalizedStringKey` whose escape character is the backslash, so the one item that spells its key only in its title has lost it. **Pinned as it ships** rather than fixed, because the fix is a line in a file this story does not own and a check that is red the day it lands is not a check. ⚠ **(3) The footer's `lastFailure` branch had no oracle** (#121's M8): `nofailure` pins the engine's value, not the line that shows it, and no scene rendered a failed frame. **`--scene render-failed`** plants the failure **and suspends the engine**, and the second half is load-bearing — **laying the interface out renders** (the canvas's `onAppear` assigns `engine.cropPreview`, whose `didSet` is `pushAndRender()`), so the first version wiped its own failure and came back showing the ordinary hint and `0.0 ms`: a photograph of the bug, captioned as a photograph of the fix. Suspending is what a failed engine looks like from the panel's side. ⚠ **Three mutations, each the exact deletion the two splits named, and all three are caught**: **M1** (five Detail sections) takes `detail-tail` from exit 0 to **exit 1**, with `--scene detail` byte-identical and 40 scenarios green — the old coverage is still blind, which is the point; **M2** (Reset Adjustments) takes `menu` from 26/26 to **exit 1** naming the item, bar 75 → 74, every frame identical; **M3** (the footer branch) leaves `cmp` **differing** at char 11,672,187, confirmed by eye to be the amber line gone and the 9-point `failed` readout alone — with `nofailure` and all 40 scenarios green on the same mutation. Gates 800 / 3708 / 40 of 40 / bench 0 before and after, and all three frames byte-stable across two runs of one binary, self-checked before being trusted |
| 126 | 2026-08-02 | **The 1000-line rule binds `apps/tests/` too, but a test file is split by fixture rather than by region, and it is not urgent** | Raised by #122's sweep, which found that after six splits the only files over the ceiling are four in `apps/tests/`: `tests_effects.cpp` **1,716**, `tests_brush.cpp` 1,142, `tests_perspective.cpp` 1,110, `tests_grade.cpp` 1,029. Flagged as unsettled rather than assumed, because someone was about to split the largest on the strength of a rule nobody had checked applied. ⚠ **It applies.** `CLAUDE.md` says *"No 1000-line anything"* and gives the reason — *"the developer must be able to hand-edit any file later"* — and that reason does not care whether a file ships. A solo maintainer who cannot find the test for a filter will not write one. There is precedent, too: two test files were split on 2026-07-31, before any rule was invoked, simply because they had become unnavigable. ⚠ **But the seam is different, and this matters more than the number.** The six product splits all cut **by region of the problem**, because the question they answer is *"where does a new node go?"* — a dispatcher's cost of being long is that the answer is *"somewhere in the middle of the largest function in the app"*. A test file has no dispatcher and nobody hunts for where to add a case: the question it answers is *"what broke?"*, so the seam is **the fixture** — the frame and the state a group of checks share — and a split that separates a check from the fixture it reads makes the file worse at 400 lines than it was at 1,700. ⚠ **And it is deliberately not urgent.** The product splits paid for themselves the same day, because each one made the next feature a smaller diff. These do not: no feature is blocked, and the four files are the *least* dangerous place in the repository to have a long file, since a test that is hard to read is discovered by being wrong rather than by shipping a bug. Queued below the coverage holes the splitting wave found — a check that cannot fail costs more than a file that is hard to scroll |
| 127 | 2026-08-02 | **`tests_effects.cpp` is three files, cut where the fixtures already were, and no check moved away from the frame it reads** | #126 settled the seam; this is it applied to the file it was raised about — **1,716 lines, the largest in the tree** — and the fixtures turned out to be **fewer than the subjects**, which is the whole reason the seam is not the subject. Seven test functions, four fixture families: `tests_display.cpp` **331** (`testOutputDepth` + `testCreativeLut`), `tests_highlights.cpp` **865** (`testHighlightHaloGpu` + `testHighlightFillGpu` + `testHighlightFillWiring`), `tests_effects.cpp` **555** (`testLocalLaplacianGpu` + `testDehazeGpu`). ⚠ **Two of the three groupings put different subjects in one file, and that is the seam working rather than failing.** Output depth and creative LUTs read the *same dispatch* — one row of scene-linear ramp through `developDisplay`, with a curve LUT and a cube bound — and the depth test's own comment already has to explain the cube binding it never samples, *"or every texture after it shifts by one, which is silent and total"*. A subject split would have put that comment in one file and the texture it describes in another. Likewise the three highlight tests are one blown lamp in a warm surround in three forms; the wiring test builds its own `DevelopPipeline` but its checks are **written against the solver test's** — *"`testHighlightFillGpu` asserts separately that it does not"* — so splitting them leaves each half asserting a premise it no longer establishes. Clarity and dehaze, by contrast, each build their own frame and read nobody else's, so they are cheap neighbours and keep the old name: a new effect's checks land there, next to a fixture they can copy. ⚠ **Verbatim motion, proved rather than asserted:** 1,708 body lines sliced out of the original **by index** and compared back against `HEAD` line for line, and `orion-tests` output **byte-identical** before and after — `diff` clean, 800 checks, 0 failures, same names, same order, because the running order lives in `main.cpp` and a translation unit does not have one. ⚠ **Six mutations, two per new file, and every one named its own check**: quantising `develop_display.slang` to eight bits (*"the output resolves far finer than eight bits could"*), swapping red and blue in `cubeCorner` (*"an identity LUT leaves every pixel where it was"*, 28/255), dropping the premultiplied guard in `hl_push.slang` (*"the shader and its host twin agree"*), making `hl_apply.slang` §3.3 a replacement (*"it moves only the clipped channel"*), shifting the Burt downsample one texel (9 red, led by *"alpha = 1 collapses back to the input"*), and skipping the dark-channel stage in `airlightFrom` (*"the atmospheric light is the haze, not the brightest pixel"*). ⚠ **One finding, left alone rather than fixed:** *"a constant rim fills with that constant"* claims *"any weighting error… shows here"* and does not — the push mutation scales colour and weight together, so `v/w` is unchanged and that check stays green while three of its neighbours go red. It is not unfailable, it is narrower than its comment. Also left: a doc comment for `testExposureFusionMath` dangling at the end of the file since the 2026-07-31 split, describing a test that lives in `tests_fusion.cpp` |
| 128 | 2026-08-02 | **A comment belongs to a declaration, and a split is when it stops. Grep is the check for dead code, because the build is not one** | The other half of #122: two defects it recorded and correctly did not fix, since a fix inside a refactor is unreviewable. Both are fixed here, and the sweep they invited found four more of the second kind. ⚠ **`maskKindCell` is deleted, and the justification is the grep rather than the build.** An unused `private func` compiles without a warning, so the build cannot see this class at all; `grep -rn maskKindCell` over the whole tree less `.git`, `build` and `third_party` returns **one occurrence — its own declaration** — plus two mentions in the planning docs. That is the bar, and the reason the bar is grep. ⚠ **The second defect needed rewriting rather than moving, and reading it first is what showed that.** The doc block above `maskKindCell` opens *"Masks — 'local' adjustments, on a tab of their own"*, which is `maskPanel`'s subject, not a button's; it had been stranded since `maskPanel` moved away from underneath it, and `git show` of the pre-split file confirms it was already 104 lines from its declaration before #122 touched anything. But its last paragraph claimed *"the picker is a grid now"* — false since the grid became `addMenu`, the same change that stranded `maskKindCell`. **Relocating it intact would have moved a wrong sentence to a better address.** Rewritten to say what is actually there and keep the reason that is still worth knowing: six named kinds do not fit on one row at this width, which is what broke the window when the picker was a segmented control. ⚠ **The class is real and it is caused by splitting, in both directions.** `DevelopPanels.swift` sent readers to `OrionApp.swift` for `section`, `slider` and the tab bar, which #121 had moved to `+Tools` and `+Chrome`; `OrionApp+Tools.swift` and `OrionApp.swift` still called `DevelopPanels.swift` the home of the panels, which #122 had reduced to one button. **Two splits landing the same day left each other's file references stale**, and neither is visible to any compiler. Four more found the same way: `Engine.swift` (`edit`'s doc on `log`), `PhotoIndex.swift` (`refreshMarks`'s doc on the `marksReadWindow` test hook), `Screenshot.swift` (three docs — `measure`'s, `regionStats`'s and `Surface`'s — merged onto `enum Surface`), `MatteStore.swift` (`referenced`'s one-liner and the forty-line sweep policy both on `SidecarState`, which `a76ebfb` inserted above them; the parent commit shows each correctly attached). ⚠ **And one comment that was not misplaced but wrong**: `DevelopPanels+Mask` claimed `LocalRefusals` and `PipelineOrder` are *"kept and still tested"*. They are kept — deliberately, an in-code note says restoring either is one line — and **nothing tests them**; what is tested is `AdjustmentCatalogue.refusedLocally`, the table they generate from. Corrected rather than deleted, because a wrong reason for keeping something is worse than no reason. ⚠ **Five more unreferenced declarations found and deliberately left**: `removeSpot(_:)`, `jumpHistory(to:)`, `clearPlaceholder()`, `moveCrop(dx:dy:)`, `allSyncableKeys` — one occurrence each. Every one is the named half of a pair whose other half ships (`removeLastSpot`, `undo`/`redo`, `showPlaceholder`, `setAspect`, `keys(for:)`), so each reads as a feature never wired rather than a leftover — `removeSpot`'s own doc says *"what a selected spot and a Delete key mean"*, and there is no spot Delete handler. **Deleting something that turns out to be used is much worse than leaving something dead**, so the rule is: one occurrence *and* no plausible caller names it. ⚠ **Both sweeps are scripted, so the next one is cheap.** Dead declarations: every `func`/`var`/`struct`/`enum` name in `app/`, counted whole-word against every code file in the tree with comments stripped — one hit is the declaration alone. Lost comments: inside a `///` block, a **short** line ending in `.` followed immediately by another `///` line, because a paragraph that ends before the wrap width is a paragraph that ended, and a doc pasted onto a doc looks exactly like that. Fifteen candidates, six real, nine one subject written in two headlines. ⚠ **Gates 800 / 3708 / 40 of 40 / bench 0, and three of those cannot see a Swift panel.** `--scene detail` and `--scene detail-tail` rendered twice from the pre-change binary first — **byte-identical, so the oracle is one** — and byte-identical after by `cmp`. Which is what a correct answer looks like here: the only executable line removed was one nothing could reach |
| 129 | 2026-08-02 | **The last three files over the ceiling are split, all three at a fixture boundary — and the two mutations that stayed green are the finding, not the split** | #126 named four files and #127 took the largest; these are the other three, and they are the hard case #126 warned about because **none of them was more than 142 lines over**. Each was read for its fixtures before anything moved, and the answer was the same shape every time: **the subjects in the file outnumbered the fixtures, and the odd ones out were whole tests that shared *nothing* with the rest of their file** — not a device, not a kernel, not a helper, not a frame. That is why all three could be split and why none of the cuts is close: no check moved away from the premise it reads, because in every case the block that moved read nothing from the block that stayed. ⚠ **`tests_brush.cpp` 1,142 → 824, and the two it lost were never brush tests.** Four fixture families: a CPU Bayer mosaic (`testBayerDecimation`), the `spotMeasure`/`spotApply` pair on a flat two-field frame (`testSpotRemovalGpu`), the `maskComponent` kernel with a dab texture and a bounds texture (`testDabBlockRejection` + `testLongBrushStroke` + `testBrushErase` — three near-identical rigs, `uploadDabBounds` written out verbatim in two of them), and the prefix predicate (`testBrushPrefixPredicate` + `testBrushPrefixWiring`). Decimation went to **`tests_color.cpp`** (324 → 414) rather than to a file of its own, because its checks are `channelAt` on an `orion::raw::BayerImage` with `filters = 0x94949494u` — *literally the fixture `testCfa` builds twelve lines above where it now sits*, and its headline assertion is "every sample keeps its CFA channel". Spot removal became **`tests_spot.cpp`** (246), two kernels nothing else in the tree dispatches. ⚠ **The prefix pair was deliberately not separated**, on #127's grounds exactly: the wiring test's own header says *"the unit test above cannot see the two things this one exists for"* and the unit test's says *"`testBrushPrefixWiring` is the same predicate through the real pipeline"* — each is written against the other's premise, and mutation M10 reddens checks in both at once, which is that dependency measured rather than asserted. ⚠ **`tests_perspective.cpp` 1,110 → 837.** This is the only one of the three with a real file-scope fixture — an anonymous namespace holding `kW`, `kH`, `coordinateFrame` and `loadMatrix` — and **six of its nine tests use it while three do not**. Of those three, `testPerspectiveMaskGeometry` and `testPerspectiveMaskExtent` touch no GPU at all: they are `mask::toFrame`, `fromFrame` and `radiusToFrame` on the host, which is **exactly what `tests_mask_geom.cpp` already is** (350 → 642). A homography is the fourth transform in that file's list beside the crop, the quarter turns and the straighten, and both files open with the same check — *nothing applied is nothing changed*, on the same call. So this is a merge into an existing fixture rather than a new file. `testPerspectiveWiring` stays: it drives `DevelopPipeline` and shares the subject, not the fixture, with what is left. ⚠ **`tests_grade.cpp` 1,029 → 653**, and it is the file with **no shared state whatsoever** — five tests, five self-contained fixtures, no anonymous namespace, no helper above the first function. The cut is the one place two of them *do* share: `testToneBandsWithoutGuide` and `testLocalAdjustments` both dispatch `developLinear` with the same eight-texture binding list and the same `params::LinearAdjust`, and the first has to explain a mask binding it never samples — *"the binding has to exist or every texture after it shifts by one, which is silent and total"* — while the second is the test that samples it. That is #127's `tests_display.cpp` argument, arriving unprompted in a different file, so the pair is one new file, **`tests_linear.cpp`** (390). ⚠ **`testLensAutoScale` was left where it is and that is a decision, not an oversight**: it is host lens maths with nothing in common with hue/sat or the grade, and its subject-mate `testLensGpu` lives in `tests_tone.cpp` at 858 — moving 90 lines there to save 90 here takes a second file to 948 and buys nothing. **A split that does not clearly help is worse than no split.** ⚠ **Verbatim motion, proved by index and not by eye:** all four blocks — 276, 377, 82 and 238 lines — were sliced out of their originals by line number, then sliced back out of their destinations and compared with `HEAD` line for line. `orion-tests` output is **byte-identical** before and after: `diff` clean, **800** checks, 0 failures, same names, same order, because the running order lives in `main.cpp` and a translation unit does not have one. Four headers were rewritten, since three of them named subjects that had left; no moved line was edited. ⚠ **Fifteen mutations, at least two per file touched, and thirteen named their own check.** `spot_measure.slang`'s rim moved inside the disc (2 red, *"heal pastes the source's detail with the destination's tone"* at 0.0500 — the blemish itself); `spot_apply.slang`'s aspect dropped (*"a spot is round on a 2:1 frame"*, 18 x 9 px); `decimate`'s phase shifted (3 red) and its averaging replaced by a point sample (1 red, isolating *"an output sample is the mean of its same-phase inputs"* at 500 against 125); `radiusToFrame` made isotropic — **the mutation that test's own comment names** — 5 red including *"every point of the mask's rim, mapped, lands on the rim of the ellipse that comes back"* at 15.88; the half-texel dropped in `unperspective` (5 red, across the moved pair *and* `testPerspectiveWiring`); the warmth renormalisation removed (2 red); `develop_linear`'s guide fallback made to read the guide anyway — the shipped bug — (2 red, 1.19 EV); `unchangedPrefix` comparing two floats instead of four (3 red, in **both** halves of the prefix pair); `buildDabBounds` shrinking each box by 0.02 (5 red); the homography transposed in `geometry.slang` (5 red, 90.5 px); `autoScale` made to never zoom (8 red); `kEvShadow` nudged **0.05 EV** (2 red — the test predicted *"fails here and nowhere else"* and it does); and the ProPhoto round trip broken (6 red). ⚠⚠ **One mutation is green everywhere, and it is the exact mistake the check beside it says it exists to catch.** `mask::unperspective` conjugates the derivative by the frame's axis scales — `jn = {j.a, j.b*t/w, j.c*w/t, j.d}` — and **deleting that conjugation outright leaves 800 / 3708 / 40 / bench 0 all green**. `testPerspectiveMaskExtent`'s check 6 was written for it: *"Getting the conjugation W⁻¹JW wrong on a 3:2 frame is a plausible wrong answer of exactly the kind this file exists for: it is invisible on the square fixtures and wrong on every photograph."* The reason it cannot see it is in the fixture, not the assertion: the check drives `persp::compose({0, 0, 1.0f}, 600, 400)`, **a pure aspect squeeze, whose Jacobian is diagonal** — and a diagonal `J` has `b = c = 0`, so the conjugation multiplies two zeros and is a no-op. The check needs an off-diagonal derivative, which means a keystone rather than an aspect. **Left unfixed and written down**, per the brief: a finding is worth more than a silent repair, and this is the second time in two days a check has named its own mutation and missed it (#127's *"a constant rim fills with that constant"*). ⚠ **Two dangling doc comments, both left alone.** `tests_brush.cpp` ends with a header for a luminance range mask whose test lives in `tests_mask_range.cpp`, and `tests_grade.cpp` ends with one for `testOutputDepth`, which #127 moved to `tests_display.cpp`. Both are strays from the 2026-07-31 split, both are the same shape as the `testExposureFusionMath` comment #127 recorded, and none of the three has been touched — the pattern is worth more visible than tidied. ⚠ **The ceiling is recounted by sweep, never edited in place**: `git ls-files` over all **235** tracked `.swift/.cpp/.h/.hpp/.mm/.c/.m/.slang` files at `49c8a83`, and **exactly one file is over 1,000** — `app/Screenshot.swift` at **1,194**, which is a different story and another agent's. `apps/tests/` is entirely under the ceiling for the first time; the largest survivor anywhere is `ShaderParams.h` at **905**. Gates: 800 / 3708 / 40 of 40 / bench exits 0, **p95 9.37, 9.15 and 9.27 ms over three runs against a 16 ms gate** — the M0 gate is noisy (#116) and did not need re-running once |
| 130 | 2026-08-02 | **Both checks that named their own mutation and missed it now fail on it — and proving the first turned up a third over-claim in the same file's header** | The two holes #127 and #129 found by mutation and deliberately left, closed together in `apps/tests/` with **no engine line changed**. ⚠ **`testPerspectiveMaskExtent` check 6 was blind because of its fixture, exactly as #129 diagnosed**: it drove `persp::compose({0,0,1}, 600, 400)`, a pure aspect squeeze, whose Jacobian is **diagonal** — so `b = c = 0` and the `W⁻¹JW` conjugation its comment names multiplied two zeros. The squeeze block **stays**, because what it asserts — that a *shape* and not merely an area survives the trip — it asserts well, and it now carries a check that says out loud what it cannot see: `report(jac.b == 0 && jac.c == 0, "the squeeze's derivative is diagonal, so the conjugation is a no-op on it")`. ⚠ **Beside it, 6b drives a two-way keystone** — `{0.8, 0.6, 0}` on a 600×400 frame, four spots off both axes of symmetry, least &#124;off-diagonal&#124; **0.0674** — and the fixture's own premise is asserted rather than assumed, so a future edit cannot quietly return it to diagonal. ⚠ **The answer is checked against a central difference of `toFrame`'s own neighbouring centres**, and that is the part worth keeping: a *position* never passes through the conjugated matrix (it goes into texels, through `persp::apply`, and back out divided by the frame), so the two agree only if `Placement::jac` really is the derivative of the map the placement performs, in the normalized coordinates its own documentation claims. It is an independent answer, not the same algebra written twice — which is what the first draft of such a check always is. Baseline worst **9.0e-6** against a 1e-3 tolerance; **deleting the conjugation prints `worst 0.072389` and `1.483727 rad` and exits 1**, on the two checks that name it. ⚠⚠ **The mutation run that confirmed it exposed a third over-claim, in this file's own header**: *"every check below fails on the isotropic version"*. That mutation reddens **five** — both axes of check 1, the turn in check 2, the rim invariant in check 3, the two-to-one ratio in check 6 — and four checks are blind to it **on purpose**, check 4 preserving area and check 5 being the neutral control, each of which says so where it sits. The header now counts what the run counts. Three over-claims in one file in two days, and the shape is always the same: **a sentence written about the algorithm rather than about the fixture underneath it**. ⚠ **The highlight-fill comment was rewritten *and* its check strengthened, rather than either alone.** *"A constant rim fills with that constant"* claimed *"any weighting error, any lost normalization, any half-texel drift in either kernel shows here"* and reaches none of them, for a reason that is structural rather than accidental: with constant Dirichlet data every value in the pyramid is c·w for one c, so **any** blend that carries colour and weight through the same arithmetic returns c whatever its weights are — wrong taps, drifted taps, no taps. Two mutations measured it. The naive un-premultiplied `lerp(up, f, f.a)` — the mistake `hl_push.slang`'s own header warns about — leaves it green at **2.98e-7** and reddens **one** check, the CPU twin. So the comment now claims the **pairing** and nothing more: that the fill divides by the weight it actually accumulated, on both paths, in both kernels. ⚠ **One piece of the old claim could be made true where it stood, for free, and was**: both shaders promise in prose that w stays in [0, 1] — `hl_pull.slang` calls Gortler et al.'s `min(1, sum)` cap *unreachable* because its taps are a partition of unity, and the push's `w + (1−w)·w_up` is a convex combination of two numbers in [0, 1] — and **nothing tested it**. The loop already walked every texel; it now tracks the maximum beside the minimum. Dropping the premultiplied guard (`f + up`) carries the weight to **7.12**, and that mutation now reddens **three** checks including this one, where before it reddened two, both in other blocks. A weaker check honestly described is fine; a weaker check honestly described **and** cheaply widened is better. ⚠ **Every verdict here is a build-and-run**, the exit code read directly and never after a pipe (#129's warning), and every engine edit reverted with `git checkout` before the gates. Gates: **806** checks (800 + 6), 3708, 40 of 40, bench exit 0, M0 p95 **9.21 ms** against 16 |
| 131 | 2026-08-02 | **`Screenshot.swift` is five files, cut at the check/picture seam — and the ceiling is met everywhere for the first time** | **1,196 lines against a stated ceiling of 1,000, and the last file in the tree over it.** It was **809** on the morning of 2026-08-02; #125's three interface checks took it over the same day, so the tool that closed three coverage holes became the only remaining violation. ⚠ **The seam is what a change to this harness is usually about, and the sharpest edge in it is one #125's own header had already drawn**: three of the scenes — `detail-tail`, `menu`, `render-failed` — are *checks* rather than *pictures*, and a scene that asserts is a different kind of thing from a scene that poses. So `Screenshot+Checks.swift` (189) holds the assertion and the knobs an assertion needs (`requiredCommands`, `checkMenu`, `scrolls`, `minimumHeight`, `failureText`), `Screenshot+Scenes.swift` (353) holds `apply(scene:to:)`, `tab` and `snapshots`, and **"add a scene" and "add a check-scene" are now each one obvious file** — which is `CLAUDE.md`'s test. The rest splits by the same question: `Screenshot+Measure.swift` (315, the readback and the two arithmetics over it), `Screenshot+Render.swift` (109, a SwiftUI hierarchy to a PNG offscreen, and `scrollToEnd`, which is `detail-tail`'s mechanism but a walk over `NSView` rather than a scene), and `Screenshot.swift` falls to **315**: the command line and the driver, in the order a run happens. ⚠ **Verbatim motion, asserted rather than trusted** — extracted by line range with a script that proves every one of the 1,196 lines is claimed by exactly one destination. **One non-blank line is not carried, and it is named**: `// MARK: Scenes`, replaced by the per-file headers. ⚠ **#117's Swift tax again, and it is the same tax**: `private` is file-scoped, so eleven members widened to internal — `fail`, `relaunched`, `checkMenu`, `scrolls`, `minimumHeight`, `tab`, `snapshots`, `apply`, `developed`, `measure`, `render` — because `run` calls across every seam and a driver is what a driver does. Four stayed private (`requiredCommands`, `panelClip`, `scrollToEnd`, the `CGImage` overload of `regionStats`). Recorded in the header of `Screenshot.swift`. ⚠ **Nothing moved, and the oracle was checked against itself before it was believed.** This file *is* the interface oracle, so breaking it silently would blind the checks that guard the UI. **45 scenes plus `menu`, rendered twice from the pre-split binary: 44 of 45 frames agree with themselves and `versions` does not** — exactly #125's warning, its rows stamp `Date()` at minute resolution — so `versions` is reported rather than asserted around. Against the split: **44 of 45 byte-for-byte identical by `cmp`, `versions` the only difference**, all **46 exit codes identical**, and all **46 stderr streams identical** once the output path is normalised — including `detail-tail`'s measurement, which reproduces #125's numbers to the tenth (570.0 points scrolled, 1,701.0 of content past a 1,131.0-point window) and `menu`'s 75-item bar with 26 of 26 present. ⚠ **The 40 repro scenarios were run on both binaries and the full output diffed**: 36 of 40 byte-identical, 40 of 40 exit 0, and **the four that differ differ only in milliseconds and frames per second** — `slider-drag-cost`, `gesture-preview-agrees`, `eyedropper-latency`, `dehaze-reaches-the-picture`, every one of them a scenario that prints a latency. ⚠ **All three of #125's mutations were re-run and all three go red**: **M1** (delete Detail's five below-fold sections, `DevelopPanels+Detail.swift` 126–185, exactly the 60 lines #125 counted) takes `detail-tail` to **exit 1** — "nothing overflows the panel column" — and writes no frame; **M2** (delete Reset Adjustments) takes `menu` to **exit 1**, bar 75 → 74, 25 of 26, naming the item and printing the whole bar; **M3** (delete the footer's `else if let why = engine.lastFailure` branch) leaves `render-failed` at exit 0 with the frame **differing at char 11,672,187 — the same offset #125 recorded** — confirmed by eye to be the amber "Render failed" line replaced by the ordinary grey hint, **8,671 pixels in a band x 2632–3359, y 1790–1846**. ⚠ **M2 could not be written as #125 wrote it.** Deleting only the `Button("Reset Adjustments")` line does not compile — the `.keyboardShortcut` and `.disabled` modifiers below it dangle onto `View` — so the mutation is the three-line item. Worth knowing before anyone re-runs it. Each mutation was reverted with `git checkout` and the tree confirmed clean. ⚠ **The ceiling row is recounted by sweep and replaced wholesale, never edited**: at `6767716`, over all **239** tracked `.swift/.cpp/.h/.hpp/.mm/.c/.m/.slang` files by `git ls-files`, **nothing is over 1,000** — the first time that has been true. Largest survivor `ShaderParams.h` **905**. This is one worktree and cannot see what is in flight elsewhere; it is a floor on the violation, not a ceiling. Gates: **800 / 3708 / 40 of 40 / bench exits 0**. ⚠ **The bench's first run said FAIL at p95 32.13 ms and it was contention, not the build** — spread 5.65 ms across rounds, run while the split's own rebuilds were still finishing; rerun on a quiet machine it is **9.68 ms, spread 0.24**. The bench links no Swift at all, so this split cannot reach it. #116's point about the M0 gate stands

| 132 | 2026-08-02 | **`STATUS.md` is pruned to its six most recent sessions, and a prune moves rather than copies — verified heading by heading** | Twelve agents merged on 2026-08-02 and every one appended a session entry; #131 deliberately skipped the prune because another agent held the same file and the gap table had already been found in contradictory copies twice. Nothing was running when this ran. **2,221 lines and 28 session/wave blocks → 745 lines and 6 sessions** (#131, #130, #129, #128, #127, #125); the other 22 blocks appended to `HISTORY.md` verbatim, in `STATUS.md`'s own order so no entry's neighbours changed. ⚠ **The rule the previous prune broke is the one that matters: a prune moves, it never copies.** 2026-08-01 left `2026-07-31j` in both files byte for byte, and a duplicate is worse than a long file because the two copies drift and each looks complete from either end. So the move is *proved* rather than asserted — all **63** moved headings (22 blocks plus their sub-sections) were checked by exact-match grep to be **absent from `STATUS.md` and present in `HISTORY.md`** afterwards, 63/63. ⚠ **The gap table was re-derived from the tree rather than trusted, and four of its claims were stale**: the ceiling row recorded `tests_highlights.cpp` at 865 as at `6767716` and commit `1a3083d` had taken it to **897** hours later (re-swept at `191b451` — `git ls-files` over 239 tracked sources, **nothing over 1,000**, largest `ShaderParams.h` **905**); the `InteractionLog` row asked for a `minchecks 0` that `InteractionLog.swift:77` **already writes**; the trailer count said 101 and is **363**; and the suite counts said 800/3702 against a measured **806/3708/40/bench 0**. A row that goes stale in hours is an argument for re-deriving every number at every prune, not for writing them more carefully. ⚠ **What was deliberately *not* touched**: the header still carries two `Last updated` lines, two `Phase` paragraphs and three overlapping copies of the "next story" queue. Choosing which queue is real is a judgement about what the next story is, and a prune that quietly rewrites the plan is how the four contradictory gap rows got there in the first place; it is named in the session log for a session that can do it on purpose. `HISTORY.md`'s own duplicates (`2026-08-01b` three times, four others twice) are left for the same reason — the brief for this prune was append-only |

| 133 | 2026-08-02 | **The `reopen` leak is closed by measurement, not by code — and the fit that said otherwise was reading a step as a slope** | The queue's next item carried its own instruction, *re-measure before spending a session on it*, and that instruction was right: **240 reopens grow +0.9 KB a cycle after a single 556 MB step on the first**, against an `open` control at +1.8. Decision #90's fix held, and the 2-file and 202-file folders now measure the same, which is what "no longer the folder" looks like. ⚠ **The first analysis reported 5,932 KB/cycle** — a hundred times the entry it was checking, and alarming rather than reassuring, which is the direction that costs a session. It was a least-squares fit across one discontinuity: the series is `330 330 330 330 886 886 … 886 887`, flat-step-flat. Two rules out of it — **print the series before fitting it**, and **falsify the shape, not only the size** (240 cycles rather than 120 is what turned "one step" from a reading into a result; a staircase with a long period would have shown a second, and there is none in 230). ⚠ Nothing was pinned and that is deliberate: seeing a 25 KB/cycle slope needs tens of real decodes at ~0.27 s each, so a gate that could see it would add minutes to every run and one that runs in seconds could not. The **cause** stays pinned by `testSweepDoesNotHoardTheDirectory` (400 sweeps, 300-file folder, 8 MB ceiling); the residual is a measurement in `STATUS.md`, not a test, and it is written down as such. ⚠ Separately noted, not chased: **one range mask holds 556 MB resident**. A budget question, not a bug — it is paid once and never repeats. Gates: **806 / 3708 / 41 of 41 / bench exits 0 on all three frames**. |

| 134 | 2026-08-02 | **A gradient's ramp length goes through \|J·u\|, not √\|det J\| — and the fix is worth 0.002 luma, which is why the call site is unpinned and says so** | The last first-order term in a mask's extent. A ramp has exactly one direction, and the geometric mean of two axis scales is not the scale along it; `mask::lengthAlong` returns \|J·u\| for the ramp's **pre-image** direction, exactly 1 at the identity. Four checks in `tests_mask_geom.cpp`, including the shear, so no answer can come from reading one matrix entry. ⚠ **Two measurements mattered more than the change.** *First*, the fixture was written around the aspect squeeze — the case that rescued the radial mask (#102) — it passed, and **it passed just as happily with the fix reverted**: `perspectiveAspect` alone leaves `Placement::jac` the identity, so both codes multiply by 1 and the frames are byte-identical. *Second*, rewritten around a real keystone with an off-centre angled ramp, the fix moves the frame **0.6753 → 0.6734 luma** on a patch straddling the ramp's edge — against **0.1461** for the radial first-order error — and **the mutation still passes**, because no cell classification can see 0.002 luma at 12 cells or at 20. ⚠ So this ships **knowingly unpinned at the call site**, with the number recorded in `repro/perspective-carries-the-mask.txt` §4b and `UNSOURCED.md` §24 rather than left to be found: a golden-value check with a 0.002 margin fails for reasons other than the defect, and a block that implies coverage it lacks is the failure this repository catalogues. What §4b does pin is a *linear* mask's placement under a keystone, which sections 3 and 4 only asserted for a radial one. The non-uniformity along the ramp stays and cannot go the same way — a projective map preserves cross-ratios along a line and not ratios. Gates: **810 / 3708 / 41 of 41 / bench exits 0 on all three frames**. |

| 135 | 2026-08-02 | **A planning document's duplicates are a correctness problem, not a tidiness one — this queue was offering two shipped features as the next story** | `STATUS.md`'s header had regrown the duplicates a prune removed the day before: two `Last updated` lines, two `Phase:` blocks, and **three** overlapping copies of the queue, each spliced into the next mid-sentence so no seam was visible unless the list was read end to end. That much was known and had been declined by three agents as cosmetic. It is not. The three copies **disagreed**, and merging them is what surfaced it: the incremental brush was the top open story in two of them at "~1-2 sessions" and had shipped on 2026-08-01 (#102, #108); **snapshots / versions** was "the last unbuilt line of M4, unestimated" and had shipped as #99; **grading Balance** was open at "~half a session" citing #97's remainder, which is exactly what #104 built. Four open items, two of them finished work. A recovery point that sends the next session to rebuild `app/Snapshots.swift` is worse than no recovery point. ⚠ **The check that found this was against the tree, not against the file** — every item was confirmed open or closed by grepping for the code and the ROADMAP ✅, which is the only method a queue this old admits. The open list is now two items: the perspective map's curvature (uncosted) and the persisted-key migration (#89, needs sign-off). Closed items collapse to one line each with their decision number, and every measurement removed was confirmed to exist in `DECISIONS.md`, `HISTORY.md` or `ROADMAP.md` first. ⚠ `HISTORY.md` had **771 lines of byte-identical duplicate sessions** — whole blocks pasted three times — deleted under an assertion that no removed line existed nowhere else, plus two label *collisions* where two different sessions shared one id (`2026-07-30k`, `2026-08-01m`). Those were relabelled to free letters with a note that the letter carries **no claim about ordering**, because the sequence could not be recovered and inventing one would be the same failure in a new place. The reliable identifier for a session is its decision number. ⚠ Also corrected: the export panel was recorded as decisions #90-#92 in the queue and #93-#95 four lines later; #93-#95 is right (#90 is directory enumeration, #92 is dehaze). And `research/perspective.md` still described a gradient's ramp length as isotropic √\|det J\| a day after #134 replaced it — a stale row in the document `CLAUDE.md` says to read *before* touching a filter. |

| 136 | 2026-08-02 | **The curvature is costed, the claim that justified the last decision was false, and measuring it turned up a first-order defect in the shipping build** | The queue's next item was the perspective map's **curvature** across a large mask, carried as *uncosted*. Costing it meant measuring it against the exact answer — carry each frame point out to the displayed picture and evaluate the mask the photographer drew — rather than inferring it from cell counts. ⚠ **It is bigger than the record said.** "About a fifth off" becomes, on a 600 × 600 grid: a 0.34 mask under vertical 1.00 differs by **1.0000 coverage at worst**, mean 0.039, over **5.8%** of the frame. Full coverage difference is not a soft rim — it is a pixel the render covers completely and the interface draws clear. It reads as a fifth through `maskcheck` because that instrument counts *cells* and most of the disagreement is inside cells the overlay already calls covered. The error is quadratic in mask size (0.25% of frame at 0.10), and an aspect squeeze measures **exactly 0.0000**, which is the harness checking itself against a map that has no curvature to find. ⚠ **A claim shipped with #134 was false and had reached five documents.** It said `perspectiveAspect` on its own leaves `Placement::jac` the identity, so a ramp changes nothing at all under a squeeze and the frames come out byte-identical. Printed directly, that Jacobian is **diag(0.500050, 1.000100)** — constant, because a squeeze is exactly linear, but nowhere near 1 — and a 6 × 6 patch grid moves in **7 of 36** places under the squeeze (worst 0.0003 luma) and **10 of 36** under a keystone (worst 0.0018, which corroborates #134's 0.0019 by a second route). The *observation* it explained is real: a ramp fixture built around the squeeze does pass with the fix reverted. The reason is duller than the one written down — the term is small everywhere, not absent there — and #134's conclusion, that the call site cannot be pinned, survives unchanged. A wrong explanation attached to a right observation is harder to catch than a wrong measurement, and this one was caught only because a later session printed the matrix. ⚠ **And the measurement found a first-order defect that is not curvature at all.** A linear gradient's **level sets** are wrong under any non-conformal correction. `mask_component.slang` kind 1 projects onto the segment between two endpoints, so its level sets are perpendicular to that segment in *frame* coordinates, while the drawn mask's are perpendicular in *display* coordinates — and a linear map does not preserve perpendicularity. t is a linear **functional**: it transforms by **J⁻ᵀ** while the endpoints transform by J, and the two agree only when J is conformal, which is every case anybody checks by hand. Worst coverage difference **1.0000** under an aspect squeeze and 0.9548 under vertical 1.00; J⁻ᵀ alone removes about three quarters and leaves 0.27, because the perspective divide makes t a **ratio** of linear forms. It is red in the instrument the repository already has: `maskcheck 20 -2.0` on the **shipping** build reports **3 of 27 clear cells leaked, worst 0.1300 luma**. No `repro/` section runs a *linear* mask under a squeeze, which is why it has never been seen. ⚠ **Not added to the scenario file as a red section** — a repository whose gate is red teaches everyone to ignore the gate — but the four-line recipe is in the file as a comment, marked to become §4c when the fix lands. **The exact form is closed and cheaper than the approximation:** t(q) = ⟨n, (q,1)⟩ / (\|u\|²·⟨M₃, (q,1)⟩) with M the whole frame → display map as one 3 × 3 and n = uₓ·M₁ + uᵧ·M₂ − ⟨z,u⟩·M₃ — six floats, two dots and one divide against four floats and one dot, verified to **2.2 × 10⁻⁶** on a 400 × 400 grid across four corrections. Both remaining terms share a first piece (`mask::displayMatrix`), so `ROADMAP.md` costs them together at ~2 sessions. Nothing was built: the tree is the same tree, and the gates were run to prove it. |

| 137 | 2026-08-02 | **A linear gradient's ramp is pulled back through the whole frame-to-display matrix as a ratio of two linear forms, because t is a covector and the endpoints were not** | The queue's next item, found by #136 while costing the curvature. `mask_component.slang` kind 1 projected onto the segment between two endpoints, so its level sets were the lines perpendicular to that segment **in frame coordinates**, while the mask the photographer drew has level sets perpendicular in *display* coordinates. A homography preserves neither perpendicularity nor spacing along the ramp. The endpoints themselves were right — direction exact since #100, length exact since #134 — and that is what made it invisible: everything anybody had thought to check about the ramp was correct. t is a linear **functional**, so it transforms by **J⁻ᵀ** while a pair of endpoints transforms by J, and the two agree only where J is conformal, which is a rotation, a uniform scale and a crop — every case checked by hand. ⚠ **The fix is exact, not closer, and cheaper to justify than what it replaced.** Pulling an affine ramp back through a projectivity gives a ratio of two linear forms: t(q) = ⟨n, (q,1)⟩ / ⟨M₃, (q,1)⟩ with M the whole frame → display map and n = (uₓ·M₁ + u_y·M₂ − ⟨z,u⟩·M₃)/\|u\|². Six floats, two dots and one divide against four floats and one dot. Exact for the homography, the crop, the straighten and the quarter turns **at once**, because all four compose into M — so the mask's own numbers now go through no transform at all and the transform is applied to the *point*. `mask::displayMatrix` is that composition and `mask::ramp` is the two rows; both are in `MaskGeometry.h` beside the map they belong to, and the GPU tests call `mask::ramp` rather than a second copy of the algebra. ⚠ **The denominator is exactly (0, 0, 1) with no correction**, so a photograph at rest renders as it did — asserted, not assumed. Measured: `maskcheck 20 -2.0`, ramp at (0.30, 0.25) 0.6 rad under `perspectiveAspect 1.0`, went from **3 of 27 clear cells leaked, worst 0.1300 luma** to clean, and `repro/perspective-carries-the-mask.txt` §4c pins it both signs plus a squeeze composed against a keystone. ⚠ **The squeeze, not the keystone — the opposite of §4b's instinct.** A keystone's derivative is anisotropic but mildly so over a short ramp; diag(1/g, g) at g = √2 is as far from conformal as this control goes. §4b spent a session concluding the squeeze was where nothing happened. ⚠ **One mutation was green on all 32 scenario checks and all 821 engine checks**: replacing the exact ratio with its affine part — dropping the projective divide. `maskcheck` asserts that cells drawn *clear* are bit-identical and cells drawn *covered* moved, and says nothing about the falloff band between them, which is exactly where that error lives. `testRampIsTheExactPullBack` closes it by asserting the algebra directly against carrying the point out through `fromFrame`, over seven configurations, and it reddens 5 checks on that mutation. **Six mutations tried, six caught.** Gates: **829 / 3708 / 41 of 41 / bench exits 0 on three frames**. |

| 138 | 2026-08-02 | **A radial mask is no longer transported at all — the kernel carries each pixel back and asks the ellipse the photographer drew** | The queue's last geometric item: the perspective map's **curvature** across a large mask, costed at ~1 session by #136 and carrying the one open performance question in the area. The mask travelled to the kernel as an ellipse pushed *forward* into frame coordinates — centre through `mask::toFrame`, semi-axes and angle through the map's derivative at that centre, first as √&#124;det J&#124; (#100) and then as the exact singular values of J·R·diag (#128). The second removed the whole first-order error, which is all it ever claimed. What remained is second order and #136 measured it at a **full 1.0000 of coverage** at the rim of a 0.34 mask under a vertical keystone of 1.00, over 5.8% of the frame — a pixel the render covers completely and the interface draws clear. ⚠ **The fix was already written and was being used as the grading instrument.** Every number in that table came from comparing the shipping answer against the exact one — carry each frame point out with `mask::fromFrame` and evaluate the mask as drawn — so the reference implementation *was* the answer, and had sat in the harness for a session grading an approximation it could replace. The kernel now multiplies each pixel by `mask::displayMatrix` (crop, straighten, quarter turns and the correction as one 3 × 3, the same matrix #137's ramp already used), divides, and evaluates the superellipse. **The centre, the semi-axes and the angle reach the shader untransformed** — they are the numbers in the sidecar. ⚠ **It is a net deletion.** Gone: `toFrame`/`radiusToFrame` from the render path, the angle-as-a-*delta* bookkeeping of #83, the straighten added to the ellipse's angle while the turns are kept out of it (#83 again), and `MaskComponent::rampDen`, which was field for field the bottom row of the matrix now being sent anyway — `testRampDenominatorIsTheMatrix` is a check on that deletion, asserting the two are bit-identical. `radiusToFrame` and `lengthAlong` stay in the tree, tested, marked *not on the render path*, as the first-order answer to measure the exact one against. ⚠ **The performance question is answered by measurement and the instrument had to be built first**: `mask:0` appeared in `orion-bench` four times and was a **brush** every time, so the parametric kinds every local adjustment starts as had no timing at all. Found by trying to answer the question with the instrument that was supposed to cover it and getting pure noise, because the measured kernel was untouched by the change. With a radial node profile added: **1.02 ms with the arithmetic, 1.07 ms with it removed**, on 24 MP — the same number twice, the spread within a condition larger than the difference between them. The pass is bound by writing R16Float over the frame. A keystone on top costs nothing further, because the matrix is applied whether or not it is the identity — deliberately, so there is no second code path and no branch only the uncommon case takes. ⚠ **Everything in the new test passes on the code it replaced except one block, and that block is the point.** The matrix and `fromFrame` were both always correct; what was wrong was using a *derivative* instead of either. So `testRadialIsTheExactPullBack` ends by building the first-order ellipse the old host built and bounding the gap: **0.0000 under an aspect squeeze** — which is exactly linear, so there is nothing to buy, and that row is the measurement checking itself — and between 0.50 and 1.00 under a keystone at 1.00. The GPU case carries the same pairing: the render matches `fromFrame` under a crop, a turn, a straighten and a two-way keystone, *and* disagrees with the first-order ellipse by more than 0.5, so the case cannot go slack silently. ⚠ `repro/perspective-carries-the-mask.txt` §4d is the two-sided proof, 32 → 40 checks: four `maskcheck 12 -2.0` calls, both keystone signs, an off-centre angled mask under a two-way keystone, and one composed with a squeeze. Reverting the host prints **5 of 74, 8 of 74, 3 of 94 and 3 of 94 clear cells leaked, worst 0.1219 luma**. **Five mutations tried, five caught** — skipping the pull-back (GPU, worst 1.0000), dropping the projective divide (0.9693), transposing the matrix (1.0000), kind 1 reading the wrong row (two checks), and the full host revert (four scenario checks). Gates: **844 / 3708 / 41 of 41 / bench exits 0 on three frames**. |
| 139 | 2026-08-02 | **The queue's last item was already shipped, the decision that shipped it was never written, and the ledger is now checked** | The queue in `STATUS.md` offered **"Americanising the persisted keys (#89) — needs sign-off, it rewrites sidecars already on disk"** as the only open item. It had shipped a day earlier as **#112**, and the reason nobody knew is that **#112 had no row in this file** — the migration was cited nine times from `EditHistory.swift`, `Presets.swift` and two test files, and the ledger `CLAUDE.md` calls the record of every settled choice had never heard of it. A session that trusted the queue would have re-run a schema migration over the photographer's sidecars, which is the one item on that queue with a blast radius. ⚠ **Closed by mutation, not by reading**: deleting the `?? float(.legacyColourR)` fallback reddens `colourR still lands on colorR — got 0.18000, want 0.44000` and *and to exactly the same component the British one gave*. The migration is complete — dual reads on all seven mask keys plus `denoiseColour` and `PresetGroup`'s `"colour"`, American keys written and British ones never written back, both-keys-present rules asserted at two levels, and `testAmericanKeyMigration` closing with `CanvasLayout.maskAlpha` on a grid so a renamed key cannot pass by landing on the default. ⚠ **The ledger had five more holes and one of them had a warning pointed at the wrong number.** Never written and never recoverable — `git log -S` over the whole history finds no version of this file that contained them: **#110** (three Swift-layer gaps, 18 citations), **#112** and **#115** (the silent-failure sweep). All three are reconstructed from the code that cites them, `HISTORY.md` and the tests, and each row **says on its face that it is a reconstruction**, because a rebuilt row presented as contemporaneous is the same class of error as the invented constant this repository has a sourcing rule for. **22, 23 and 24** are declared gaps — written without the `#`, since the sigil would claim a decision exists: no row was ever written and **nothing in the tree cites them**, so they are numbers skipped on 2026-07-27 rather than decisions lost, and backfilling them with anything would be invention. ⚠ **Two rows were numbered 71** — the wordmark and batch selection — while the header carried a prominent warning that two rows were numbered **96**. That warning was false in all three of its claims: there is one row 96, there is **one** citation of #96 in the tree, and it means the row that exists. Its "twelve files cite #96" was **one file counted twelve times**, because the count had swept `.claude/worktrees/`, which are checkouts of this same repository. The real duplicate sat unmentioned underneath it. The uncited 71 became **71b**; a number is an identifier and its position in the table still carries the date. ⚠ **`tools/check-decisions.py` is the gate**, listed in `CLAUDE.md` beside the two suites: it fails on a duplicate number, on a cited number with no row, and on an undeclared gap, and it excludes `.claude/worktrees/` for the reason above. **Three mutations, three caught** — re-colliding 71b, deleting row 112 (it names all nine citations), and undeclaring gap 23 — each exiting 1. ⚠ **Nothing in `engine/` or `app/` changed this session.** The tree was already right; the record was not. Gates: **844 / 3708 / 41 of 41 / bench exits 0 on three frames / check-decisions exits 0** |
| 140 | 2026-08-02 | **A control's explanation moves to an ⓘ on the section nameplate, and the panel gets 554 points shorter** | Asked for directly: *"it's kind of bad to stuff text there."* It is. Thirty-odd helper paragraphs sat under the sliders they explained, three lines of 10-point gray each, and the panel paid for them on every draw while the photographer read each one once. **Fourteen sections** now carry the text on an `Engraved.Info` ⓘ at the far end of their nameplate, after the hairline, so every icon lands in one column at the panel's right edge — before the hairline each would sit at the end of its own name and the column would ripple down the panel. ⚠ **`.help` and not a popover, which is a narrowing rather than a shortcut**: it is the idiom this codebase already uses in ten places, needs no `@State` per row, cannot get stuck open, and **VoiceOver reads it for free** — a popover would need all four built and kept working. The cost is a system-set hover delay and plain text, so nothing a photographer needs *while dragging* belongs there. The developer asked for click first and changed to hover; both were built to the same place because the icon carries a `String` either way. ⚠ **The transform was mechanical and the first two attempts were wrong**, which is worth recording because the failure was silent-ish: matching the enclosing `section("…") {` by counting braces forward picked the wrong section on nested content and produced files that would not parse. A backward walk from the paragraph — depth 0 on `}`, and the first `{` at depth 0 is the enclosing brace — is correct, and the script **refuses and exits rather than dropping a paragraph it cannot place**, which is what stopped the first run from deleting text it could not re-home. Six blocks are deliberately untouched: they are interpolated status strings (`"Exporting \(p.done) of \(p.total)…"`, a lens-profile name, a refusal), not explanations, and an ⓘ that explains nothing teaches the photographer the icon is not worth hovering. ⚠ **`detail-tail` nearly became a gate that fails for the wrong reason.** It asserts the Detail panel *overflows* its column, exiting nonzero when nothing does, and it ran under a **1,500-point window override** chosen when the panel's content was 1,701. The move took the content to **1,147**, and the fold with it to **16 points** — one row of margin from going red and blaming the next unrelated panel edit. The override is **deleted, not retuned**: at the default window the fold is **466 points** with the two frames overlapping by **215** and nothing unseen between them, which is a better fold than the override ever bought. The number that matters there is the overlap, not the overflow, and the comment now says so. Gates: **844 / 3708 / 41 of 41 / bench exits 0 on three frames / check-decisions exits 0**, and all three check-scenes render |
| 141 | 2026-08-03 | **The ⓘ shipped drawing perfectly and explaining nothing — `.help` needs a hit-testable view, and a screenshot cannot tell the difference** | Reported by the developer against **v0.4.0-alpha.4, already public**: hovering the icon #140 added does nothing. It was a bare `Image` with `.help(…)` on it. SwiftUI's `.help` needs a view that takes part in hit testing; an `Image` does not, and every other `.help` in this app — ten of them — happens to sit on a `Button`, so the pattern had never been tried anywhere else and looked obviously correct. The fix is two lines: `.contentShape(Rectangle())` over a padded 14-point square, and `.onHover`, which is what installs the tracking area. The hover now also lights the glyph, which is the only feedback available before the tooltip's own delay elapses. ⚠ **The way it was verified is the actual defect, and #140's own gates could not have caught it.** A capture of a working ⓘ and a dead one are **the same pixels**, and the session that shipped it looked at a screenshot, saw the icon, and called it done — against a repository whose stated rule is that a picture is not a test. ⚠ **Two attempts to build a gate for it both failed, and both are recorded rather than quietly dropped.** First, walking the AppKit hierarchy for `NSView.toolTip`: it reports **0 tooltips in a panel with seven icons**, and reports 0 on the *fixed* build too, so it proves nothing — SwiftUI collapses that panel into **134 views containing 2 `NSButton`s**, and `.help` never reaches `NSView.toolTip` at all. That check was written, run, shown to be worthless, and **deleted rather than shipped as a false gate**. Second, drawing the bubble as a `.popover` so a forced-open capture could photograph it: a popover is its own window and does not appear in a capture of the hosting view, so it buys no verification and adds a second failure mode. Also reverted. **What ships is the idiomatic form matching the ten call sites that work**, and the header says in as many words that the verification for this control is a person putting a pointer on it. A control whose correctness is invisible to the suite should say so where it lives, rather than imply coverage it does not have. Gates: **844 / 3708 / 41 of 41 / bench exits 0 on three frames / check-decisions exits 0 /** three check-scenes render |
| 142 | 2026-08-03 | **The ⓘ's text is drawn rather than delegated to `.help`, confirmed by hand on the third attempt — and alpha.5 exists because alpha.4's notes promised it working** | Follow-on to #141. Fixing the hit testing was **necessary and not sufficient**: with `contentShape` and `onHover` in place the developer reported the glyph **highlights** on hover and **still shows no text**, which is the observation that settled it — events reach the icon, and `.help` produces nothing on that machine. So the text is now **drawn**: a SwiftUI `.popover` bound to the hover state. ⚠ **A popover and not an overlay, because the nameplate lives inside a scroll view** — an overlay wide enough to be readable is clipped by the column it sits in, and the clipped part is the end of the sentence. `arrowEdge: .leading` opens it across the panel rather than under the pointer, which would bounce the hover on and off. `.help` is kept beside it at a cost of one line, in case the platform provides it elsewhere; **nothing depends on it**. ⚠ **Three attempts reached the developer before one worked**, and the first two were reported as fixed. Both were signed off on evidence that could not distinguish working from broken: the first on a screenshot showing the icon drawn, the second on reasoning from the ten `.help` call sites that do work. **The thing that finally diagnosed it was a one-bit observation designed to split the space** — does the glyph highlight? — which separated "events do not arrive" from "`.help` does nothing" in a single hover, after two rounds of guessing. Ask for the discriminating observation before shipping the next guess. ⚠ **v0.4.0-alpha.5 was cut immediately rather than at the next feature.** alpha.4 is public and its release notes describe controls that explain themselves on hover, which they did not; a release whose notes are wrong about a shipped feature is not a thing to leave standing while other work accumulates. The site's interface capture was regenerated in the same pass — it was taken on 30 July under a caption reading *"the interface as it runs today"*. Gates: **844 / 3708 / 41 of 41 / bench exits 0 on three frames / check-decisions exits 0** |
| 143 | 2026-08-03 | **One hint window, and it is the instant one — `.help` is dropped rather than kept beside the drawn bubble** | #142 shipped the drawn popover **and** left `.help` in place, on the reasoning that it cost one line and might work where the popover did not. It worked, and the result was **two windows on one hover**: the bubble immediately, the system tooltip after its own delay. Belt-and-braces is not free when both braces are visible. ⚠ **The developer chose the instant one, and the reasoning generalises**: a hint that arrives a second after the pointer does is a hint nobody waits for, and the whole point of moving thirty-odd paragraphs out of the panel (#140) was to make the text available *on demand* rather than *always* — a delayed tooltip is neither. What `.help` owned and the popover now owes is placement, dismissal and screen-edge flipping; AppKit gives the popover the first two, and the third is a real if minor regression, recorded here rather than discovered later. ⚠ **The immediate cause of the mess was a stale-binary reading.** `.help` was reported dead, so a drawn bubble was built to replace it; `.help` was in fact already fixed by the hit-target change in #141, and the "replacement" became a duplicate. **Rebuild and relaunch before believing a negative observation about a UI** — three of this control's four rounds were spent on evidence that was true of a binary nobody was running. Gates: **844 / 3708 / 41 of 41 / bench exits 0 on three frames / check-decisions exits 0** |
| 144 | 2026-08-03 | **The Optics panel says why a lens was not matched — it had no `else` branch at all, and the answer turned out to be a data gap rather than a spelling one** | Raised by the developer as *"how come the camera can't find my lens?"*, which is the interface failing to answer something the engine already knew. `if engine.hasLensProfile { … }` had **no else**: a photograph whose lens the database does not carry showed two bare sliders and no reason for them. ⚠ **Two different failures wanted two different sentences, because they have different fixes.** A file naming a lens the database lacks is a data gap; a file naming **no lens at all** is every adapted and fully manual lens ever made and can never be matched by name — `Engine.h` already said so in a comment nothing surfaced. So the engine now carries `photoLens()`, the lens name **as the file records it**, separate from `lensProfile().lens`, which names the *matched* entry and is empty precisely when the explanation is needed. Plumbed the repeatable way — `Engine` → `orion.h` → `CApi.cpp` → `Engine.swift` → the panel — and the facade comment says why the two strings are not the same thing. ⚠ **The diagnosis is worth more than the change.** The developer's file records its lens perfectly well; it ends `DG DN | Art 023`, and **the bundled lensfun database contains no `Art 023` entry at all** — a 2023 Sigma Art, absent from the data rather than spelled differently. That rules out the tempting fix: no `normalize` rule can match a lens that is not there. The real options are updating the bundled database or letting a photographer **choose a lens by hand**, and the second is now the open item in `STATUS.md` with the reason it cannot be skipped. ⚠ Nothing was guessed at from the lens name: `lookup` deliberately refuses a near-miss rather than applying one optical design's distortion to another's picture, which `tests_io.cpp` pins for exactly this family — a DG DN lens must never match a DG HSM entry. Gates: **844 / 3708 / 41 of 41 / bench exits 0 on three frames / check-decisions exits 0** |
| 145 | 2026-08-03 | **The lens database can be listed and chosen from — the engine half, with the interpolation now shared rather than copied** | #144 established that the developer's lens is **absent from the data**, not misspelled, so no matching rule can reach it and the only honest answer is a list to choose from. `LensDatabase::names()` returns every lens as "Maker Model", sorted and de-duplicated — the data carries one optical design once per mount, and a picker offering the same lens four times is a list nobody trusts. `lookupExact` takes a string that came *out* of `names()` and matches it **exactly**: `lookup`'s longest-containment search exists to survive EXIF spellings nobody controls, and a picked string is not one of those, so anything but an exact hit is a bug rather than a spelling. A chosen lens is **never reported approximate** — the photographer picked it, and telling them it is a near match for their own decision is wrong. ⚠ **`vignetteAt` was extracted rather than copied.** The aperture interpolation — in 1/N, after lensfun's own `InterpolateVignetting` — was inline in `lookup`; a second copy in `lookupExact` is how two paths quietly start giving one lens two answers, and this codebase has paid for duplicated derivations twice. `Lens` is public only so the helper can be a free function both call. ⚠ **A mutation survived the first version of these tests and the reason is the useful part.** The check that matching and choosing agree *to the bit* was written `if (viaExif.found) { … }`, so when the fixture lens was not found it **skipped silently** — deleting `lookupExact`'s vignetting call passed all 852 checks. Two things closed it: the fixture's own premise is now asserted, so a skip is a failure, and a second check counts how many chosen lenses come back carrying non-zero coefficients (**1,452 names, >100 with data at 50 mm f/5.6**), because *two paths that both return zero agree perfectly*. The mutation now prints `0 of 1452`. ⚠ **This is the engine half only**: nothing in the interface can reach it yet, and `STATUS.md` says so rather than implying the feature landed. Gates: **855 / 3708 / 41 of 41 / bench exits 0 on three frames / check-decisions exits 0** |
| 146 | 2026-08-03 | **A hand-chosen lens lives beside the profile it picks, not in `Adjustments` — and the facade lists the database rather than copying it** | The second piece of the picker (#145 was the database half). `Engine::setLensChoice(makerModel)` runs `lookupExact` at **this photograph's own focal length and aperture**, which are kept from the open rather than re-read — picking a lens would otherwise be a second decode of the file for two numbers already in hand. Empty restores the EXIF match, which may itself be nothing. ⚠ **Deliberately not a field in `Adjustments`.** A lens choice is not a per-frame render parameter: it *selects* a profile, and that profile's coefficients already reach the shader as `lensPoly` and `lensVignettePa`. A string in the POD block would cross the facade on every slider tick to carry a value that changes once a photograph, and `Adjustments` has no string precedent to follow. It belongs beside `lensProfile_`. ⚠ **A bad name leaves the previous profile alone rather than clearing it.** A sidecar can seat a choice written against a database that has since changed — losing the correction silently is worse than keeping one the photographer already accepted, so `setLensChoice` reports false and changes nothing. ⚠ **The list crosses as a count and an index, not an array.** ~1,450 strings, and a picker filtering as somebody types wants to read them, not own a copy; the vector is built **once** in a function-local static, because `names()` sorts and de-duplicates on every call and that would otherwise be paid per keystroke. An out-of-range index returns `""` rather than failing, so a caller iterating a stale count reads empties instead of garbage. The choice setter is wrapped in `try/catch` like every other entry point — **no C++ exception may cross the facade, it would terminate the process.** ⚠ **Stopped at the engine boundary, and `STATUS.md` says so plainly**: nothing in the interface reaches any of this yet, the glue in `setLensChoice` has no test of its own beyond the primitives underneath it, and the remaining work is a `lensChoice` string through `DevelopState` — whose decoder is tolerant, so old sidecars default rather than fail to parse, which is the #112 hazard cleared — plus the control itself. Gates: **855 / 3708 / 41 of 41 / bench exits 0 on three frames / check-decisions exits 0** |
| 147 | 2026-08-03 | **A lens can be chosen by hand, end to end — and the scenario proves the correction reaches the picture, not just the panel** | The interface half of #145/#146. `DevelopState.lensChoice` persists it, `Engine.lensChoice` seats it and **reads back what the engine settled on**, and `LensPicker` filters the 1,452 names as somebody types. ⚠ **A name, never an index.** The database is bundled data that will be refreshed, and an index is a number whose meaning changes the day the XML is updated — a sidecar written today would then seat a *different* lens, silently, with the correction still applied. A name that no longer resolves leaves the previous profile standing instead. ⚠ **A filter over a list, not a `Picker`**: 1,450 rows is a menu nobody can use and SwiftUI builds every one; this shows nothing until two characters are typed, then at most twelve matches, and each typed word must appear in any order so *"sigma 24 art"* finds *"Sigma 24mm F1.4 DG DN | Art 023"* without anyone remembering the spacing. The catalogue crosses the facade once — 1,450 calls per keystroke is the cost that does not show up until somebody types quickly. ⚠ **`Engine.lensChoice` corrects itself rather than claiming what was asked for.** `setLensChoice` refuses an unknown name and leaves the previous profile alone, so the property re-reads the engine's own answer; the interface shows what is *applied*, which is the whole reason `refreshLensProfile` is one function called from two places rather than the display name being built twice. ⚠ **`repro/lens-chosen-by-hand.txt` asserts the thing a unit test cannot**: that a name out of the *shipping* catalogue, seated through the *shipping* property, moves a real photograph. **Profiled luma 0.6009 against 0.5683 plain**, choosing again reproduces 0.6009 exactly, and clearing returns to 0.5683 — a profile that seated and rendered identically would be a control that does nothing, which is the defect class this repository has shipped four times. Deleting the `orion_engine_set_lens_choice` call reddens **3 of its 10 checks**. ⚠ The roster backstop (#110) caught the persistence gap immediately — `busyState()` must move every field off its default or the sidecar round trip cannot see it — which is exactly the trap that shipped two dead features before it existed. Gates: **855 / 3708 / 42 of 42 / bench exits 0 on three frames / check-decisions exits 0** |
| 148 | 2026-08-03 | **The performance audit's own premise table was wrong in every row it asserted, and the reason it could rot is that nothing can test a gesture** | The audit item asked for directly on 2026-08-01 opens with a table saying four gestures do not arm the preview graph — `CropOverlay`, `SpotOverlay`, `CurveEditor`, `ColorWheel` — and builds its case on that. ⚠ **All four had been fixed, and the table had never been updated.** Re-derived against the tree: `CropOverlay.swift:179`/`:219`, `SpotOverlay.swift:142`/`:168`, `CurveEditor.swift:282`, `ColorWheel.swift:128`/`:148`, every one real code rather than prose. The table also named `AnalogTrack` as where sliders arm; it contains **no such call** — they arm at `AdjustmentSlider.swift:80`. Starting the audit from it would have meant "fixing" four things already fixed, which is the fourth time this repository's own record has offered finished work as the next job. ⚠ **The first count was wrong too, and in an instructive way**: a plain `grep -c` reports `ColorWheel` calling `beginInteraction` twice and `CropOverlay` calling `endInteraction` twice, because both **mention the function in a comment above the call**. A checker that counts raw occurrences stays green when the only real call is deleted and a comment survives. `tools/check-gestures.py` strips `//` and `/* */` first, and a mutation commenting out `SpotOverlay`'s call is what proved it — it reddens where a naive version would not. ⚠ **It is a grep and the header says so twice.** Decision #110.3 already established that a SwiftUI `DragGesture` closure cannot be driven from either suite: an off-screen `NSHostingView` lays the control out and hit-tests it, but `NSEvent.mouseEvent` through `sendEvent` never reaches the recognizer, and CGEvent-backed events need a real on-screen window and cursor — **deleting `ColorWheel`'s call measured green across the whole suite**. So the honest options were a grep or nothing, and nothing is what there was. It proves the call has not been deleted and claims nothing about whether the gesture arms on the right branch, ever disarms, or makes anything faster — all three are listed in the file as *not* checked, because a green run that reads as more than it is would be worse than no run. **Two mutations, two caught.** Gates: **855 / 3708 / 42 of 42 / bench exits 0 on three frames / check-decisions exits 0 / check-gestures exits 0** |
| 149 | 2026-08-03 | **"Sliders slow" is answered by measuring the path a hand is actually on — clarity drags at 4.2 ms and releases at 56.4** | The audit's *tick, attributed* row. `repro/slider-drag-cost.txt` was opened against a real report and recorded **exposure 9.3 ms, clarity 60.5 ms, dehaze 67.3 ms per tick**, concluding that the preview path *"was never built, so every tick pays full resolution for a picture the hand is still moving"*. ⚠ **It was built, and this file could not see it**: the scenario drove `drag`, which does not arm, so it measured and reported the path nobody is on — for however long it has been since every canvas gesture started arming (#148 re-derived all six). Measured both ways in one run, same frame, same 40 ticks: **exposure 9.4 → 4.5 ms (2.1×), clarity 56.4 → 4.2 ms (13.4×), dehaze 79.0 → 6.5 ms (12.2×)** — 18 fps becomes 235. ⚠ **The answer is not "clarity is expensive."** It is that clarity costs **4.2 ms to drag and 56.4 ms to release**, and the second number is the one a photographer experiences as the wait at the end of a gesture. That is a different problem from the one this file was opened for, it is now stated as such, and nothing was optimised to reach it. ⚠ **Both paths stay measured, deliberately.** Reporting only the armed number would hide a regression in the full-resolution path that every commit, every export and every keyboard nudge still takes; reporting only the unarmed number is exactly what let this file describe a solved problem for weeks. ⚠ **A measuring instrument that drives the wrong path is worse than none**, because its number is precise, reproducible and about nothing anyone does — the third time in this audit that the record and the tree have disagreed, after #148's premise table and #144's lens. Gates: **855 / 3708 / 42 of 42 / bench exits 0 on three frames / check-decisions exits 0 / check-gestures exits 0** |
| 150 | 2026-08-03 | **The measuring protocol is sound and the threat is not what the row said — this bench is immune to a saturated CPU and wrecked by a busy GPU** | The audit's last foundational row read *"2026-07-31 recorded the M0 gate swinging **8.97 → 44.53 ms on an identical binary** with GUI load. Any number this audit produces is worthless unpaired. Decide the protocol first."* ⚠ **The protocol was decided — #116 made the gate read best-of-three and print every round — so "decide it first" was the fourth stale instruction found in this audit**, after #144's lens, #148's premise table and #149's instrument. What the row could not say is whether it *works*, and that is measurable rather than arguable. **Five runs of one binary on a quiet machine: 8.97, 9.09, 8.95, 9.09, 9.08 — a spread of 0.14 ms.** The protocol holds. ⚠ **Then the two-sided half, which changed the rule.** Every core in a busy loop: **8.98 ms, spread 0.06** — *immune*. A second Orion rendering alongside it: rounds of **10.09 / 21.23 / 11.23, spread 11.14 ms**. So "run it on a quiet machine" is imprecise and the precise version is **CPU load is harmless; anything else touching the GPU is fatal** — which is consistent with the original outlier having been recorded under *GUI* load, and is a rule somebody can actually follow. ⚠ **Best-of-three rescued the verdict both times and that is the danger.** Under GPU contention the gate still printed **PASS at 10.09 ms**, with the reassuring caption *"machine noise, not the build"* sitting beside an 11 ms spread — a reader who trusted the headline would never have known the machine was not quiet. The caption now says so above 2 ms, which is a fifth of the measurement and roughly fifteen times the idle spread: far enough outside normal to be worth a sentence, tight enough that an ordinary run does not trip it. Verified both ways — quiet prints the ordinary line, loaded prints the warning while the gate still says PASS. ⚠ **The row's underlying rule survives and is worth keeping**: a number taken beside a busy GPU is not comparable to one taken on a quiet machine, so pairing is still the protocol. Gates: **855 / 3708 / 42 of 42 / bench exits 0 on three frames / check-decisions exits 0 / check-gestures exits 0** |
| 151 | 2026-08-03 | **A cold open is 210.9 ms and the photographer spends all of it looking at the previous photograph, unmarked** | The audit's *cold open* row asked *"decode 36 ms + full render 72–92 ms. What does the photographer see in between?"* — and both halves of the question turn out to be answerable, one by grep and one by clock. ⚠ **What is seen is the photograph that was open before.** `isLoaded` is set true once, at the end of the first successful open (`Engine+Document.swift:86`), and **never set back to false**, so a second open leaves the canvas holding the old render until the new one lands. There is **no busy state anywhere in the application** — no `isBusy`, no `ProgressView`, nothing in the chrome or on the canvas — so nothing marks the window as stale while it is. The first open of a session is the honest case: `isLoaded` is false, and the empty state shows until the picture arrives. ⚠ **The duration is 210.9 ms**, measured over eight repeat opens of `_PIC8220` on a quiet machine (#150's protocol), not the ~108–128 ms this row's own arithmetic of 36 + 72–92 implies. **The fifth figure in this audit found stale against the tree**, after #144's lens, #148's premise table, #149's instrument and #150's protocol instruction. ⚠ **Whether holding the last frame is right is a design question and is not settled here.** It avoids a flash of black, which is why it presumably happened; against that, for a fifth of a second the application asserts a photograph that is not the one being opened, and the case it looks worst in is exactly the one the developer already reports — a frame that comes back wrong, where the previous *correct* picture lingers over it. **Nothing was changed**: the finding is a measurement and a grep, and a busy state on the canvas is a design decision with a screenshot check attached, which is its own story. Gates: **855 / 3708 / 42 of 42 / bench exits 0 on three frames / check-decisions exits 0 / check-gestures exits 0** |
