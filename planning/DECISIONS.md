# Orion — Decision Log

One line per decision. Newest last.

| # | Date | Decision | Why |
|---|---|---|---|
| 1 | 2026-07-27 | Planning docs live in `planning/`, kept concise | Single source of truth for Opus build sessions |
| 2 | 2026-07-27 | macOS first; Windows/Linux later | Smaller test matrix, native Metal + Core ML, ship sooner |
| 3 | 2026-07-27 | **No Rust.** Engine in C++20 | User can't debug Rust. C++ also has the stronger imaging ecosystem (LibRaw, OCIO, lensfun, lcms2) |
| 4 | 2026-07-27 | GPU via Metal; shaders authored in Slang | Best Mac tooling + f16 + ANE path; Slang keeps SPIR-V/HLSL portability open |
| 5 | 2026-07-27 | Engine = compute DAG, one shader per node, `rgba16f` linear Rec.2020 | vkdt's proven design; avoids darktable's dual CPU/OpenCL maintenance tax |
| 6 | 2026-07-27 | Scene-referred pipeline, exactly one display transform, no Lab | Avoids artifacts/hue shifts of display-referred; industry direction |
| 7 | 2026-07-27 | Default tone mapper: AgX-family sigmoid, 2–3 sliders | Great defaults without filmic's fiddly multi-tab UI |
| 8 | 2026-07-27 | Demosaic: RCD default (MIT impl), bilinear for thumbs | Near-AMaZE quality at PPG speed, excellent GPU fit |
| 9 | 2026-07-27 | XMP sidecars = source of truth; SQLite = disposable rebuildable index | Folder-first workflow, no catalog lock-in, but instant filtering on large shoots |
| 10 | 2026-07-27 | Avoid exiv2 (GPL-2.0); use Apple ImageIO for metadata | Prevents forced open-sourcing / commercial license fee |
| 11 | 2026-07-27 | Sony ARW is the only priority format for v1 | User's camera; dogfooding beats breadth |
| 12 | 2026-07-27 | Engine is a library with a narrow C API; UI is a client | Makes UI choice reversible and Windows port tractable |
| 13 | 2026-07-27 | **No Vulkan.** Metal only on macOS | Vulkan needs ~1000 lines of boilerplate to draw a triangle; unmaintainable for a solo dev |
| 14 | 2026-07-27 | Maintainability is a hard constraint: one node = one 50–150 line shader; adding a feature = 3-file change | Solo developer must be able to hand-edit any file months later |
| 15 | 2026-07-27 | Build bilateral grid + Bilateral Guided Upsampling as first-class DAG node types in M1 | General escape hatch for the 16 ms budget — decouples algorithm cost from slider responsiveness (MIT/Google, open-source impl) |
| 16 | 2026-07-27 | `CLAUDE.md` at root + `planning/STATUS.md` updated every session | Survives context clears and session wipes |
| 17 | 2026-07-27 | **No bespoke/Skia UI layer** | 1.5–2.5 engineer-years for macOS alone; Zed took 5 yrs + 1M LOC funded. Impossible solo |
| 18 | 2026-07-27 | No Dear ImGui for product UI — but **use it for internal debug tooling** | Its own FAQ says skinning is limited, built for debug tools. Perfect for a pipeline inspector |
| 19 | ~~2026-07-27~~ | ~~UI shell = SwiftUI/AppKit~~ **SUPERSEDED by #21** | Was based on two errors: HDR is not Qt-exclusive-missing, and the platform-integration argument killed bespoke, not Qt |
| 20 | ~~2026-07-27~~ | ~~`@Observable` view models~~ **VOID** — SwiftUI not chosen | |
| 21 | ~~2026-07-27~~ | ~~UI shell = Qt 6 / QML~~ **SUPERSEDED by #25** | Was correct when the criterion was "stay in one language". Priorities shifted to stunning look + minimal bloat, where native wins |
| 22–24 | ~~2026-07-27~~ | ~~Qt LGPL / styling rules~~ **VOID** — Qt not chosen | Note for the record: Qt is **not** paywalled. LGPLv3 is genuinely free for closed-source commercial use with dynamic linking |
| 25 | 2026-07-27 | **UI shell = SwiftUI/AppKit + C++ engine** (final) | Stunning look and minimal bloat are the top two priorities; native wins hardest on both. ~5–15 MB bundle vs Qt's 40–60 MB; Apple materials/vibrancy/typography free vs building a visual language from scratch. Zero licensing obligations |
| 26 | 2026-07-27 | View models = plain `@Observable` objects, zero SwiftUI types inside | Any panel re-hostable in AppKit when SwiftUI hits a density cliff. Cheap on day one, expensive to retrofit. Sets macOS 14+ floor |
| 27 | 2026-07-27 | Narrow hand-authored POD facade at the C++↔Swift boundary | No templates, no move-only types, **no exceptions escaping (they terminate the process)**. Only boundary stable across Swift releases |
| 28 | 2026-07-27 | Rejected: C++ webview shells (`webview/webview`, saucer), WebGPU, WebAssembly, Go | Webview canvas layering is fragile and the canvas *is* the app. WebGPU is an abstraction over Metal (no speedup). Wasm is slower. Go has no imaging libraries, cgo overhead, GC in the pixel path |
| 29 | 2026-07-28 | **Clip the mosaic to a single white level after white balance**, in `linearize`, before demosaic | A blown pixel arrives as (S, S, S) and white balance turns it into the gains — magenta on every clipped light. dcraw's default highlight mode; before demosaic because RCD interpolates across an unclipped neighbour and leaves a fringe rather than a clean edge. Costs the headroom highlight reconstruction could have used, which is the right trade: the cast was on every frame, the reconstruction is optional |
| 30 | 2026-07-28 | **A control's readout is its own reset**, colored by whether it has moved | One affordance says which controls are modified *and* what is clickable. A separate change dot in the margin would say it twice, smaller and harder to hit. The base is per photo, not per type: white balance is the camera's reading |
| 31 | 2026-07-28 | **Export converts through ColorSync, tagged sRGB at source** | A chromatic adaptation typed in here is a cast waiting to happen, and ColorSync's definitions are what every other application reads the file against. Tagging the pixels as the destination instead of converting them is how a file opens oversaturated |
| 32 | 2026-07-28 | **Export metadata comes from ImageIO reading the RAW**, not from LibRaw or exiv2 | exiv2 is GPL (#10). ImageIO reads the container's own EXIF/TIFF/GPS blocks, so the export carries what the camera recorded. Orientation is dropped on purpose — the geometry node already applied it, and copying the tag turns the picture twice |
| 33 | 2026-07-28 | **American spelling throughout**, code and docs | The developer's own English. `denoiseColour` is still read from old sidecars |
| 34 | 2026-07-28 | **Edits autosave: coalesced writes plus a flush on `willTerminate` and on resign-active** | `saveDevelop` ran only when switching photos, so open-one-file-edit-quit lost everything. Two triggers because they fail differently: coalescing covers the crash and the logout, `willTerminate` covers the ⌘Q that lands inside the coalescing window. The queued state travels *with* its URL, so a write cannot be misfiled during the ~50 ms a photo switch spends between `current` moving and the decode finishing |
| 35 | 2026-07-28 | **Lens corrections autoscale to keep the frame full** | poly3 pins the corners, not the edges: at k₁ = −0.35 the edge midpoints fetch 325 px past a 6024 px frame and the clamp smears the border column into a band. lensfun, darktable and Lightroom all zoom for this reason. Computed on the host in `pipe/LensGeometry.h` — it is a question about every pixel at once, which a shader cannot answer |
| 36 | 2026-07-28 | **A disabled node's consumers are told it is disabled** | `Pipeline::resolve` walks a disabled node back to the last live producer, which is correct for pixels and wrong for anything read as something other than pixels. `develop_linear` was reading the colour matrix output as log2 luminance and as guided-filter coefficients. Any node whose input carries a *meaning* rather than an image needs a flag, not just a binding |
| 37 | 2026-07-28 | **Every bench probe asserts a magnitude, against its own baseline, and gates the exit code** | "Did this change something" passed for a blacks slider delivering 39% of its effect. Floors are ratios against a reference control on the same frame, so they survive a change of photo. A known defect may be `WAIVED` with a reason string that prints every run — absent from the output is not an option |
| 38 | 2026-07-28 | **The graph's tail is eight bits for the screen and sixteen only around an export** | The drawable is `bgra8Unorm`, so `rgba16f` through the two largest nodes bought precision nothing could display, at 3.0 ms of a 16 ms budget and 383 MiB. Export widens it, reads, and narrows again. The display node dithers (ordered, Bayer 4x4) because geometry resamples its output and quantises a second time, and two roundings of a gradient is where contouring comes from. The bench asserts the two paths agree to better than one 8-bit step |
| 39 | 2026-07-28 | **Intermediates are not aliased, and that is deliberate** | Render-graph resource aliasing would cut 4.2 GiB to roughly 600 MB, but a cached node's output has to stay resident — aliasing and per-node caching are mutually exclusive. The footprint *is* the cache, and it is what makes an exposure drag 3 nodes instead of 28. The answer for smaller machines is the preview-ROI path, which shrinks every intermediate, not aliasing |
| 40 | 2026-07-28 | **Geometry stays a separate node from the display transform** | Fusing them would save ~2 ms, but `geometry.slang` resamples *display-encoded* pixels on purpose: averaging unbounded scene-linear values blooms a specular edge, which is why film and VFX resample in log rather than linear. Fusion forces scene-linear resampling and merges two small shaders into one large one, against the maintainability constraint. Revisit only if the budget gets tight — there is 6.4 ms of headroom |
| 41 | 2026-07-28 | **Grading zones are Gaussian bands on log2(Y/0.18), and the offset scales with luminance** | Linear-luma zones put middle gray at 0.70 shadows, so the shadow wheel graded the whole picture and the highlight wheel was inert. Same partition-of-unity construction as the tone bands, so a photograph has one idea of where its shadows are. Scaling the offset by Y makes a wheel a constant chromaticity shift at every exposure (0.1077 relative chroma at -3 EV against 0.1079 at +3), and makes the deep-shadow clamp that broke zero-sum unrepresentable. `k = 0.25` is derived: saturation = 1.5k/(1+k), so full travel is 30% from neutral |
| 42 | 2026-07-28 | **Bench probes gate on mean absolute per-pixel movement, not on a summary metric** | A grading wheel rotates hue at roughly constant saturation, so mean luma, mean chroma *and* mean saturation each reported a working wheel as doing nothing — three instruments, same blind spot, because a frame mean cancels a rotation. The summary is still printed for insight into *what* changed; movement decides *whether* it did. Floors are half the smaller ratio measured across two very different sample frames, because one frame was not enough — four probes calibrated on a night sky tripped on a lit forecourt |
| 43 | 2026-07-28 | **Orion is open source. Paid distribution is out of scope.** Supersedes the "undecided by choice" position in STATUS's blocked list | Settles the question that gated several others. The existing rule — avoid GPL, dynamically link LGPL — is kept anyway, because it costs nothing today and it is what keeps the decision reversible. LibRaw is dual LGPL-2.1/CDDL-1.0: taking it under CDDL permits static linking, which is the escape hatch if dynamic linking ever becomes awkward for packaging |
| 44 | 2026-07-28 | **Do not redistribute Adobe's `.dcp` profiles, and do not ship RawTherapee's either** | Adobe publishes no redistribution grant for the profiles bundled with DNG Converter, and projects that derive from them tell users to generate their own copies rather than share outputs. RawTherapee's bundled profiles are user-submitted with no stated licence of their own, inside a GPL-3 project. The DNG *specification* is separate and carries a royalty-free patent grant, so writing a DCP **reader** is fine — but any implementation distributed under that grant must carry a notice crediting DNG technology licensed from Adobe. Put it in the about box before any DNG code ships |
| 45 | 2026-07-28 | **OPEN: LibRaw's built-in colour matrices may themselves be Adobe-derived** | LibRaw's `cam_xyz` table descends from dcraw's `adobe_coeff`, whose matrices were widely understood to have been extracted from Adobe DNG Converter. If so, Orion already ships Adobe-derived colour data — the same position as darktable, RawTherapee and every other dcraw descendant, and unremarkable for an open-source project. **Evidence, checked 2026-07-28:** two independent documenters describe the table that way — Doug Pardee's ProjectDcraw notes dcraw was updated "10–14 days after Adobe releases a new version of Adobe DNG Converter", and Elle Stone's annotated dcraw source calls it "the Adobe-dng camera coefficients". Consistent, but still second-hand: no primary statement from dcraw's author was found. Stays OPEN — it would need answering before any change of distribution model, not before the next commit |
| 46 | 2026-07-28 | **A silent +1.2 EV baseline exposure, and base contrast 1.45** | Orion opened a daylight frame 1.3x darker than the camera's own JPEG, which read as flat and washed *and* pushed most of the picture into the grading shadow zone — one root, two complaints. The DNG spec's `BaselineExposure` (tag 50730) is the mechanism, applied silently on open, which is why the Exposure slider still reads 0.00. Value fitted against two independent references over six patches per frame: +1.20 EV / 1.45 is the joint best on both frames that carry signal, and mean error on the daylight frame fell 0.1543 -> 0.0194. LibRaw does not carry the tag for native ARW, so it is measured rather than read. See research/camera-profiles.md |
| 47 | 2026-07-28 | **Bench probe floors are calibrated across three frames, not one or two** | A night sky, a lit forecourt and a bright daylight cityscape. One frame tripped four probes on the second; two frames tripped six on the third — a bright frame genuinely has no deep blacks, little noise and few shadows, so those controls move less in it and that is not a regression. Half the minimum ratio over three very different pictures |
| 48 | 2026-07-28 | **The camera profile gets the DNG spec's HueSatMap stage, applied in linear ProPhoto HSV** | A 3x3 matrix is linear, so it cannot correct an error that only appears where a stimulus is saturated and narrow-band — no consumer sensor satisfies Luther-Ives. Blue sky is the worst case and Orion rendered it violet. The spec's answer is a hue/saturation table, and Orion builds one for real (90 x 25, `ValueDivisions = 1`, spec entry and loop order) rather than special-casing blue, so a `.dcp` reader later is a reader and nothing else. **`ProfileHueSatMapEncoding = 0` requires linear ProPhoto HSV**, not the working space's HSV, or the cells sit at the wrong hue angles the day a real profile loads — the node converts in and out. Table populated by a correction fitted on one hue region against two independent renderings: -8 degrees, saturation x1.05, centred at 250 degrees over a 60-degree half-width. Sky R/B 0.622 -> 0.451 against a target of 0.450. Costs one full-res pass, upstream of exposure, so the gate is unmoved at 9.37 ms p95 |
| 49 | 2026-07-28 | **Lens corrections read lensfun's database, and do not link lensfun** | The maths was never the missing part — the models were already implemented, tested and running on the GPU; what was missing was the measured numbers, and those sit in XML. Linking the library would add an LGPL-3 dependency, a build step and a second implementation of the same polynomials to obtain a value that can be read directly. The database (1,558 lenses) is vendored under `data/lensfun/` unmodified with its CC BY-SA 3.0 licence; attribution and share-alike apply to the data, not to Orion's code. The shader now evaluates ptlens, of which poly3 is the a = c = 0 case, so both of the database's distortion models land in one kernel |
| 50 | 2026-07-28 | **A confident wrong lens profile is worse than no profile**, so matching is conservative | It distorts the frame and reports that it measured it. Names normalize to letters and digits only (which collapses `f/`, case and punctuation), match by containment either way with the longest winning, and are refused below eight characters — `50mm` is inside a hundred entries and identifies none of them. A match on a differently-spelled name is flagged `approximate` and the panel says so. The developer's own Sigma 24mm F1.4 DG DN is not in the database and correctly reports nothing rather than borrowing the DSLR DG HSM entry, which is a different optical design |
| 51 | 2026-07-28 | **A profile replaces the manual sliders rather than adding to them** | Two sources for one coefficient is a control that fights the data: the profile knows what this lens does at this focal length, and a slider on top would be undoing a measurement by feel. The sliders grey out while a profile is on. Chromatic aberration stays manual in both cases — the database's TCA figures are per-copy, sample variation is the same order as the aberration, and a wrong one adds coloured edges |
| 52 | 2026-07-28 | **Exports strip GPS unless asked otherwise** | `ImageWriter` copied the whole GPS block onto every export, and the panel had no metadata control at all — a photo taken at home published the home coordinates to anyone who downloaded it, silently, with nothing in any viewer to say so. Three-way control (keep all · strip location · strip everything), defaulting to strip location, and the default is asserted in the suite because the failure mode is invisible |
| 53 | 2026-07-28 | **The generated design tokens are the palette; the hand-written mirror is deleted** | `DesignTokens.swift` is generated from `design/tokens.json` and nothing imported it — the app re-declared `Palette` by hand, with two constants renamed, one dropped and two invented. The generator also emitted Display P3 while the mirror built sRGB, so the app rendered colours the token file did not specify. Tokens are authored as sRGB hex, which is what the HTML mockup renders and where the palette was judged, so the generator emits sRGB and gains a numeric `Components` enum for the consumers that cannot take a SwiftUI `Color` (the Metal clear colour, the canvas surround) |
| 54 | 2026-07-29 | **Brush masks are reinstated into M4**, reversing the v1 cut recorded in ROADMAP and FEATURES | Asked for directly by the developer. The original cut named three costs — stroke capture, edge-aware snapping, and mask storage — and two of them turned out not to be costs. **Storage is a list of dab centres, not a raster**: a few kilobytes, resolution-independent, and exact under crop or rotation because each centre goes through the same transform the image does, where a raster mask would be 24–120 MB at 24–60 MP and compound blur every time it was resampled. **Edge-aware snapping is the guided filter Orion already has**, built and tested for dehaze — masking.md §4 is a second input binding on it, not a new algorithm. What remains is stroke capture, which is one kernel and the canvas gesture work the gradient overlay just paid for. The kernel and its tests landed the same day the decision was reversed; the interface has not. Recorded rather than quietly built, because ROADMAP and FEATURES both stated the opposite and two copies of "what we are building" disagreeing is finding #14 of the outside review |
| 55 | 2026-07-29 | **One brush node serves all four mask kinds, chained after the gradient rather than replacing it** | The graph cannot swap a node's kernel per render, and writing into a node's output from outside the graph is worse than one extra pass. Chaining works because a pass with no dabs is the identity — a property the suite already pinned, and which is now asserted over *every* pixel rather than one, because that is what the pass-through actually rests on and an off-by-one would surface at an edge. So: kind 0 gradient writes 1.0 and the brush passes it through; kinds 1 and 2 pass the gradient through; kind 3 starts from empty and lays the stroke. ⚠ The risk was never the brush — it is that every existing gradient mask now reaches `develop:linear` through a new node, so the three pre-existing measurements were re-taken and are exact: 0.4703, 0.1981, 0.4110 |
| 56 | 2026-07-29 | **Brush dab centres live outside `Adjustments`; the struct carries only a revision counter** | `Adjustments` is compared field by field on every slider tick to decide what to redirty. Carrying a stroke through that comparison would make every tick walk thousands of points. `DevelopPipeline` owns the list, set through its own facade call, and `brushRevision` — a single int — is what tells `apply` the mask is stale. The trap this creates is real and is documented at both the C and Swift boundaries: change the points without bumping the revision and the picture stops following the hand, with nothing in the code looking wrong. `Engine.setBrushStroke` therefore does both, so no caller has to remember |
| 57 | 2026-07-29 | **The mask overlay is view state, not edit state** | It is how the photographer is *looking* at the picture, not a change to it. So it is deliberately absent from `DevelopState`: it never reaches the XMP sidecar, never enters undo history, and never follows the photo to another machine. `export()` forces it off around the write and restores it afterwards, including when the export throws — an export with the overlay on would write a red-tinted photograph and nothing in the file would say why. ⚠ That guard is correct by construction but has no test |
| 58 | 2026-07-29 | **The landing site is dependency-free static files in `web/`, deployed by GitHub Actions to Pages** | React and Framer Motion were scaffolded and reverted. The page has no interactivity by explicit requirement, so React's state-to-UI core is unused, and no gestures, so Framer Motion reduces to one `IntersectionObserver`. What they would have cost is a build step, a lockfile in a C++ repository, and Vite's `base` trap — the default emits absolute asset paths that 404 on a project page **while the deploy still reports success**. That failure mode already occurred once here via `.gitignore`'s `orion-*.jpg` rule silently swallowing `web/img/*.jpg`: green push, green deploy, every image missing. Rule that came out of it: a green deploy is not proof — verify the page *and every asset* returns 200 |
| 59 | 2026-07-29 | **The landing page's static state is the finished page; JavaScript rewinds it** | The redesign adds two scroll-scrubbed scenes — the hero develops the raw as you scroll, a gradient mask sweeps a sky — and both are gated on an `html.js` class the script adds as its first act. Without JavaScript, or under `prefers-reduced-motion`, nothing is hidden and nothing is flat: the developed photograph, the placed mask and the final readout values are what the HTML itself contains. The old page had this backwards — reveal-hiding was unconditional and only JavaScript could undo it, so a no-JS visitor got a blank page |
| 60 | 2026-07-29 | **Landing page display face is Bricolage Grotesque, self-hosted in `web/fonts/`** | The page needed real display weight and the system stack cannot carry a headline at 9 rem. Self-hosted (77 KB variable woff2, latin) rather than loaded from a font service, for the same reason the app has no accounts: no third-party request, works when its owner is the only server left. Chrome discipline unchanged and now written down: teal for live numbers only, plus one new ink — film-rebate amber, from the app's own `star` token, for the frame counters that number the sections, and nothing else |
| 61 | 2026-07-29 | **The landing hero is a viewfinder, and the title is the name** | The sentence headline read as a generic hero. Now the page opens inside a camera: frame brackets, a thirds grid, an AF point that turns teal the moment the develop lands, and ORION set in Michroma — the wide engraved-on-the-body lettering cameras actually use — where the eyepiece display sits. Scrolling still develops the raw; keep going and you push through the finder, the overlay scaling past the eye while the photograph stays. The lede and the numbers moved to a strip below the hero so the finder stays clean. Michroma is self-hosted like the other two faces (11 KB) |
| 62 | 2026-07-29 | **Mask groups are one component kernel instanced per node, folding left from a written zero** | Decision #55's two-node chain (gradient + brush, one always idle) existed only because the graph cannot swap a kernel per render; a `kind` branch inside one kernel is what was actually wanted, and the merge deleted the pass-through contortion — and fixed a fourth dead control it had created (invert never reached a brush, because the gradient node held the invert and the brush node discarded its output). The group folds left in listed order per research/masking.md §6 — add is `max` (no buildup in the overlap, which is the section's whole point), subtract `α₁(1−α₂)`, intersect `α₁·α₂` — starting from a `mask:base` node that writes 0, the additive identity, so the first component needs no special case and a group of one runs the same code as a group of four. `kMaxMaskComponents` is 4: each live component is a full-res R16F pass (~46 MiB at 24 MP), so the cap is a memory number, not a concept. Compose ops and the falloff live in `ops/mask_ops.slang` — the falloff had been two identical copies each claiming to be shared. Both compose mutations (add→screen, invert-skips-brush) die |
| 63 | 2026-07-29 | **Film-rebate amber enters the app as structure only, never near the photograph** | The landing site numbers frames in amber (`star`, #F0C674) and the app had no tie to it. Amber now marks section nameplates that hold a moved control, the selected tool plate's top edge, and the histogram's clipping flags — all at the far end of the window from the canvas. It is kept off the canvas surround, off swatches and out of the colour panels, because the neutral surround exists so colour can be judged and a warm ink beside the picture biases that judgment. One ink, two roles: the rebate amber and the star rating are the same mark, the frame's own index |
| 64 | 2026-07-29 | **Version tracks the milestone; `v1.0.0` is reserved for FEATURES' v1 column** | Asked for a "v1 build"; shipped `v0.4.0-alpha.1`. Minor is the milestone in flight, so M4 closing gives 0.5.0. `FEATURES.md` still lists luminance/colour range masks, spot removal, presets, copy/paste sync, batch export and AI subject selection as v1, and a build labelled v1 promises them. The number cannot be reused; the rule can be followed by anyone |
| 65 | 2026-07-29 | **Runtime resources resolve from the bundle when there is one, and from the build tree otherwise** | `ORION_SHADER_DIR` and `ORION_DATA_DIR` are absolute paths into the source tree — correct for the tests and the bench, and fatal for anything installed: a copied app found no metallibs and died on the first open. `src/ResourcePaths.cpp` prefers `Contents/Resources` when it exists and falls back to the compile-time path, so one rule covers both and the bench needs no special casing. Verified by hiding `build/shaders` and `data/lensfun` and rendering with the packaged app — with the control that the build-tree app fails on the same run, which is what says the test was real rather than the fallback quietly working |
| 66 | 2026-07-29 | **Auto-enhance replaces the five controls it owns, and resets them before measuring** | Auto was not idempotent: press one left the frame corrected, press two measured *that* frame, derived a different look from it and solved the tone controls differently — +2.25 EV with lift 0.16, then +2.99 EV with lift 0.00. It also falsified the code's own comment, which says the look must respond to the photograph rather than to the correction; from the second press onward it responded to the correction. Auto now zeroes exposure, whites, blacks, lift and clarity before its first measurement, so it is a function of the photograph and pressing it twice is a no-op. Only those five: white balance, contrast, the crop and the curve are measured *through* and never discarded. Second cause, fixed with it: the solver ran a flat six passes, and because every step undershoots by construction (see `kDamping`) a frame far from the anchor ran out of passes short of it. It now stops when the median is within `kSettled` of the anchor, capped at 24 — the sample frames need 6, 11 and 17 |
| 67 | 2026-07-29 | **A control that changes the picture goes through `Engine.edit`, never a bare assignment** | Auto set its five sliders by direct assignment, with a comment claiming that kept them on the path an ordinary edit takes. It did not: `edit(_:_:)` is what records history, and a bare assignment records nothing — so Auto left no entry and the next undo stepped past it to the edit *before* it, appearing to discard everything the photographer had done. The rule is now explicit because the failure is invisible: the picture updates, the control shows the new value, and only the undo stack is wrong |
| 68 | 2026-07-30 | **Every piece of graph state that is not an `Adjustments` field is fanned out to both graphs by `orion::Engine`, not by the facade** | `setAdjustments` already sent the adjustment block to the full graph and the preview, and said so in bold. Brush dabs, raster mattes and creative LUTs are not in that block — they went through `developMutable()`, which is the full graph alone — so a painted mask vanished for the length of every drag and came back when the hand stopped. Worst on exactly what the preview exists for: local exposure through a brush mask is a slider. The routing belongs in `Engine` because that is the only object that knows both graphs exist; the facade calling `developMutable()` for anything stateful is now the smell. **One deliberate exception, `setWideOutput`**: sixteen bits exist for export, export reads `develop()`, and widening the preview's tail would reallocate two textures for a picture nothing measures — the reason is at the call site so it does not get "fixed" |
| 69 | 2026-07-30 | **`DevelopPipeline::reload` clears the state a reload cannot replace** — the brush dabs and the mattes | A reload is a different photograph through the same compiled graph, which is every frame of a folder from one camera. It re-pushed every parameter block and left the two things that are not parameters where they were, so opening the next photo with a saved Subject row rendered the *previous* photograph's subject — full coverage, right coordinate space, wrong picture. ⚠ `STATUS.md` had asserted the opposite for two sessions, and it was true only because `Engine.assign` happens to re-send every stroke on open. An invariant that holds because a caller does the right thing is not an invariant, and it belongs in the object that owns the state |
| 70 | 2026-07-30 | **A point on the displayed picture reaches the frame through `displayedToFrame`, never through a hand-rolled inverse** | `Engine::sampleAt` undid the quarter turn and nothing else, so the colour-mixer's scene sample read whatever sat at that fraction of the *uncropped* frame: the yellow car picked band YELLOW at 58.8 degrees uncropped and MAGENTA at 303.3 under a crop, and the drag then moved a band the car is not in. `mask::toFrame` already composes the crop, the straighten and the turns and is tested against renders; a second transform beside it is a second thing to keep in step. Same rule as #36 — the failure was not a crash but a plausible wrong answer |
| 71 | 2026-07-30 | **The hero wordmark reads through a centre-spot ND, not a scrim, and the type carries a contact edge instead of a glow** | ORION sits on the blown showroom of the plate: measured, the background behind the wordmark peaked at L 1.000 with 15% of the box above L 0.5, so white type on it was **1.0:1** — invisible, which is what the developer kept reporting. The fix is the filter a photographer would screw on. Compositing neutral black at 0.56 alpha *is* a multiply by 0.44, so it scales light rather than adding ink: the clipped highlight comes down about a stop and a half while the asphalt, already at L 0.008, does not move. The photograph keeps its shape and the flanking cars keep their colour — a flat wash would have muddied all three. Elliptical, core held out to 52% because the tracked wordmark is wider than the viewport and a tighter core left the N standing on the white car unfiltered. Worst case 1.0:1 → **3.64:1**, mean 4.6:1 → 13.6:1. The two text-shadows it replaces were a 34px and a 70px blur, which light the area around a letter without ever defining it; display glyphs in a finder carry a thin dark keyline so they survive whatever the scene is doing, and that is what they carry now. No `mix-blend-mode` — alpha-black is the same arithmetic with one less compositing layer. Rule the measurement earned: type over a photograph is a number, not an opinion; sample the plate before arguing about the shadow |
| 71 | 2026-07-30 | **A selection of one photograph is not a selection**: a batch acts on everything in view until two are chosen | The photo on the canvas is always in the set — every route to a new photo collapses the selection onto it — so one is the resting state of the interface rather than a decision. Reading "act on the selection whenever it is non-empty" makes Export All silently export a single frame, for every user, on every folder, and it looks like the button being broken. The alternative is a flag recording whether a selection was deliberate, which is a second piece of state that can disagree with the first; counting needs none, and the filmstrip shows a count only once it means something, so what a batch will do is what the strip says it will do. Lightroom draws the same line. ⚠ Two supporting rules with the same character: a modified click opens nothing (forty frames chosen is not forty raw decodes), and the open photograph cannot be command-clicked out of the set (its settings are what a sync copies *from*) |
| 72 | 2026-07-30 | **The colour range mask measures Euclidean distance in Oklab chromaticity (a/L, b/L), and excludes lightness** | `masking.md` §4b had deferred the choice to CIE76 or CIEDE2000; both were rejected. CIEDE2000 is scoped to near-threshold differences (ΔE ≲ 5) while a photographer drags a tolerance at 10–40, and any monotone miscalibration is absorbed by the slider anyway — what a slider cannot fix is the *shape* of the iso-distance surface, and there CIELAB's documented blue-to-purple bend is the flaw that bites, since sky is the first thing anyone reaches a colour mask for. CIEDE2000 also has genuine discontinuities in its mean-hue handling (Sharma, Wu & Dalal 2005), and a discontinuous distance feeding a smootherstep prints a seam across a smooth gradient — disqualifying for a mask before any argument about line count. Oklab (Ottosson 2020; W3C CSS Color 4 `oklab()`) fixes exactly that at Euclidean cost. ⚠ **Chromaticity only, because the identity is exact**: Oklab's nonlinearity is a pure cube root, so scaling the input by k scales L, a and b by k^⅓ and a/L, b/L are *invariant under exposure* — which is what makes the control well-defined on a scene-linear input that is unbounded above 1.0 and has no honest reference white. Lightness is §4b's band, composed with intersect. The target is stored as RGB and converted **in the kernel**, so the target and the pixel cannot disagree about what Oklab is |
| 73 | 2026-07-30 | **A spot is placed and adjusted by dragging, and both of its handles are on the canvas** | It was a click that placed a disc, chose the source for you and then showed neither. Healing is a judgement about *where the replacement comes from*, so the source has to be draggable; the automatic source survives as the starting position, because a click that immediately does something sensible is worth keeping as a first guess. ⚠ This needed `mask::fromFrame`, the inverse of the transform a spot is stored through — and the risk there is the **order**, not the algebra: the inverse must undo turns, straighten, crop in that sequence, and applying the forward order with negated angles is exactly equivalent whenever at most one of the three is active, which is every case anybody checks by hand. ⚠ Two hit-test orderings are load-bearing and both are tested: the **source** wins where the two discs overlap (they overlap at any useful radius, and the source is the handle with no other route to it), and **later spots beat earlier ones**, matching the draw order so the spot being looked at is not the one that cannot be adjusted. A move is labelled "Move spot" rather than "Spot" because `EditHistory` coalesces same-labelled entries, which merged a placement with a later move and made undo delete the spot |
| 74 | 2026-07-30 | **The brush erases, and the polarity belongs to the dab** | A component accumulates every stroke ever laid on it, so per-stroke polarity is not enough — the component has to remember which of *its dabs* added coverage and which took it away. It rides in the dab texture's third channel (RG32Float → RGBA32Float, 256 KB a component against 128). ⚠ **Erase is destination-out**, `a -= cov·a`, the exact inverse of paint's source-over `a += cov·(1−a)`: painting takes the alpha a fraction of the way to one and erasing takes it the same fraction of the way to zero, so the two are reversible against each other. Subtracting the coverage outright drives the alpha negative wherever a slow hand lingers, and the `saturate` at the end hides that as a hard hole in a soft brush. ⚠ **Migration is a parallel array, never a third interleaved number**: `brushStroke` is a flat list of floats in the sidecar, and re-interleaving it would read every stroke saved before erasing existed as garbage — silently, because a scrambled stroke is still a valid stroke. There is deliberately no separate *opacity*: Flow is per-dab buildup rather than a ceiling, a ceiling would need the kernel to track a per-stroke maximum, and Erase is the honest way back down |
| 75 | 2026-07-30 | **Per-layer adjustments will cover the pointwise set only; the pyramids stay global** | Asked for "every edit, mask by mask". Exposure, contrast, highlights/shadows, whites/blacks, vibrance, saturation, colour grading, the mixer and the curve are per-pixel functions — scaling them by coverage is well defined and one fused pass over baked per-layer coverages costs one pointwise pass. Clarity, dehaze and exposure fusion are 16–32-node pyramids each; a per-layer copy multiplies both the node count and the 6.4 GiB of cached intermediates that make an exposure drag 10 ms rather than 67, and it dies on arithmetic rather than on taste. The honest version of "clarity on a mask" is to render the pyramid **once** and let each layer blend toward the input by α × amount. Cap 8 layers at 46 MiB of baked coverage each ≈ 370 MiB. ⚠ **Refused: blend modes over rendered frames** — blending two tone-curved images is not the same operation as scaling a parameter and needs N framebuffers; layer opacity is a scalar into α, and the existing add/subtract/intersect are the mask blend modes |
| 76 | 2026-07-30 | **A mask may change any adjustment that is a function of the pixel alone — and Warmth is not white balance** | §2's rule is that the coverage scales the *parameter*, not the result, which is only well defined pointwise. Exposure, contrast, saturation and a colour cast pass that test and are now local. ⚠ **White balance cannot be**, and structurally so: temperature and tint are applied in `linearize`, at the head of the graph and **before the demosaic**, because the demosaic interpolates white-balanced data and the level a channel clips at moves with its multiplier — a local one means demosaicing the frame twice and choosing per pixel, which is a second pipeline rather than an adjustment. So the local controls are named **Warmth** and **Tint**, are a per-channel multiply renormalised on luminance so they move colour without moving exposure, and are deliberately not called Temperature and Tint. The contrast shares `develop_display`'s −2.5 log2 pivot; a second pivot would make a local contrast of zero visibly different from no local contrast near the crossover. ⚠ Taken **before** #75's stage 2 rather than after, because it defines what a layer *is* before multiplying layers, and carries none of the schema or graph risk |
| 77 | 2026-07-30 | **The adjustments are a catalogue, and each scope carries its own stage** | Every control was hand-listed where it was drawn — range, unit, decimals, default — which is fine until the same adjustment appears twice, and giving a mask more than exposure had just made the Local section a second copy of the Light section's list. `AdjustmentCatalogue` is the one definition and `AdjustmentGroup` renders any subset in any scope, so the global and local panels are the same specs through the same view. The binding table is a `switch` with no `default`, so an unbound id is a compile error rather than a slider that draws nothing — the way `lutStrength` once shipped dead. ⚠ **`stage` is per scope, not per adjustment**: global contrast runs in `develop_display` *after* the mask on the combined result while local contrast runs inside the mask node, so one stage per spec would make the ordering readout state one of them falsely. Found by writing the readout rather than by reading the code. ⚠ Refusals carry a **reason** and are generated from the table, so a control that stops being local-able says so the day it changes; a test fails if any refusal loses its reason, because a refusal without one is indistinguishable from a control that has gone missing |
| 78 | 2026-07-30 | **No sky matte from Apple, and no bundled segmentation weights — build a classical detector instead** | Checked rather than assumed: Vision offers people, subjects and saliency, and saliency is the wrong tool because sky is the *least* salient region in most frames. `AVSemanticSegmentationMatte` and the ImageIO auxiliary constants do include a sky matte, but they are read accessors for a matte the capturing iPhone embedded at shutter time — `AVCapturePhotoOutput` is the only producer and needs a live capture session, so an imported ARW will never carry one. ⚠ **Bundling a network is refused on the grounds already written for camera profiles (#44)**: sky appears in ADE20K, COCO-Stuff and Cityscapes, whose terms are respectively unclear, unclear and research-only, and weights inherit their training data's ambiguity however permissive the architecture's code is — no model is known whose architecture, weights *and* data all carry a clean redistribution grant, and it would add a Python/coremltools step for a solo C++/Swift maintainer. The answer is a classical gradient-energy sky detector (Shen & Wang 2013; Hoiem's geometric context before it) feeding the existing raster-matte path, ~80-90% right on daytime landscapes. ⚠ Its failure list — sunsets, water and glass reflections, white overcast against white buildings, night skies, sky through foliage — is acceptable **because a mask is a proposal inspected as an overlay and corrected with the brush**: the purple cast was dangerous because it was invisible, and visibly wrong and correctable is a different category. It must not claim anything beyond "smooth bright region connected to the top edge" |
| 79 | 2026-07-31 | **A raster matte is saved as a sibling 16-bit linear-grey PNG, referenced by a per-component id — and it is never regenerated on its own** | Every other mask kind is a handful of numbers in the sidecar; kind 4 is ~700k alpha values and was written nowhere, so a Subject, Person or Sky row reopened present and empty. ⚠ **Storage, not cache**: Vision's models move between macOS releases, so re-running on open would silently change a finished edit on an OS update — which also rules out an Application Support cache keyed by a path that dies the moment the photograph moves. Base64 in the XMP is refused because autosave rewrites that attribute 900 ms after any slider moves, and megabytes of text per settle is not a save path. So: `PHOTO.orion-matte-<uuid>.png` beside `PHOTO.xmp`, and what travels with a photograph is the raw, its sidecar and its mattes, all caught by a `PHOTO.*` copy. ⚠ **16-bit and the colour space is stated at both ends.** CoreGraphics colour-manages greyscale: write 0.5 through a Gamma-2.2 grey space and read it back as linear and it returns 0.735 — every feathered edge on every reopened photograph shifted, nothing crashing, nothing to see. That is the purple cast's shape exactly, and it is why the round-trip fixture is a **gradient ramp**: a binary matte survives a wrong colour space, a wrong bit depth and a wrong byte order, because 0 and 1 land on 0 and 1 however the curve between them is mangled. 8 bits was rejected because the case for it is "the guided filter probably averages the staircase away", and *probably* is not a thing to ship a mask on, for ~1.5× on data PNG compresses well anyway. A hand-rolled deflate blob was rejected as a bespoke container needing its own header, dimensions and versioning — PNG is self-describing, one ImageIO call each way, and openable in Preview the day a matte looks wrong. ⚠ **Files are immutable and the write precedes the reference**: regenerating mints a new id, so a half-written file can never clobber the only good copy, and the sidecar can never name a file that is not already on disk — the only reachable crash window leaves an orphan. Orphans are swept on open and **only after the sidecar has parsed**, because an unreadable sidecar yields no components and sweeping against that would delete every matte the photograph has. Deletion is never eager: a deleted component must be undoable without re-running a three-second inference. ⚠ A **missing file is reported, not swallowed** — leaving the row with no coverage changes the picture with nothing on screen saying why |
| 80 | 2026-07-31 | **Brush dabs are rejected a run of 64 at a time, by a host-built bounding box — and the rejection is provably bit-identical, not merely close** | The kernel looped every dab at every pixel: one texture fetch per dab per pixel, which is ~58 billion fetches for a 2400-dab stroke on a 24 Mpx frame and measured **2148 ms a render** against a 16 ms budget. ⚠ The cost is the **fetch**, not the arithmetic, so a faster inner test was never the answer — the fix has to remove fetches. Hierarchical bounding volumes, one level (Clark, CACM 1976; `research/brush-acceleration.md`): a box per run of 64 consecutive dabs in a 4 KB aux texture, skipping 64 fetches with one test. Measured 152→14.5 ms at 300 dabs and **2148→65.6 ms at 2400**. ⚠ **Runs of consecutive dabs, never a spatial partition**: paint is source-over and erase is destination-out and the two do not commute, so an index range preserves application order for free where a per-tile bin has to rebuild it with a stable scatter or a sort — three kernels and a prefix sum inside a static graph. ⚠ **The boxes are unexpanded and tested in the per-dab test's own expression shape**, because `fl(q·W) − fl(c·W)` is not `fl((q−c)·W)` — floating point does not distribute, so a box pre-grown by the nib radius would be a *different computation* and would disagree with the full loop on some pixel near some rim. Monotone rounding then gives the guarantee: when the block test fires, every dab in it takes the existing `continue`, which performs no floating-point operation on the accumulator, so the skip executes exactly the instruction stream the full loop would have. ⚠ The boxes are built from the **float32 texels actually uploaded**, never from the higher-precision values before the geometry transform, or the box can round tighter than what is stored and skip a real dab. ⚠ **A frame-filling scribble defeats it entirely** and returns to the old cost plus ~1.6%; that is not fixable with a tighter volume, only by not re-evaluating the whole stroke. Deliberately **not** done here: the incremental accumulator that would make painting O(1) in stroke length needs a persistent ~97 MB R32F texture per brush component and a host predicate deciding when the stroke's prefix is unchanged, whose failure mode is a stale accumulator rendering a completely plausible brushstroke — costed in `ROADMAP.md` rather than bolted on |
| 81 | 2026-07-31 | **Film grain is a precomputed correlated plate applied after the display transform, not noise added in scene-linear** | `research/film-grain.md`. Newson, Delon & Galerne (CGF 36(8), 2017) model the emulsion as a Boolean process of Poisson-distributed discs and that is the right physics — but their exact renderer is per-pixel Monte Carlo over the disc process, orders of magnitude outside 16 ms at 24 Mpx and several hundred lines against this project's 50–150 line node ceiling. Both hard constraints, broken at once. So: AV1's architecture (Norkin & Birkbeck, DCC 2018) — synthesise a correlated plate **once**, apply it per pixel with an intensity-dependent scale — carrying Newson's variance law `σ(Y) = amount·√(Y(1−Y))`, which is the Boolean model's own Bernoulli statistics and is zero at both ends, so `saturate` cannot truncate it into a mean shift. ⚠ **Monochrome**: luminance grain is what film looks like, per-channel grain is what a noisy sensor looks like. ⚠ **After the display transform, and the reason is not taste**: `develop_display` opens with `max(c, 0)`, which half-clamps zero-mean noise near black and lifts the mean, and AgX takes `log2` of its input, which turns small symmetric linear noise into large asymmetric shadow noise — and an amplitude defined against unbounded scene-linear EV would mean something different on every photograph and would move every time the exposure slider did. ⚠ **Keyed to the frame, not the output**, which comes free from running before geometry: a crop enlarges the grain exactly as enlarging more of a negative does. Two exports at different sizes therefore differ, and that is correct — the promise kept is the narrower true one, that the same edit at the same size renders the same bytes. ⚠ **Determinism is the dangerous part.** A hash of the pixel coordinate fails twice: the preview graph is a different grid, so preview and export would be different *realisations* rather than two resolutions of one field, and the preview would show full per-pixel variance where the settled render averages 16×16 — an order of magnitude grainier than the thing it previews. The plate plus an explicit `log2(gridStep/size)` level of detail makes both graphs sample one field. Also refused, each for a specific reason: hardware samplers (filtering precision unspecified across GPU families, so export could differ by device), `std::normal_distribution` and `generateMipmaps` (implementation-defined, so the plate would differ by toolchain), and grain applied after the dither. Controls are **Amount and Size only** — roughness is the plate's spectrum, which the fixed band-limit already encodes, and a chroma slider's correct value is always zero |
| 82 | 2026-07-31 | **The grain node disables to nothing at Amount 0, and the Bayer dither moves to whichever node writes the eight bits** | #81 costed grain as `develop:display` → `RGBA16Float` unconditionally plus one always-on node. Built that way it is a full-resolution pointwise pass, plus a doubled write on the node feeding it, on **every frame of every drag** — measured on `_PIC8220` as the exposure slider going from 3 nodes and 10.63 ms p95 to 4 nodes and **17.03 ms**, past the 16 ms M0 gate, and reported from the app as sliders getting slow before the bench was next run. Every other expensive feature in the graph already disables to nothing when it is off; grain is the same, and the only thing that made it different is that it is also the node that **quantises**. So the two facts move together in one place, `retargetOutputChain`: grain off → display writes `RGBA8Unorm` and dithers, grain on → display writes float and grain dithers, wide export → neither. ⚠ Deciding that in two places is how the narrow path ends up rounding twice, or not at all. The +194 MB #81 costed is now paid only while the slider is up; the resting cost is the idle node's own 93 MB. `testGrainWiring` pins that a tick costs the same as it did before grain existed, that Amount 0 is bit-identical to never having touched the slider, and that *something* still dithers |
| 83 | 2026-07-31 | **The brush cursor is round on screen, because the paint is round on the picture** | Reported live as "the circle is an OVAL". `CanvasLayout.brushCursor` built its outline as a circle in **normalized** coordinates and mapped it out, so on a 3:2 frame it drew 1.497× wider than tall — measured, and exactly the frame's aspect. ⚠ It was correct when written and its comment said why: the dab was measured in normalized coordinates too. Decision #62 folded `mask_brush.slang` into `mask_component.slang` and that fold moved the dab into **frame pixels** (`nibPx = brushRadius × min(shownW, shownH)`) so the brush would stop stretching under the Size slider. The paint went round; the outline over it did not. What kept it alive for two months is that the only thing tying the two together was a **comment naming a file that had been deleted** — nothing compiled against it and nothing tested it. `testBrushCursorIsRound` checks both roundness and that the radius equals `nibPx`, on landscape, portrait and square, at two zooms; reverting the shape fails 8 checks. ⚠ The **radial mask** is still an ellipse in normalized coordinates and is deliberately left that way for now — it has explicit per-axis semi-axes, so unlike the nib its shape is something the photographer sets, and changing the convention would change the meaning of `radius[1]` in every sidecar already written |
| 84 | 2026-08-01 | **Every gesture on the picture arms degrade-then-refine, exactly as every slider does** | Reported as "forced updates per stroke". `AdjustmentSlider` armed the preview graph through `AnalogTrack`; no canvas gesture ever did, so a slider tick rendered a quarter-linear preview while a brush stroke rendered the **full graph at full resolution** once per pointer event — on a photograph that gets more expensive with every dab already laid. Measured on `_PIC8220` with the new `paint` verb: 7.6 ms an event at 41 dabs and **27.3 ms at 246**, against **0.7 and 1.9** armed; and placing a radial mask carrying a local exposure, **13.0 ms a tick → 1.3**. The argument is the one already written on `AdjustmentSlider`: *every* slider arms it, not only the slow ones, because which controls are expensive is a property of the graph and it changes. The same holds for gestures. ⚠ **Arming happens after the mask hit test, not before**: a press that grabs no handle pans the picture, and arming first would swap the canvas to the preview texture for a gesture that is not an edit. ⚠ Four gestures — crop, spot, the curve editor, the grading wheels — are named and left for the performance audit rather than changed in the same breath: each swaps the canvas to a differently-sized texture mid-gesture with an overlay drawn over it, which is the shape of a bug this project has already shipped once, and the crop overlay's rectangle *is* the geometry being changed |
| 85 | 2026-08-01 | **`scenario paint` measures the stroke and refuses to arm the preview graph itself** | `brush` hands the engine one finished stroke in a single `setBrushStroke`; the canvas calls it again on every pointer event, appending. Nothing issued a stroke the second way, so the cost a photographer actually feels was unmeasured — this is piece 1 of ROADMAP's interaction-latency story. ⚠ The verb does **not** arm the preview graph, and that is the decision: a verb that armed it itself would report the same number whether `MaskOverlay` still called `beginInteraction` or not, which is a measurement that cannot observe the thing it exists to measure. Arming stays the `interact` verb's job so a scenario can time both sides. ⚠ What it does not cover is stated in the code and in the gap table rather than implied away: `Scenario` drives `Engine` and `CanvasLayout` and never a SwiftUI view, so **nothing asserts that a gesture arms** — the four unarmed ones were found by `grep`, not by a red test |
| 86 | 2026-08-01 | **Autorelease-pool discipline lives in `Resources.mm` and nowhere above it** | ARC is on for the engine's three `.mm` files, so strong members release correctly — but **ARC does not drain autorelease pools**, and nothing in this engine turns a run loop. Every autoreleased Metal temporary accumulated for the life of the process: measured at 393 B per texture descriptor, 1.6 KB per library load and 2.3 KB per kernel, which is **~0.64 MB per `DevelopPipeline` built** and so ~1.3 MB per photograph opened, since a photograph builds a full graph and a preview graph. ⚠ **`leaks` reports zero for this and is right to**: the blocks are still *reachable* from an undrained pool, so they are not leaks by its definition — only a footprint measurement finds it, which is why this survived a clean `leaks --atExit` on both the tests and the bench. ⚠ The shipping app was shielded **by accident**: `pushAndRender` runs on the main thread, whose run loop drains each cycle. The bench, the tests and the scenario runner are not, and moving a photo open to a background queue would have exposed it. Six `@autoreleasepool` blocks inside the Objective-C++ layer, which is where the file's own design says ObjC stays — no change to `Pipeline.cpp` or to Swift. Verified on the harness that found it: the same 15-iteration build/destroy loop went from **+8.92 MB, linear** to **+0.58 MB, flat from iteration 13** |
| 87 | 2026-08-01 | **A sidecar has three states, not two, and the sweep has to tell absent from unreadable** | `MatteStore.sweep` ran only inside the successful-parse branch of the loader, guarded by a comment that is correct about a sidecar which *exists and did not parse*: sweeping against that reads as "nothing is referenced" and would delete a photograph's selections. But a photograph that had **simply never been saved** fell into the same `else`, and a matte id lives only in a sidecar — so nothing on disk could ever reference its PNGs and they accumulated forever. Measured: **26 orphan mattes, 512 KB, beside one sample frame, the oldest three days old**, because pressing Subject mints a fresh UUID and writes a new file every time. The policy is now one function, `MatteStore.sweepAfterLoad` — parsed → keep what it references; **absent → keep nothing**; present-but-unreadable → keep everything. ⚠ It is a function rather than a rule repeated at each call site because it had already been written twice, in the app's loader and in the scenario runner's `reopen`, whose own comment claims it takes "the same steps `Editor.load` takes". Two copies of a delete policy is how one of them quietly stops matching. Tested where it lives, because `Scenario` cannot reach the no-sidecar case at all — its `reopen` needs a sidecar to reopen *with* |
| 88 | 2026-08-01 | **A stroke in flight lives outside anything observable, and is written to the document once when the hand stops** | Reported at **~155 ms a pointer event** in the app, against **0.9 ms** in the headless harness — a 170x nothing here could see, because `Scenario` drives `Engine` and never renders SwiftUI. `Engine` is `@Observable` and Observation is property-granular, so writing `maskComponents[i].brushStroke` per event invalidated every view that had read `maskComponents` — and `DevelopPanels` reads it in eleven places including a `ForEach` over the mask rows. Measured with `withObservationTracking` in the `paint` verb, which is a check that can fail: **41/80 events before, 0/80 after**. Everything the old path did per event was for the *record* of an unfinished stroke — re-flattening it with an array allocation per dab, then `onEdit?(state)` copying the whole `DevelopState` and handing it to `Autosave.note`, whose first act is a full structural compare. The dabs now go straight across the facade from `@ObservationIgnored` buffers, appended rather than rebuilt, and `maskComponents` is written exactly once in `endBrushStroke`. ⚠ The first draft also skipped `orion_engine_set_adjustments`, which looked free because dabs travel by their own call — but `brush_revision` rides in that block and is the only thing the engine compares to decide the mask node is stale. Without it the kernel never re-ran and the paint did not appear, at 0.0 ms an event and 45,000 fps, which is what "did no work" looks like when you are hoping for "fast". `repro/gesture-preview-agrees.txt` caught it |
| 89 | 2026-08-01 | **The codebase reads American; the on-disk keys stay as they were written** | Asked for directly. Renamed in two compile-checked stages — C++/Slang/C-header first (58 files, with `static_assert(offsetof(...))` as the tripwire, since the C++ struct and its Slang mirror bind by byte offset rather than by name), then Swift (28 files). ⚠ **Persisted keys are an interface, not prose, and are frozen.** `MaskComponentState`'s stored property names *are* the sidecar keys — its `Key` enum is deliberately named apart so the decoder can carry legacy spellings — and a renamed key does not fail to parse, it yields a perfectly valid mask sitting in the middle of the frame. `PresetGroup.colour`'s raw value is in presets.json, where `PresetStore.load` decodes the whole array or nothing, so one unknown value loses every saved preset at once. The three `legacy*` strings exist to read old files, and only two of the three fail loudly if swept. ⚠ The scenario grammar keeps **both** spellings permanently: renaming collapsed four existing alias pairs into duplicate cases and fourteen checked-in `repro/*.txt` failed at once. That grammar is an interface too — the session log has to stay runnable as a scenario. Americanising the keys is a schema migration with dual reads and a both-keys-present test, which is its own session with sign-off |
| 90 | 2026-08-01 | **The folder index is a two-stamp cache in Application Support, and every row is validated against the file it came from** | M1's `Epic: Cull` named a SQLite index and it was never built: every folder open re-opened every raw through LibRaw, re-extracted every embedded preview and re-read every XMP. Measured at **300 frames, page cache warm: 454–688 ms cold against 28–54 ms warm, 12.9–17.2×**, and an indexless open lands on the cold number, so the win is the index and not the disk. ⚠ **The danger is not a miss, it is a hit that is wrong** — a rating nobody set, or a thumbnail of a photograph that has since been replaced, either of which looks perfectly correct on screen. So: **two stamps per row, not one.** The raw's `(mtime, size)` validates the dimensions, the camera and the thumbnail; the *sidecar's* validates the rating, the reject flag and the label. A single stamp taken from the raw is the obvious design and it is wrong, because **rating a photograph does not touch the raw** — the row would report the first rating it ever read, forever. mtime is whole **nanoseconds** from one `fstatat`, never a `Date`: pressing 4 on a photograph rated 3 rewrites the sidecar to the same length in the same second, so second granularity is a hole you can drive a cull through. The thumbnail hangs off the raw and deliberately *not* the sidecar — it is the camera's embedded preview, which no develop setting has ever affected, and invalidating it on a star press would throw away a good thumbnail sixty times a session. ⚠ **Nothing is recorded that was not read back off disk**: `refreshMarks` stats, reads, and stats again, and files nothing when the two disagree, so the contents of an old file can never be filed under the identity of a new one. `Library.persist` calls it *after* writing rather than filing what it holds in memory, because a merge that failed would otherwise leave a rating in the index that is in no sidecar. **Thumbnails are blobs in the same database**, not a folder of files: eviction and payload then move in one transaction and the cache cannot leak files whose bookkeeping is gone — decision #87's orphaned mattes, already paid for once — and it is one file to delete when a disposable cache needs disposing (Kennedy, *35% Faster Than The Filesystem*, 2017; Sears/van Ingen/Gray, MSR-TR-2006-45). **Eviction is LRU against a 512 MB budget, down to 90% of it**, because culling is folder-shaped: you finish a shoot and leave it, so least-recently-used is the folder you are done with, where LFU keeps last month's favourite and insertion order evicts the start of the folder you are still in. `used` is only rewritten when an hour stale, so opening 500 frames is 500 reads and no writes. ⚠ **A schema change drops and rebuilds; there is no migration**, because a migration implies the data is worth keeping. ⚠ **Corruption degrades, never crashes and never deletes a sidecar**: `SQLITE_CORRUPT` and `SQLITE_NOTADB` alone — never `BUSY` or `LOCKED`, which mean another process holds the file — take the index out of service, and at open discard the database once and rebuild, refusing any path not named `*.sqlite3`. This does not relitigate #79, which refused an Application Support store keyed by a path "that dies the moment the photograph moves": it refused it for **storage**. For a cache a moved photograph is a key that misses, which costs one re-read and loses nothing. SQLite is used through `import SQLite3` from the macOS SDK rather than as a dependency — it is already on every Mac, it is public domain, and the alternative was a package for something the platform ships |
